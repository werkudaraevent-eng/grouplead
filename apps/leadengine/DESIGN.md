# LeadEngine design rules

The CRM follows Material Design 3 as rules and logic (structure, spacing,
hierarchy, colour, how a control behaves), never as a visual copy, with
global platforms (HubSpot, Salesforce, Linear, Notion) as references for
patterns Material leaves open. The sibling app records the same discipline
in `apps/sales-mission/DESIGN.md`; the two apps share a session, a colour
system and a set of shell behaviours, and should read as one product.

| Area | Rule (and where it comes from) | Where it lives |
|---|---|---|
| Tokens, not hex | Every colour is a token from `app/globals.css` (M3 tonal surfaces: `--background` surface, `--muted` surface-container-low, `--card` surface-container-lowest, `--sidebar` the navigation panel only, which the person can switch to dark independently of the content, so nothing outside the drawer and its menus may use it, `--border` outline-variant, `--primary`, `--secondary`, `--accent`, `--warning`, `--success`, `--danger` / `--danger-foreground` (Sales Activity's pair, for text that warns, such as an overdue closing date: `--destructive` is 3:1 on a card, too faint for small text, `--danger-foreground` 7:1), `--tonal` / `--tonal-foreground` the secondary container outside the drawer: the phone navigation bar's active pill and the More sheet's current row, the same pair as Sales Activity's and as the drawer's light indicator, but a token of its own because `--sidebar-*` turn dark with the panel toggle). A raw hex or Tailwind palette colour in a component is a bug, because it cannot follow a theme or a rebrand. A disabled control is a role too, on-surface at 12% and 38%, never a faded colour (see Buttons: disabled is on-surface) | `components/ui/*`, `components/shared/*`, `components/layout/mobile-nav-bar.tsx`, `SheetRow` in `components/ui/bottom-sheet.tsx`, the phone's lead cards in `features/leads/components/pipeline-phone-view.tsx` (`CHIP_TONE`, `dateTone`), a record page's stage chips (`STANDING_TONE` in `components/shared/record-page.tsx`) |
| Buttons: disabled is on-surface, not a faded colour | A button that cannot be pressed takes M3's disabled treatment, the same for every button in the product: one with a container (filled, destructive, tonal) turns it on-surface at 12% with its label and icon on-surface at 38%; an outlined one keeps no fill, its outline on-surface at 12% and its label at 38%; a text or icon button only its label at 38%. It was the button at half opacity, which left a disabled filled button a pale primary ("Save note" read as a washed-out blue that still looked like the page's call to action) and a disabled outlined one a faded version of itself; M3 greys every disabled button the same way so it reads as unavailable whatever its colour, and its 38% label is deliberately below contrast, since nothing is to be done with it (M3 buttons, states: disabled; M3 colour roles, on-surface). A disabled button takes no pointer, so a tooltip saying why ("This contact has no phone number" on Call) sits on a wrapper around it, and the reason is always said. The phone's labelled icon buttons on a record (Call, WhatsApp, Email, Website) take the same treatment on their tonal pill. `--foreground` is on-surface | `buttonVariants` in `components/ui/button.tsx`: `disabled:pointer-events-none disabled:shadow-none` on every button; `disabled:bg-foreground/12 disabled:text-foreground/38` on `default`, `destructive` and `secondary`; `disabled:border-foreground/12 disabled:text-foreground/38` on `outline`; `disabled:text-foreground/38` on `ghost` and `link` (the `dark:` fills cleared when disabled); the `disabled:` utilities outrank a call site's own `bg-`, `text-` and `border-` classes, so a button restyled where it is used (the record header's `OUTLINED_BUTTON`, `FILLED_BUTTON`, a destructive `AlertDialogAction`) is disabled the same way, and no call site overrides it (none sets its own `disabled:` look). `QuickAction` in `components/shared/record-page.tsx` (`bg-foreground/12 text-foreground/38`, the label at 38%). `Tooltip` in `components/ui/tooltip.tsx` wraps its child in a span, which is what shows the tooltip of a disabled button (`HeaderLinkButton`). Not buttons and unchanged: text fields, selects, checkboxes and the calendar's month arrows keep their own disabled styles |
| Form fields have a visible edge | `--input`, the outline role shadcn's `border-input` reads, was #ffffff, so every text field, select, textarea and the list search drew a white edge on a white card and read as bare text. M3's outlined text field puts its outline at the outline role, 3:1 against the surface (WCAG 1.4.11): #7F8993 (3.56:1 on the card), with `--field` #F3F5F8 as the resting fill one step off the card, and the primary as the focus ring. The same two tokens and the same fill as Sales Activity, so a field looks the same in both apps | `--input` / `--field` in `app/globals.css`; `bg-field` on `components/ui/{input,textarea,select}.tsx`, `SearchField` and `PhoneInput` (one fill behind the country button and the number; it painted `bg-background` behind the country), and on the record composer's date and person pickers (`FIELD_BUTTON` in `components/shared/record-composer.tsx`: `DatePickerField` and `SearchableSelect` are outlined buttons on the surface elsewhere, still open against this rule) |
| Page headers | One header for every page, the same in both apps, rule for rule. A 56dp row, level with the drawer's header, holds the title (20px, semibold, sentence case) and the page's actions centred on it; nothing sits above the title but, on a derived page, its parent. The drawer's header is that same 56dp, exactly, when the drawer is open (its collapse button fills the row's height rather than adding its own 56dp to the row's padding, which made it 72dp and set the page's title and buttons 8dp above the drawer's logo), so the two headers share one line and one bottom edge. **Top-level pages** (every item in the drawer, and the pages the account menu opens) carry no eyebrow: the drawer's active item and the title already say where you are, and the product's name is already in the drawer. **Derived pages** (a page under Settings, a record, a form that creates or edits one) carry their parent as one small line above the title inside the same row, in sentence-case label-medium (12px, medium, muted): "Pengaturan", "Pengaturan / AI", "Aktivitas"; "Settings", "Settings / AI". In LeadEngine, which has no Back button, the parent is a link; in Sales Activity, whose derived pages have "Kembali", it is text. **Record pages** that have a record header (LeadEngine's contact and company) carry a leading back icon button instead of the parent line: a 40dp icon button (←, named and tooltipped "Back to Contacts" or "Back to Companies") at the leading edge of the title row, centred on the 48dp avatar and the name, its arrow on the page's margin, where M3's top app bar puts navigation; it opens the list bare, so the list's remembered view comes back, as the parent link did. Pages under Settings keep their parent line. **The description** under the row follows what it says: a sentence that teaches (what the page is for, how it works) shows with a ✕ right after its last word, on whichever line that is, never parked at a column edge where it reads as belonging to nothing, and wraps at the reading width (768px, about the 100 characters past which a line gets hard to follow back), until the person closes it and then stays closed for them on every device, per page (`page-intro-<page>`; lists keep `list-intro-<list>`); a line that states facts (today's date, a record's type and place, its contact, "Updated 2m ago") is information and always shows. The title keeps its size when the page scrolls. **Below `lg`** the phone shell's top app bar carries the title (see Phone shell), so the page draws none of its own: the parent line becomes the bar's back arrow to the nearest parent with a page ("Settings / AI" on AI usage goes back to AI), the row keeps only the page's actions and is not drawn when there are none, and the description stays. (M3 top app bar: one title slot with its actions, the same bar in the same place on every screen; M3's type scale dropped M2's all-caps overline, so a location line is label-medium in sentence case; Linear, Notion, GitHub and Stripe put no product name above a page title, and Stripe and Vercel show a parent only on a page below it.) Two headers in one product, a 56dp row on the lists beside a taller block of eyebrow, title and description everywhere else, made the title jump on every move between drawer items. | `SettingsPageHeader` in `components/layout/settings-page-header.tsx`, the header of every page that is not a list: props `title`, `subtitle`, `intro` (a `pageIntroKey(...)` when the subtitle teaches; left out when it states facts, and then it is a plain `text-sm text-muted-foreground` line), `breadcrumbs` (the parent line is "Settings" plus every crumb but the last, each a `Link`; left out, the page is top-level and has no parent line) and `actions`. The sticky part is the one row: `min-h-14 py-1.5`, `px-4 sm:px-6 lg:px-8`, on `bg-background` (the shell's `<main>` surface it sits on; the four pages that painted `bg-[#f2f3f6]` behind it, Pipeline, Goals, a pipeline's and a goal's page, now paint the token too, so the row is the surface), its bottom edge `border-transparent` turning `border-border shadow-sm` past 12px of scroll, with no change of height or type size; the parent line `text-xs font-medium text-muted-foreground`, the title `text-xl font-semibold tracking-tight`, the actions trailing. Below `lg` the header announces `PageChrome title` and `backHref` (the last parent crumb with an `href`), the title block is `max-lg:hidden`, and the row is `max-lg:hidden` when there are no actions (the description's wrapper then takes `max-lg:pt-1.5`, so it starts 12px under the top app bar). Under it, not sticky, the description (`PageIntro`, or the factual line), shown on a phone too, and `pb-4` whether or not a line shows. Call sites and their keys: `app/(app)/settings/page.tsx` (top-level, `settings`), `settings/profile` (top-level: opened from the account menu and open without the Settings grant, so no "Settings" link to bounce from; `settings-profile`), `settings/ai` (`settings-ai`), `settings/ai/usage` ("Settings / AI", `settings-ai-usage`), `settings/announcements`, `settings/companies`, `settings/history` (its count moved to the footer, where it always shows), `settings/master-options`, `settings/pipeline`, `settings/recycle-bin`, `settings/registry`, `settings/segments`, `settings/usage`, `settings/users` (each `settings-<page>`), `settings-goals` in `features/goals/components/settings/goal-settings-page.tsx`, `settings-goal-configuration` on every goal's page (`GoalConfigurationPage` in `features/goals/components/settings/goal-configuration-page.tsx`, "Settings / Goals", Save Configuration as its action); factual lines on `settings/companies/[slug]` ("Settings / Companies"; the unit's type; the page now scrolls in `<main>` rather than in a box of its own), `settings/pipeline/[pipelineId]` ("Settings / Pipeline"; its stage count; the parent link replaced "Back to Pipeline Configuration", and the stage flow and tabs, once part of a sticky block, scroll with the page) and `settings/permissions` (the company whose matrix it is); `app/(app)/changelog/page.tsx` (top-level, `changelog`), which now scrolls in `<main>` like the rest; `app/(app)/settings/loading.tsx` draws the same row (`max-lg:hidden`). `PageIntro` in `components/shared/page-intro.tsx` (✕ with tooltip "Dismiss", 40px on a phone, 32px from `md`; writes through `markHintsSeen` to `public.user_hints` with localStorage as the fallback; `seen` passed by a list's page, otherwise read from the provider, and a close marks the key there so every instance of it closes). `IntroSeenProvider`, `useIntroSeen`, `useMarkIntroSeen` in `components/shared/intro-seen-provider.tsx`, filled in `app/(app)/layout.tsx` by `listSeenHints` (`lib/hints/hint-queries.ts`) in the same `Promise.all` as the profile and currency reads, narrowed by `pageIntroKeys` to `page-intro-*`, an empty list on any failure. `pageIntroKey`, `IntroPage` and `INTRO_PAGES` in `lib/hints/hint-key.ts`, tested in `hint-key.test.ts`; no migration. `ListPageHeader` in `components/shared/list-page-header.tsx` is the same row for Contacts and Companies (`listIntroKey`). A contact's and a company's page draw their own record header instead: the back icon button, the avatar, the name and the actions in one row, the facts under it (`RecordHeader` in `components/shared/record-page.tsx`, see Record pages). The dashboard's title in `features/leads/components/analytics-dashboard.tsx` is 20px, weight 600, `var(--foreground)`, -0.025em, at rest and scrolled, beside its factual line ("Performance Dashboard · pipeline · Updated …"), and below `lg` its whole first row (the title, Tools, the view switcher, Edit Dashboard) is `max-lg:hidden`: the top app bar says "Dashboard", its ⋮ holds the views and the tools, and the factual line stays as a line of its own above the filter chips (see The dashboard on a phone); the Pipeline's in `features/leads/components/lead-dashboard.tsx` is `text-xl`, and below `lg` its whole first row gives way to the phone shell (see Phone shell). Still open here against the rule: the dashboard's row is 68px at rest, because its factual line sits inside it, and 56px once scrolled, and its title is a greeting ("Halo, …") rather than the page's name; the Pipeline's row is 60px (16px above 36px buttons, 8px below); the changelog's column stays centred while its title sits at the page's leading edge; Role & Izin's copy is Indonesian in an English app; a lead's page keeps its own header, a back arrow beside the title on a desk (below `lg` the top app bar's arrow), rather than a parent line above it; a contact's and a company's pages have the approved record header instead of the 56dp row: the back icon button, a 48dp avatar, the 24px name and the line under it and the actions in one row, the facts below (see Record pages). |
| List pages | One shape for every list, the same in both apps (Sales Activity's Aktivitas, Prospek and Laporan; LeadEngine's Contacts and Companies), rule for rule. **Header**: one compact row, level with the drawer's 56dp header: the title at 20px and, centred on the same line, the one primary action filled at the trailing edge with the secondary actions (Export with the number of matches it will write, Import, where the list has them) as labelled outlined buttons beside it; no eyebrow or breadcrumb above it (the drawer's active item and the title already say where you are) and no padding beyond the row's own, because every pixel above the table is a row of records fewer (M3 top app bar: one row, the title and its actions centred on it). The description under the row only teaches (what the list holds, whose rows you see), so it shows with a ✕ icon button at its end (tooltip "Tutup" / "Dismiss") until the person closes it, and then stays closed for that person on every device, per list (M3: supporting text that has done its job should not cost every visit; Google Workspace feature discovery; Sales Activity's coach marks, "once per account, on every device"). An ⓘ beside the title was the other way and was not taken: it hides the sentence from exactly the newcomer who does not yet know to press it, and leaves a control in the title row for text a returning person never reads again. Below `lg` the header row is not drawn in either app: the title is in the top app bar, the secondary actions in its overflow menu and the primary one is the FAB, and the description is not shown. **Toolbar**: the search field (outlined, 8dp corners, leading icon, trailing clear, debounced), then the filter chips, "+ Filter" and "Clear all", in one group that wraps onto a second line when there is more than fits, so an applied filter is always in sight (M3 sets chips on a wide screen that way; scrolling is for a narrow one); "Clear all" clears the search too. **Saved views** are named URLs, one person's own, per list, chosen from one "Tampilan" / "Views" menu button at the trailing edge of the toolbar's first line (M3 menu; Linear's and Notion's view switcher): its label names the view the screen shows exactly, else "Tampilan" / "Views"; the menu lists "Tampilan awal" / "Default view" and each view, a check on the one shown and a star on the default, then the actions (save, save changes, save as new, rename, set or unset the default, delete, whose snackbar undoes it). A chip row of views above the toolbar was the earlier way and was dropped: it cost a 48dp row for one or two chips and moved the table down the moment the first view was saved. **The count** of matches sits at the leading end of the table's footer, level with the paging, and only while a filter narrows the list ("X dari Y" / "X of Y"; "Menyaring…" / "Filtering…" while it loads): unfiltered, the footer's range already says the total, and the toolbar holds controls only. A view is shown (checked, and named on the button) only when it is exactly what the screen shows (search, filters, sort, page size, columns); "Tampilan awal" / "Default view" is the list as it first opens, with no view chosen, and choosing it clears the query, the size and the columns; choosing a view writes its query and size into the URL and its columns into the columns menu. Each action shows only when it applies: save while the list differs from how it first opens and no view is in hand; save changes and save as new once the view last chosen has been changed; rename, default and delete for the view in hand (the one shown, else the one last chosen), headed by its name when it is not the one shown, so a changed view is never acted on blind; deleting a person's own shortcut asks nothing and the snackbar undoes it. The button is drawn whenever saved views exist as a feature, not only once one is saved, so saving the first view moves nothing. **View tools** sit at the trailing edge of the toolbar's first line: the "Tampilan" / "Views" button, outlined with 8dp corners and exactly as tall as the facet buttons beside it, its label cut with an ellipsis at 12rem and a trailing chevron; then the columns button, a 40dp icon button named by its tooltip. The columns button carries no badge (an M3 badge flags something new or waiting, not a setting): "N of M shown" is said inside the menu, where each column is a checkbox with a drag handle, with Reset (M3 menu with checkboxes; Gmail, HubSpot, Airtable); the choice is kept per list in the browser and travels in a saved view. **Table** (from `md`): the list fills the window under its toolbar and scrolls inside its card in both directions, so the header row stays at the card's top, the footer at its foot and the sideways scrollbar at its visible bottom edge rather than after the last row; the header row is opaque and sits over every body cell, frozen ones included, and its own frozen cells over the header cells that scroll under them (M3 data table; Airtable, HubSpot, Sheets); the selection box and the record's name frozen at the leading edge with one edge shadow, the row's own action frozen at the trailing edge, as wide as its widest button and carrying the mirror of the leading edge (the hairline and the soft shadow on its left), so a column scrolling under it reads as passing beneath rather than as a word cut off ("Indu", "Mec"), the name taking whatever width the other columns leave, the columns between them scrolling sideways inside the card, never the page; 52dp rows with one hairline between them; one line per cell, cut with an ellipsis rather than wrapped, the whole value in the cell's title; sentence-case headers at the body size in medium weight, carrying the label and the sort arrow and nothing else: the column that holds the default order shows its arrow muted and names the order in its tooltip, never in a caption beside the label; a header click steps ascending, descending, then back to the default order. **Footer**: on a desk the count at its leading end while a filter narrows the list, cut with an ellipsis before the paging would wrap; then, at the trailing edge, rows per page 25, 50 or 100, the range with the total, previous and next; no page numbers. **State**: the view is the URL, replaced rather than pushed; a new search, filter or sort keeps the sort and the size and returns to the first page; a cookie per list remembers the last query (without page or size) and a bare open redirects to it; a default saved view chooses the view only on the list's first open in a browser, when nothing is remembered, so a link or a remembered view is never overridden. **Phone** (below `md`): the list opens on its records: one card per record, search first, the filter chips behind one "Filter" button carrying the count, which opens a bottom sheet with the saved views to choose from at its top; what is applied repeats under the search as sideways-scrolling input chips (in LeadEngine the row fades at each edge it can still scroll toward, see Chips), and, in Sales Activity, the count ("170 aktivitas", "12 dari 170 aktivitas", "Menyaring…") stays under them, above the cards, where no footer is in sight; saving a view and choosing columns are desk work (M3 top app bar, search, chips, menu with checkboxes, data table and pagination; Gmail, Linear, HubSpot, Airtable, Notion); the search and chips scroll away as the reader reads down and return, pinned under the top app bar, the moment they scroll up (M3 top app bar enter-always; Gmail, Google Contacts), while the count stays where it was. Scrollbars are the thin, button-less kind in every browser (the standard properties apply only where the -webkit- ones do not, so Chromium on Windows draws no arrows into a rounded corner), and the table's vertical bar starts under its sticky header. The trailing action column's header is left-aligned like its buttons (M3: a header takes its column's alignment; only numbers go right). The paging footer is one slim 48px row and the page ends 16px under it. A menu never runs past the window (M3 menus): the columns menu caps its height at the room around its trigger with a 16px margin, keeps its title and reset in place, and scrolls only its list; the "Tampilan" / "Views" menu keeps the same margin and scrolls as a whole past 32rem. | `ListPageHeader` (one `min-h-14` row, the title and the actions `items-center`; on its page's wrapper `max-lg:hidden`, and it announces `PageChrome title` and `phoneMenu`, Export (with the count, "Exporting…" while it runs) and Import (for whoever may create), to the top app bar's ⋮; Add contact / Add company is `Fab`, and the page's root ends `FAB_CLEARANCE` above `<main>`'s foot while the FAB shows; `intro` with `PageIntro` in `components/shared/page-intro.tsx`, which writes through `markHintsSeen` to `public.user_hints` with localStorage as the fallback, the page's server half reading `hasSeenHint` in `lib/hints/hint-queries.ts` so the first render is right, keys from `listIntroKey` in `lib/hints/hint-key.ts`: `list-intro-contacts`, `list-intro-companies`; no migration) on `app/(app)/contacts/contacts-list.tsx` and `app/(app)/companies/companies-list.tsx`, and `ListPageSkeleton` drawing the same one row; `ListToolbar` (desk row from `md`, `PhoneFilterBar` below it on `PhoneFilterFrame` in `components/shared/phone-filter-frame.tsx`, the phone's one filter pattern, shared with the Pipeline and the Dashboard), `SearchField`, `FilterBuilder` (`onClearAll`, `extraActive`), `FilterChip`, `filterValueLabel` in `filter-builder-types.ts`; `ViewsMenu` in `components/shared/saved-views-bar.tsx` (the desk's one Views button: outlined, `h-9` like the filter chips and the search, 8dp corners, the name cut at `max-w-48`, a chevron; a `DropdownMenu` aligned to the end with `collisionPadding={16}`, at most 32rem tall and scrolling as a whole: "Default view" and each view as a `menuitemradio` with a leading check and a star on the default, then Save view, Save changes, Save as new view, Rename, Set as default / Remove default and Delete, under "Changed from “…”" when the view in hand is not the one shown; the name dialogs are `SaveAsDialog`), `ToolbarIconButton` (no badge), `ColumnsMenu` (checkbox list); `SavedViewsBar` (the phone's chips, drawn `md:hidden`); `ListControls` in `components/shared/list-toolbar.tsx`, which wraps the phone's saved-view chips and the toolbar on both lists: from `md` the toolbar's band as before (`md:static`, `pb-4`, `border-b`), with its 12px top padding kept up to `lg` (`lg:pt-0`), where the header row above it is not drawn; below `md` one `sticky top-0 z-20` block on `bg-background` (above the cards; every sheet, menu and dialog is portalled at z-50), edge to edge because the page gives each band its own side padding (`px-4 sm:px-6`), `pt-3`, `gap-3` between the saved-view chips, the search row and the applied chips, then `pb-[11px]` and its 1px `border-b`, transparent until it is pinned and shown, down to the first card, whose list has no top padding (`pb-3` only), with no header row above it below `lg`, so every step down the phone is 12px and the block pins right under the top app bar, which sits above `<main>` rather than in it; slid away it is `-translate-y-full`, `inert` and `pointer-events-none`, on `transition-transform duration-200 ease-out motion-reduce:transition-none`; `useQuickReturn` in `hooks/use-quick-return.ts` decides, only below `md` (an empty anchor before the block gives its own place, which its pinned box no longer does; one passive scroll listener on `<main>`, read once per animation frame; held while a visible focus is in the block, the search's or a keyboard user's, or while a trigger in it is `aria-expanded`, the Filter sheet or a menu open), through `nextQuickReturn` in `lib/ui/quick-return.ts` (shown while its own place is less than its height above `<main>`'s edge; past that, down hides and up shows once the travel in one direction reaches 8px, `QUICK_RETURN_JITTER`; overscroll bounce ignored), tested in `quick-return.test.ts`; `PhoneFilterFrame` spaces its rows with `gap-3` and gives the applied-chip row `-my-1 py-1` (room for the ✕'s grown target inside a row that clips, without adding to the 12px) and a bleed to the band's edge at `sm` too; `ListPageSkeleton` draws the same phone spacing, and no header row below `lg`, so the list does not jump when it loads. LeadEngine's phone shows no count above the cards (the range sits in the footer at the list's end), so nothing is left behind when the block scrolls away; `useListViews` (`hooks/use-list-views.ts`, `applyDefaultOnLoad`, `canonical`, `available`, `marked` / `target` / `dirty` through `markedView` in `lib/lists/list-state.ts`, `clearView`, `deleteView` with Undo in the snackbar, `unsetDefault`) on `public.user_list_views` through `app/actions/list-views-actions.ts` (`clearUserListViewDefaultAction` for Remove default; no migration); `EMPTY_LIST_STATE` with the columns' reset for Default view, `isPlainState` and `columnsKey` for whether the screen differs from it (Default view's check, Save view, the default view's first open), in `lib/lists/list-state.ts`; `frozenCell`, `SELECT_COL`, `MENU_COL` and `MENU_CELL` (the row menu, sticky at the trailing edge with the leading edge's mirror) in `components/shared/list-table.tsx`, `h-13` rows in `components/ui/table.tsx`; `ListFooter` (`narrowed`, `unfiltered`; the count from `matchCountLabel` in `lib/lists/list-state.ts`, in an `aria-live="polite"` span at the desk row's leading end, `text-sm text-muted-foreground`, truncating; the count before the filters is `unfiltered` from `runListPage`); `useListUrl` and the `le-view-*` cookies. Sales Activity's side of the same rule is its own "List pages" row. Still open here against the rule: below `md` `SavedViewsBar` draws its chips above the search instead of at the top of the Filter sheet, its ⋮ asks with the browser's `confirm()` before deleting (the snackbar then offers Undo, as on a desk), and a chosen view's chip stays marked while the view is being changed (`activeViewId`), with Save changes beside it; the desk footer is 56px (`h-14`), not the slim 48px row; `frozenCell` holds the first two data columns, the name and the one after it; only Notes and Address carry their full text in a title; and the name column can be unticked in the columns menu, where Sales Activity locks it; every column, the name included, is given a fixed width (`frozenCell`), so the name is not the column that takes what the others leave; and with no sort chosen the list runs in its default order (name A to Z) with no muted arrow on Name and no tooltip naming that order |
| Data table | Header on surface-container-low with its labels in sentence case at the body's size in medium weight (M3 title small / label large; HubSpot, Attio, Linear, Notion), never small tracked capitals, which read as a different typeface from the page; 52dp rows, one hairline between rows, hover as a tonal wash, selection as a primary tint, frozen leading columns (select and name) with one edge shadow, the row's actions as a trailing round icon button that appears on hover or focus, in a column frozen at the trailing edge that carries the mirror of that shadow on its left (it covered the column scrolling under it with no separation), and a fixed-height footer with rows per page, the range with the total ("1–25 of 1,196 contacts") and previous / next icon buttons at the trailing edge, and at its leading end, only while a search or filter narrows the list, how many of the list match ("170 of 1,196 contacts"; "Filtering…" while it loads); no page numbers, because a list sorted and filtered in the database is read forward and back, not jumped into (M3 data table and pagination; Gmail; Sales Activity's `MissionPagination`). Rows per page is a plain dropdown of 25, 50 and 100. The row opens its record and the name is a real link to it, for the keyboard, a hover preview of the address and a new tab: any click on the row navigates except on a control inside it (checkbox, row menu and its items, a website or social link) or while text is being selected, and Ctrl or Cmd opens a new tab (Sales Activity's "A table row opens its record"; Gmail, HubSpot, Linear, Pipedrive). A selection belongs to the page it was made on and clears on any change of page, filters, sort or business unit (Gmail), so a bulk action never reaches rows nobody can see; the header checkbox reads this page only, indeterminate when part of it is ticked. A "Has …" filter is a true/false field with the boolean operators. No row-number column: a number that changes with every sort names nothing and took width from the frozen name (HubSpot, Salesforce, Pipedrive, Attio leave it out). The sort arrow shows on the sorted column only, and on another while it is hovered or focused; every column with an order sorts in the database, a related name (company, owner, parent) included, empty values last either way; Social links has no order and no arrow. A header click steps ascending, descending, then back to the default order (name A to Z), with a tooltip naming what the next click does; toggling only between the two directions left no way back (M3 data table sorting; MUI Data Grid, AG Grid, Airtable; Sales Activity's `SortHeader`). While the next page loads the current one stays and dims; the skeleton is for the first load only. A record that needs attention (Needs details) carries a small warning icon with a tooltip beside its name, never a labelled pill that truncates the name. Below `md` the table becomes one card per record, in Sales Activity's card anatomy (see Cards on a phone); the card opens the record the way a row does. Cards carry no checkboxes: bulk selection, export of a selection and merge are desk tasks, and Export of every match stays in the header's overflow | `components/ui/table.tsx`, `components/shared/list-table.tsx` (`SortableHead`, `RowMenu`, `ListEmpty`, `ListCardSkeleton`, `nextSort` in `lib/list-sort.ts`), `ListFooter` in `components/shared/list-footer.tsx` (`loadMoreStep`, `pageRange`, `matchCountLabel` in `lib/lists/list-state.ts`), `useRowLink` in `hooks/use-row-link.ts`, `InitialsAvatar`, `NeedsDetailsMark` in `status-badge.tsx`; the tables in `contacts-list.tsx` and `companies-list.tsx` (their cards: see Cards on a phone) |
| Cards on a phone | Below `md` Contacts and Companies show one card per record in Sales Activity's card anatomy, rule for rule (its prospect, activity and report cards; its rows "Who is on it, at the card's foot" and "Lists on a phone"). An outlined card with 12dp corners on the card surface, 12px from the next. **Top**: the headline, the record's name in semibold on one line, cut with an ellipsis, the Needs details mark beside it; under it one supporting line, 12px and muted (company · job title for a contact, sector · line industry · city for a company; "No company" / "No sector" when there is none); 8px lower the key fact, 14px on one line (email · phone, or phone · website; "No email or phone" / "No phone or website", muted, when there is none). **A hairline**, then **the footer**: whoever owns the record at the leading edge, as an avatar and the name, a long name shortened to the first name and initials ("Setyorini D. I. H.", Sales Activity's rule, the whole name in its title), or "No owner" in the muted ink; the card's one action at the trailing edge, a 44px target: the ⋮ (Edit, Add contact and Delete on a company; Edit and Delete on a contact), since neither list has a call or WhatsApp action of its own to put there. No leading initials tile: Sales Activity's cards have none, the person is in the footer, and the tile took 44px from the name. Every card has the footer, so who owns it is always in the same place. The whole card opens the record the way a table row does and the name is the real link; the ⋮ never opens it. No checkboxes and no long press: bulk selection is desk work here (see Data table). The loading skeleton is the same card, so nothing moves when the records arrive (M3 card: headline, supporting text, a divider, supporting content at bottom-start and actions at bottom-end; Google Meet and Chat, Trello and Jira put who at a card's foot) | `RecordCard` and `RecordCardOwnerLine` in `components/shared/record-card.tsx` (`rounded-xl border bg-card`; body `p-4`: headline `truncate font-semibold`, supporting `truncate text-xs text-muted-foreground`, fact `mt-2 truncate text-sm`; footer `flex min-h-11 items-center justify-between gap-3 border-t px-3 py-2`, the owner `InitialsAvatar size="sm"` (28px) and `text-xs font-medium` after an sr-only "Owner:"); `shortPersonName` in `lib/person-name.ts`, tested in `lib/__tests__/person-name.test.ts`; the `space-y-3` lists in `app/(app)/contacts/contacts-list.tsx` and `app/(app)/companies/companies-list.tsx`, the ⋮ `RowMenu className="h-11 w-11 opacity-100"`; the owner's name and photo come with the list's own read, `owner_name` / `owner_avatar_url` on `contact_list_rows` and `client_company_list_rows`, or the `owner` embed on the base-table fallback (`lib/lists/contact-list.ts`, `lib/lists/company-list.ts`), no new query; `ListCardSkeleton` in `components/shared/list-table.tsx`, which `ListPageSkeleton` draws too. Differs from Sales Activity in one thing: the avatar keeps LeadEngine's tone per name (`InitialsAvatar`, the same colour as in the Owner column), where Sales Activity's `PersonAvatar` is neutral |
| Create and edit forms: full-screen on a phone, a side sheet from `md` | A record's create and edit form (Add and Edit company, Add and Edit contact, wherever they open: the lists, a record's page, the company and contact pickers in the lead form) is a side sheet only where there is room beside it for the page. Below `md` it is M3's full-screen dialog, the shape Sales Activity's create pages have on a phone (`/workspace/activities/new`, `/workspace/prospects/new`): the whole screen, the window's width by `100dvh`; a 56dp header, plus the status bar's inset, with ✕ at the leading edge and the title at the top app bar's 17px (Layout, for whoever may change the form, at the trailing edge from `sm`); the fields scrolling in one column with 16px gutters; a bar along the bottom that never scrolls away, above the home indicator, with Cancel as a text button and the filled primary (Create company, Create contact, Save changes) taking the rest of the row, both 48dp. The ✕, Cancel and Esc all close the same way (a changed contact asks first). The ✕ takes the first focus on a phone, so the keyboard does not rise over the form before the person chooses a field. From `md` the side sheet as it was: 576px at the right edge, the title over its description, Layout, the sheet's ✕ at its top right (the header now keeps Layout clear of it), the Esc hint, Cancel and the primary at the trailing edge. The side sheet on a phone left a strip of the list beside it and cut fields off at its right edge. Fields fan into two columns from `sm` only, and a field that spans both says `sm:col-span-2`: the company form's bare `col-span-2` gave the phone's one-column grid an implicit second column, which set the fields between the wide ones in pairs and ran the phone field over Website. The phone field never grows past its cell: the country button is compact and keeps its size, the number takes the rest and may narrow, every wrapper may shrink. (M3 dialogs: full-screen on compact windows, a side sheet on medium and expanded ones; Sales Activity's "Detail and forms on a phone" and `FormActionBar`) | `FormSheetContent`, `FormSheetHeader`, `FormSheetBody`, `FormSheetFooter` and `FORM_SHEET_CLASS` in `components/shared/form-sheet.tsx` (phone `inset-0 h-dvh w-full max-w-none border-0`; from md `md:inset-y-0 md:left-auto md:right-0 md:h-full md:max-w-xl md:border-l`; the header's leading `SheetClose` `md:hidden` in an `h-14` row under `pt-[env(safe-area-inset-top)]`, and from md `md:py-4 md:pl-6 md:pr-12`; the description `max-md:sr-only`; the body `px-4 py-4 md:px-6 md:py-5`; the footer `pb-[max(0.75rem,env(safe-area-inset-bottom))]`, the buttons `h-12 md:h-9`, the primary `flex-1 md:flex-none`, the Esc hint `hidden md:block`); the sheet's own ✕ hidden below md through `closeClassName` on `SheetContent` in `components/ui/sheet.tsx`; `AddCompanyModal` in `features/companies/components/add-company-modal.tsx` (`sm:col-span-2` on Company name, Account status, Address, Country, Owner), `AddContactModal` in `features/contacts/components/add-contact-modal.tsx`; `PhoneInput` in `components/shared/phone-input.tsx` (`min-w-0` wrappers, the country button `shrink-0 gap-1 px-2 has-[>svg]:px-2`, the number `flex-1 min-w-0`), also used on `app/(app)/settings/profile/page.tsx` and in `features/users/components/edit-user-modal.tsx`. Still open here against the rule: the company form's Country picker is an outlined button on the surface rather than a field on `bg-field`, and a failed submit finds the first missing field with `scrollIntoView` |
| Labels are sentence case | Every label in a form is written in sentence case in one style: 13px, medium, the required mark a red * after it (M3's type scale has no all-caps label; Sales Activity's "Labels"). A tenant's custom field (Settings → Layout) has the same label as the built-in fields, 14px above its control like theirs; it was 12px tracked capitals ("SEGMENT TIER *", "SEGMENT", "ACCOUNT") beside "Segment tier *". The name an admin gave a field or a form section is shown in sentence case ("Segment Tier" reads "Segment tier", "Contact Methods" reads "Contact methods"), while acronyms and mixed-case names ("PIC", "WhatsApp") keep their capitals | `FormFieldLabel` in `components/shared/form-field-label.tsx`; `DynamicField` in `features/leads/components/dynamic-field.tsx` (the company, contact and lead forms; `grid gap-2 space-y-1.5` as a native field's `FormItem`, the label tied to its control by `useId`, `aria-required`); `sentenceCaseLabel` in `lib/label-case.ts`, tested in `lib/__tests__/label-case.test.ts`, also on the section headings of `AddCompanyModal` and `AddContactModal`. A record's page labels its properties the same way, at the source: the shared inline fields (`FieldShell` in `components/shared/inline-edit-field.tsx`, on a contact's and a company's About card the label 13px muted beside the value on a desk and 12px above it on a phone); they were 11px tracked capitals ("SEGMENT TIER", "LINE INDUSTRY"), as were the Company page's number cards and table headers and the contact's and company's Timeline event kinds ("FIELD UPDATED"), which the record pages replaced (see Record pages). Still open here against the rule: the lead form's section headings are Title Case (`formatTabLabel`, "Project Details"); a lead's page heads its detail groups and draws its badges in tracked capitals (`features/leads/components/lead-detail-layout.tsx`), and its Timeline's event kinds and Log Activity labels are tracked capitals (`features/leads/components/timeline-tab.tsx`) |
| A list's view lives in the URL, runs in the database, and is remembered | Contacts and Companies no longer load a whole table to search, filter, sort and page in the browser: the search, every filter, the sort and the page run as one query per page with an exact count (and, while a search or filter narrows the list, beside it the exact count of the list without them, for the footer's "170 of 1,196"), on the flat views `contact_list_rows` / `client_company_list_rows`, which carry the related names (company, owner, parent) as columns so they search, filter and sort like any other; the filter choices (Sector, Owner, Company, Country, Contact source …) come from small distinct reads of the same rows, not from the page on screen. Text compares without case and a negative filter keeps records with no value, as before; dates are whole WIB days. The view is the URL (`q`, one parameter per filter such as `sector=eq:Hotel`, `sort=owner:desc`, `page` from 0, `size`), replaced rather than pushed and without a scroll, so a filtered page can be linked, survives a refresh, and Back leaves the list; a new search, filter or sort keeps the sort and the size and starts from the first page (Sales Activity's lists). The list reopens as it was left: a cookie per list keeps the last query without page or size, and a bare `/contacts` or `/companies` redirects to it; "Clear all" leaves only the sort in it (Gmail, Linear, Jira; Sales Activity's "Quick filters and remembered views"). Saved views are named URLs: choosing one from the Views menu writes its search, filters, sort and size into the URL (and its columns into the columns menu), and "Default view" there is the list as it first opens (no search, no filters, no sort, 25 rows, the default columns); "Save view" and "Save changes" save what the URL shows. On a bare open the remembered view wins; a saved default view chooses the view only on the list's first open in a browser, when nothing is remembered, and the Views menu names and checks a view only while it is exactly what shows, so a link or a remembered view is never overridden and never mislabelled. Export writes every match of the current search, filters and sort (up to 5,000, in the list's order), and its button counts them. Rows are what the person may see (row security, the session's own rights), narrowed to the active business unit plus unassigned records, which row security shows to everyone and the Add dialog creates; the holding view is not narrowed. Before the views' migration the pages read the base tables instead (related-name filters through an id lookup, a search reaching at most 100 related names, a related order that falls back to name order if refused), so a deploy that precedes the migration never breaks a list. Below `md` the list grows with "Load more" up to 100 rows and then steps pages with Previous and Next (Sales Activity's "Lists on a phone") | `lib/lists/list-state.ts` (URL parse and write, transitions, paging, saved-view keys), `lib/lists/postgrest-filters.ts` (operators as PostgREST terms), `lib/lists/list-plan.ts` (`planList`), `lib/lists/contact-list.ts`, `lib/lists/company-list.ts`, `lib/lists/list-queries.ts` (view first, base table fallback; `unfiltered` in `ListPageResult`, null when nothing narrows the list), tests beside each; `app/actions/list-page-actions.ts`; `lib/lists/view-cookies.ts` and `remembered-view.ts` (cookies `le-view-contacts`, `le-view-companies`), read by the server `page.tsx` of each list; `useListUrl` (`hooks/use-list-url.ts`, `history.replaceState`), `useListPage` (`unfiltered`) / `useListOptions` (`hooks/use-list-page.ts`), `useListViews` (`applyDefaultOnLoad`, `canonical`); `sharedScopeTerm` in `utils/supabase/scoped-query.ts`; migration `20260923150000_contact_company_list_views.sql` |
| Chips | Every chip has 8dp corners, never a pill (M3 chips; Sales Activity's rule "Chips are M3 chips, not pills"). A filter chip is outlined while it does nothing and, once it narrows the list, takes a primary tint (`border-primary/50 bg-primary/5`) with a leading check and reads "Label: value" (a negation says so: "Sector: not Hotel"), with an ✕ to drop it; the same 36px chip as Sales Activity's facet buttons. On a phone the applied filters repeat under the search (on the Dashboard, under its Filter button) as input chips (32px, the ✕ with a 44px hit area) in one row that scrolls sideways, bleeds to the screen edge and fades at each edge it can still scroll toward: the left once the row has left its start, the right until its end is reached, neither when everything fits, so a chip cut by an edge reads as "more this way" (M3 input chip, chip carousel and scrollable tabs; Sales Activity's `FilterChip` and `.chip-scroll`, which fades the right edge only). **Every row that scrolls sideways fades the same way**: the applied chips on the lists, the Pipeline and the Dashboard, the saved-view chips, the dashboard's filter row between `md` and `lg`, the Pipeline's stage tabs, and on a record page the tabs when they do not fit a phone, the composer's type pills and History's filter chips. Each fade is a 2.5rem mask whose stops JS sets from the row's own scroll position (`data-fade-left` / `data-fade-right`, kept true on scroll, on a resize of the row or anything in it, and when its children change; a row whose overflow does not scroll at that width, a desk's wrapping row, never fades), easing in and out over 150ms and at once under reduced motion; a row with a surface of its own keeps the surface and its hairline on a wrapper, since a mask would let what passes under it show through (the stage tabs' pinned bar). Choice chips (saved views on a phone, roles, how a Pipeline filter compares, the date range's quick ranges; on a record page a call's outcome, a meeting's in person or online, and History's kinds, one radio group each with arrow keys) are the same shape, tonal with a check on the one selected; on a desk the saved views are chosen from the Views menu (see List pages) | `filter-builder.tsx`, `filter-chip.tsx`; `.edge-fade` in `app/globals.css` (with `@property --edge-fade-left` / `--edge-fade-right` for the ease), `edgeFadeState`, `attachEdgeFade` and `EDGE_FADE_PX` in `lib/ui/edge-fade.ts`, tested in `edge-fade.test.ts`, `useEdgeFade` in `hooks/use-edge-fade.ts`, on `PhoneFilterFrame`'s chip row (`components/shared/phone-filter-frame.tsx`), `SavedViewsBar`, the dashboard's filter row (`analytics-dashboard.tsx`), the stage tabs (`pipeline-phone-view.tsx`), and `RecordTabs` (`components/shared/record-page.tsx`), `KindPills` and `ChoiceChips` with `scroll` (`components/shared/record-composer.tsx`; History's chips in `RecordActivityFeed`, `record-activity-feed.tsx`); `.chip-scroll` stays in `app/globals.css` for anything still asking for it; `ChoiceChips` in `components/shared/record-composer.tsx`, `SheetChoiceChip` in `features/leads/components/sheet-choice.tsx` and the quick ranges of `DateRangeChoices` in `components/shared/date-range-filter.tsx`; `saved-views-bar.tsx`, `settings/permissions/page.tsx` |
| A field reading is a badge, signed | A judgement sent from the field (a contact's DISC communication style) sits in About this contact as a small tonal badge (`bg-primary/10 text-primary`, the filter-chip tint) with its meaning beside it, the rep's note under it in their own words, and who assessed it and when in the meta size; it is one person's impression on one day, so it is never rendered as a status or a fact, and it is replaced by a newer reading rather than merged (Crystal Knows and Humantic AI beside a HubSpot or Salesforce contact; M3 assist chip, supporting text) The badge sits inline at the start of the meaning's first line, on the text's own 20px line height, so a long meaning wraps under it rather than dropping to a line of its own; note and signature follow 4px apart. Every label : value row uses one line height (20px, the phone's label 16px), so stacked or wrapped values keep an even rhythm. | `DiscValue` in `features/contacts/components/contact-detail-page.tsx`, a row of About this contact ("Communication style") that is left out when there is no reading (see Record pages); `readDisc` / `discSummary` in `features/contacts/lib/contact-record.ts`, tested in `features/contacts/lib/__tests__/contact-record.test.ts`; written by `POST /api/v1/contacts` into `custom_fields.disc` |
| The AI bill is shown where the connection is set, in tokens, as a pace | Every call to the proxy from either app writes one line to a shared log (app, feature, model, tokens in and out, ok), and Settings → AI → Usage (its own page off a header button, because a form ends at its Save and a read-only ledger is another task; OpenAI's and Anthropic's consoles keep Usage beside the keys, not under them) shows: the last 7 and 30 days as calls and tokens, then what a week and a month cost at the last week's pace, then the split per feature and per model, because the person paying wants the monthly token figure before the invoice, not after (OpenAI's and Anthropic's usage pages, Vercel AI usage, Salesforce's Einstein request consumption all sit beside the credentials). Tokens, never currency: the price is the proxy's and would go stale here. A log younger than a week projects from the days it covers, an empty log says so instead of showing zeros, and failed calls are counted but carry no tokens. One plain table set, not a chart | `public.ai_usage` (migration `20260921090000_ai_usage.sql`, service role only), `recordAiUsage` / `summarizeUsage` / `readAiUsage` in `lib/ai/ai-usage.ts` (written by `askAI`, `analyzeDashboard`, the Save probe), `AiUsageCard` on `app/(app)/settings/ai/usage/page.tsx`; Sales Activity mirrors it at Pengaturan → AI → Pemakaian |
| Integration settings: written keys, tested connections, chosen from what the endpoint reports | A connection to an outside service is one page in two cards: the connection (endpoint, key), then what it offers (models). The key field is empty on every visit and names the stored key only by its last four characters, because a secret is written and never read back (HubSpot integrations, Zapier connections, Vercel environment variables); "Test connection" makes one real request, reports the outcome as a sentence with a check or a cross, and fills the model pickers from the answer, so a person chooses among models the endpoint actually serves (Metabase database test, Grafana data sources); a picker falls back to a text field when nothing is listed, and a stored value the endpoint no longer lists stays selectable. Saving tests again so the stored status describes what was stored, and also asks each chosen model for one word, because a model can be listed and still not answer (a reasoning model given a small token budget answers with nothing); that failure belongs on the settings page with the model's stop reason in a sentence, never as a code on the dashboard. The same connection is shared with Sales Activity and shown identically there | `AiSettingsForm` in `features/settings/components/ai-settings-form.tsx` under Settings → AI, `app/actions/ai-settings-actions.ts`, `lib/ai/ai-proxy.ts`, `lib/ai/ai-settings.ts` (Vault-backed key via `fn_ai_store_key` / `fn_ai_read_key`, service role only); `lib/ai-client.ts` resolves the stored connection with the AI_* env as fallback |
| Settings index: a grouped list of destinations | Settings is a list of places to go, so it is an M3 list: subheaders name the groups (Configuration, Workspace, Adoption, Administration) with one supporting sentence each, and every page is one two-line list item at 72dp (leading icon in a 40px neutral container, headline, supporting text clamped at two lines rather than one, because a one-line clamp cut every description to a few words on a phone, trailing chevron). Grouped lists are what Android Settings, Google Account, Salesforce Setup, HubSpot, Linear and Notion use; a grid of equal cards makes the eye zig-zag and M3 keeps cards for self-contained content of mixed kinds. The chevron says the row opens a page inside the app (the ↗ arrow is only for leaving it). Icons are neutral and tint to primary on hover; no status colour decorates a row. Rows are filtered by the grant each page needs, and the inline Currency display row is drawn to the same measurements. The list stops at `max-w-4xl`, a readable line, instead of the 1200px a data table needs. Settings is a top-level page, so its header ("Page headers") has no parent line, and its dismissible description is the only line under the title; the first group starts 16px under the header, as every page's content does. Same pattern and measurements as Sales Activity's Pengaturan | `app/(app)/settings/page.tsx` (`sections`, `SettingsRow`, `SettingsAccessSkeleton`), `CurrencySettingsRow` in `features/settings/components/currency-settings-card.tsx`, `components/layout/settings-page-header.tsx` |
| Announcing a release | A release note nobody opens is not an announcement. Slack, Notion, Figma and Gmail announce a release once per person with a dialog of two or three highlights, each with a way straight to the feature, and a text button to put it off; nothing sequential, nothing on every login, never for every fix. The control is two layers, as those teams run it (highlights written with the feature, switched on from a panel without a deploy): a changelog entry may carry an `announcement` block, and Settings → Announcements lists those newest first, one row each with a Switch (48px hit area; on a phone the text wraps beside it) and an Announce again text action that asks first because it reaches every account (M3 dialog for a consequence that cannot be taken back). A release ships off unless it is everyone's business. The dialog, "What's new in LeadEngine", shows on the dashboard only, after the page settles, at most three items, so a person who went straight to a record is never interrupted; on a desk it is an M3 dialog, below `lg` (where the shell drops the sidebar) a modal bottom sheet with a scrolling body and its actions above the safe area. Later (text) and Got it (filled) both close it for good; Try it now goes to the feature; the changelog link shows only to people who may open the changelog. "Once" is once per account on every device, keyed on the release date or the last Announce again, so switching one off and on never repeats it and Announce again makes it new for everyone. `announced_at` is therefore empty until an Announce again: a switch never stamps it. One set for the whole group, not per business unit. Same rule as Sales Activity's Yang baru, without its unread dot, because LeadEngine's changelog is an admin page | `Announcement` on `ChangelogEntry` in `features/changelog/changelog-data.ts`; `announcementStates` / `pendingAnnouncements` in `lib/announcements/announcements.ts`, read by `listAnnouncements` / `pendingAnnouncementsFor` in `lib/announcements/announcement-queries.ts`; `public.release_announcements` (on/off, `announced_at`; readable by every signed-in person, written only by `setAnnouncementEnabled` / `reannounce` in `app/actions/announcement-actions.ts` with the service client after `settings.update`) and `public.user_hints` (seen marks `announce-<key>-<stamp>`, own rows only, `markHintsSeen` in `app/actions/hint-actions.ts`), migration `20260923110000_leadengine_announcements.sql`; `AnnouncementDialog` in `features/announcements/components/announcement-dialog.tsx`, rendered by `app/(app)/page.tsx`; `BottomSheet` in `components/ui/bottom-sheet.tsx`, `useCompact` in `hooks/use-compact.ts`; `AnnouncementList` on `app/(app)/settings/announcements/page.tsx` |
| Usage is one row per person per day, read by admins, never a tracker | An admin could see what changed in the CRM but not whether anyone opens it. Salesforce's Lightning Usage App, Notion's workspace analytics and Microsoft 365's active-users report answer that with the same small shape, and so does this: one row per person per WIB day (first and last seen, pages opened, the last page) and one counter per page per day. Daily rows, not an event stream: a day row answers "is this person using it", a page counter answers "which screens earn their place", and neither can be replayed into a trail of what someone did, which is the line between usage and surveillance. The browser calls the server at most once per 30 seconds and batches the pages opened in between; nothing is sent while the tab is hidden; after five minutes on one page a heartbeat marks the person as still here (last seen moves, no page is counted), but only if they tapped, typed or scrolled since the last call. Paths are normalised in the browser and again on the server (uuid, numeric and opaque segments, and the slug of a company unit or a goal, become `:id`; query string and fragment are dropped), so no record id lands in a usage table and every lead's page counts as one "Lead details"; names come from the drawer's and Settings' own words at render time, and an unknown path shows as itself. Per person across the whole workspace, not per company: sign-in has no per-unit gate, so there is no `company_id`. Reading is for admins (super_admin, admin) behind the Settings grant: row security returns rows only to `fn_user_is_admin()`, and anyone else with Settings access reads "Admins only" rather than a table of people who seem never to have come (the Settings row says "Admins only" too); a person writes only their own day row and nobody can delete one. The page reads as those products do: people active today, in 7 and in 30 days against the headcount (M365's active-users cards); "Daily active users", a bar per WIB day with every day drawn and a quiet day at 0, weekdays in the primary (the ink LeadEngine's charts give a measure; the `--chart-*` tokens here are unthemed) and weekends in the muted ink with a legend naming both, a one-line tooltip ("Tue 22 Sep · 5 people"), and the axis still drawn when nothing is recorded while "Nothing recorded yet" stays the only empty message; one line per person with Last active as a relative time and the exact WIB stamp on hover (Slack, Teams), days active in 30, pages opened in 7, and an eight-week line of days active per rolling week on a fixed 0–7 scale so rows compare; "Inactive 7 days" as a muted chip, never a status colour, and "Never" for someone never seen; everyone who can sign in is listed, seen or not, counted as Settings → Users counts them (active profiles). Then the most opened pages as a bar list with an "Other (N pages)" fold and "Show all". One period (7, 30 or 90 days, in the URL) governs the daily chart and the pages, and the same segmented control sits on each of those two cards, because the tiles and the people table count fixed windows. The page reflows to a phone: tiles in two columns, the people table as one card per person below `md`, bars never wider than their day's slot (ninety days at 360px), 44px touch targets, and no sideways page scroll, for which its root opts out of the shell's 900px content floor. Honest disclosure: the page's subtitle and the changelog say what is recorded (which page, when) and what is not (nothing typed, nothing shown on a page, no location). Sales Activity's Pengaturan → Pemakaian is the same page over its own tables | Migration `20260923120000_leadengine_usage.sql`: `public.usage_days`, `public.usage_pages` (admin-only SELECT via `fn_user_is_admin()`, no delete policy or grant), `record_usage` (definer, because an upsert's DO UPDATE is checked against the admin-only SELECT policy; writes `auth.uid()`'s row only; day window and clamp as Sales Activity's; heartbeat as `p_views = 0`), `usage_last_seen` (invoker). `lib/usage/usage-path.ts` (`normalizeUsagePath`, `isUsagePath`, `usagePageLabel`), `lib/usage/usage-beacon.ts` (`createUsageBeacon`, `collapseVisits`, `USAGE_MIN_INTERVAL_MS`, `USAGE_HEARTBEAT_MS`), `lib/usage/usage-schema.ts`, `lib/usage/usage-stats.ts` (`summarizeUsage`, `dailyActiveSeries`, `usageBarSize`, `rollupPeople`, `weeklyActiveDays`, `rollupPages`, `lastSeenLabel`, `isUsageAdmin`, `usageDayKey`), `lib/usage/usage-queries.ts` (every read empty rather than thrown before the migration), tests beside them; `recordUsage` in `app/actions/usage-actions.ts`; `UsageBeacon` in `components/layout/usage-beacon.tsx`, rendered by `MainLayout`, whose `<main>` takes `has-[[data-fluid-page]]:min-w-0`; `app/(app)/settings/usage/` (`page.tsx`, `usage-daily-chart.tsx`, `usage-people.tsx` with `UsageTrend`, `usage-pages.tsx`, `usage-period.tsx` on the house `SegmentedControl`); the row on `app/(app)/settings/page.tsx` |
| A phone gets the real width only where the page is built for it | LeadEngine is desk-first: `<main>` keeps a 900px floor so the tables and the Kanban board that are not built for a phone keep their columns and scroll sideways there rather than collapsing into something half-usable. A page that is laid out for a phone (a list of destinations, a settings form, a report that reflows, a list that turns into cards) opts out by carrying `data-fluid-page` on its root, and then gets the phone's own width with no sideways page scroll. Opting in is a promise that the page was checked at 360px. Contacts and Companies opted in with their phone layout: cards below `md`, the filter sheet, the top app bar's overflow; from `md` up their table scrolls sideways inside its own box, never the page. The dashboard opted in with its stream of cards (see The dashboard on a phone) and keeps the 900px floor itself from `lg`, where its twelve-column board still needs it, so a desk sees it exactly as before. The Pipeline opts in below `md` only, with `data-fluid-phone`: there it is stage tabs over cards (see The Pipeline on a phone), and from `md` its kanban board keeps the floor. A contact's and a company's pages opted in with the record layout (see Record pages): without the attribute the 900px floor, a 340px details pane, 32px gutters and a strip of number cards slid the whole page sideways on a phone. A wide page's sideways scroll happens between the phone shell's two bars, which stay on screen while it moves | `has-[[data-fluid-page]]:min-w-0` and `max-md:has-[[data-fluid-phone]]:min-w-0` on `<main>` in `components/layout/main-layout.tsx`, whose sideways scroller is the `overflow-x-auto` box around `<main>`, between `MobileTopBar` and `MobileNavBar`; `data-fluid-page` on `app/(app)/settings/page.tsx`, `settings/announcements/page.tsx`, `settings/usage/page.tsx`, `settings/history/page.tsx`, `app/(app)/contacts/contacts-list.tsx`, `app/(app)/companies/companies-list.tsx`, and on `ListPageSkeleton` (`components/shared/list-page-skeleton.tsx`), their `loading.tsx`, so the floor never flashes while a list loads; on the dashboard's root in `features/leads/components/analytics-dashboard.tsx` (with `lg:min-w-[900px]`, and the same on `DashboardGrid`'s root), its "Dashboard access restricted" state in `app/(app)/page.tsx` and `app/(app)/dashboard/loading.tsx`; `data-fluid-phone` on the root of `features/leads/components/lead-dashboard.tsx` and in `app/(app)/leads/loading.tsx`; `data-fluid-page` on the roots of `features/contacts/components/contact-detail-page.tsx` and `features/companies/components/company-detail-page.tsx` and on `RecordPageSkeleton` (`components/shared/record-page-skeleton.tsx`), both routes' `loading.tsx` |
| Who may see and change what | Role & Izin: modul × Lihat/Buat/Ubah/Hapus with a reach (Cakupan) under Lihat and Ubah; role chips on top, one pane, the reading guide folded. Granting a write implies Lihat (the database refuses a write without it, `role_permissions_write_requires_read`), and in the holding view that rule is applied per subsidiary, not only to the holding's row: a unit whose Lihat is off gets Lihat with the write, the rest get the switch alone, so one unit that differed from the holding can no longer make the change fail for every unit | `app/(app)/settings/permissions/page.tsx` (`propagateToSubsidiaries`, `cascadeSalesMissionSubmodules`), `lib/permissions/subsidiary-writes.ts` with its tests |
| Drawer rail vs account menu | The drawer holds the daily destinations (Dashboard, Pipeline, Companies, Contacts) and one Administration group for Settings; the profile, the changelog, the panel toggle and sign-out live behind the account menu at the foot of the drawer, on the panel's own tokens, with no focus ring left behind on close (M3 navigation drawer: 3 to 7 destinations; Slack, Notion, Linear). An audit trail is an admin's monitoring tool, not a destination, so History lives in Settings → Monitoring as Change history (Salesforce Setup Audit Trail, HubSpot, Linear, Notion keep it in settings; Sales Activity's Riwayat perubahan sits in Pengaturan), behind the Settings grant, and `/history` redirects there; what happened to one lead is on its Timeline tab. The drawer has two states and no free width: expanded at 220px or folded to the 60px rail by the panel toggle, never dragged (M3's navigation rail and expanded rail switch between fixed widths and give drag handles to content panes only; Gmail, Drive and Calendar fold, they do not resize). The same two widths as Sales Activity, so switching apps never jumps the drawer; a content pane such as the Pipeline's filter panel may still be dragged. The drawer exists from `lg` only: below it the phone shell takes its place and nothing opens it as a side sheet (see Phone shell); its destinations and the account menu's items are in the navigation bar and the More sheet there | `components/layout/sidebar.tsx` (`DESTINATIONS` and their grants from `permittedDestinations` / `canOpenSettings` in `lib/navigation/app-nav.ts`, shared with the navigation bar; `adminNav`; the `DropdownMenu` on the profile block; `useSignOut` in `components/layout/use-sign-out.ts`, shared with the More sheet); the fixed `lg:w-[220px]` / `lg:w-[60px]` aside in `components/layout/main-layout.tsx`; `app/(app)/settings/history/page.tsx`, the redirect in `app/(app)/history/page.tsx`, the Settings grant in `fetchAuditLogs` |
| Switching apps | A transit screen on leaving and a loader on arrival, both on the shared preference cookies, so the sidebar arrives folded or expanded as the person left it, at the same fixed width in both apps | `components/layout/{app-transit,app-switcher,top-loader}.tsx`, `lib/preference-cookie.ts` |
| Phone shell | Below `lg`, exactly where Sales Activity switches, LeadEngine has Sales Activity's phone shell, rule for rule, and the drawer is not drawn (no hamburger, no side sheet: a 430px dark panel over a 600px screen hid the page to show four links). **Top app bar**: Material's small top app bar, 56dp plus the status bar's inset, above the scrolling content so it never scrolls: the product mark at the leading edge (the collapsed drawer's "W"), or a back arrow when the page sits below another; the page's title (17px, semibold, one line); the page's overflow ⋮ at the trailing edge when it has secondary actions, a bottom sheet of 56dp rows below `md` and a menu from `md` (LeadEngine has no notifications, so no bell). **Navigation bar**: Material's navigation bar, the last row of the shell, 80dp plus the home indicator's inset: the drawer's daily destinations (Dashboard, Pipeline, Companies, Contacts), each only for whoever holds its module's read grant, as the drawer filters them, then More; icon over an always-shown label, the current one on a 64×32dp tonal pill with `aria-current="page"`, More current on a page it opens; placeholders while the grants load, as the drawer's. Because it is in the shell's column rather than over the page, `<main>` ends where it starts and no page needs padding for it. **FAB**: Material's extended FAB for a page's one primary create action, 56dp, 16dp from the edge and above the bar, `lg:hidden` (the desk keeps its filled header button): Add contact on Contacts, Add company on Companies, New lead on Pipeline, each only for whoever may create; its page ends 88dp above `<main>`'s foot so the last card or pager is never under it. A selection's action bar sits level with the FAB, clear of the bar. The shell is `h-dvh` and the page `viewport-fit: cover`, so the bars sit on the visible edges of a phone's browser and pad themselves by the safe-area insets (M3 top app bar, navigation bar, extended FAB, window size classes; Sales Activity's Phone shell) | `MainLayoutInner` in `components/layout/main-layout.tsx` (`h-dvh`; the aside `hidden lg:flex`; a column of `MobileTopBar`, the `overflow-x-auto` box holding `<main>`, and `MobileNavBar`); `MobileTopBar` in `components/layout/mobile-top-bar.tsx` (`min-h-14`, `pt-[env(safe-area-inset-top)]`, 48dp back and ⋮ targets, the title from `usePageChrome()` or `fallbackTitle(pathname)` before the page announces one); `ResponsiveMenu` in `components/layout/responsive-menu.tsx` (`useBelowMd` in `hooks/use-compact.ts`); `MobileNavBar` in `components/layout/mobile-nav-bar.tsx` (`<nav aria-label="Main navigation">`, `grid h-20`, `pb-[env(safe-area-inset-bottom)]`, `DESTINATION_ICONS` in `components/layout/destination-icons.ts`); `permittedDestinations`, `isActiveHref`, `isMoreActive`, `fallbackTitle` in `lib/navigation/app-nav.ts`, tested in `app-nav.test.ts`; `Fab` and `FAB_CLEARANCE` (`max-lg:pb-[5.5rem]`) in `components/layout/fab.tsx`, gated by `useCan` in `features/users/components/permission-gate.tsx`; `BulkActionBar` `max-lg:bottom-[calc(6rem+env(safe-area-inset-bottom))]`; `viewport` in `app/layout.tsx` |
| Bottom sheets: the header stays, the body scrolls beneath it | Every modal bottom sheet is one component with Sales Activity's anatomy: 28dp top corners and a drag handle, the title and an optional supporting line, the body, an optional footer of actions clear of the home indicator, never taller than 85% of the viewport, and only the body scrolls. The header (handle, title, supporting line) and the footer are surfaces of their own: opaque on the sheet's colour, never shrunk by a long body, and painted above it, so rows scroll beneath the header and are hidden there rather than showing through or running into the supporting line (seen on a phone, where a business unit's name sat under "The dashboard updates as you choose." in the long Filter sheet; the header had no surface or stacking order of its own and only 8px between the supporting line and the body's first row). Once the body has left its top, a hairline under the header marks the edge it passes under, as a top app bar separates itself once content scrolls under it; the footer's hairline is always drawn. A sheet never raises the keyboard by itself: when its first control is a text field (a search above a long list), opening it focuses the sheet, and the field waits for a tap. A sheet opened from a sheet (a filter's own sheet) stacks over it with its own scrim and closes back to it (M3 bottom sheets, modal; M3 top app bar, on scroll; M3 text fields; Sales Activity's "Dialog: headline and actions stay, content scrolls") | `BottomSheet` and `BottomSheetFrame` in `components/ui/bottom-sheet.tsx` (`data-slot="bottom-sheet-header"`, `relative z-10 shrink-0 rounded-t-[inherit] bg-card`, its `border-b` coloured while `data-scrolled`; `data-slot="bottom-sheet-body"`, `relative z-0 min-h-0 flex-1 overflow-y-auto`, which sets `scrolled` from its own `onScroll`; `data-slot="bottom-sheet-footer"`, `relative z-10 shrink-0 border-t bg-card`; `keepKeyboardDown` on `onOpenAutoFocus`), on `SheetContent` in `components/ui/sheet.tsx`; every sheet uses it: the Filter sheets (`PhoneFilterFrame`) and their field sheets on the Dashboard and the Pipeline, the More sheet, the page menu (`ResponsiveMenu`), Views and How the numbers are counted, the date sheet, Move to stage, Switch pipeline and the announcement. Sales Activity's own `BottomSheet` does not have the opaque header or the keyboard rule yet |
| The More sheet | Everything the drawer carries beyond the daily destinations is behind More, in one modal bottom sheet titled "More" over the person's name, in Sales Activity's Lainnya order for the equivalent items: the pages first (Settings and Changelog for whoever holds the Settings grant, as the drawer shows them; My profile for everyone), then what the drawer's header holds (the business unit in view, "Every business unit" or "Single unit" under it, which opens in place to the units to choose, the holding first, a check on the one in view, as the header's switcher does; Sales Activity, for whoever may open it, which leaves with the same transit screen as the desk's app switcher), then the account (avatar, name, role) with sign-out as its trailing 48dp icon button. The current page's row sits on the tonal fill. No panel toggle: it recolours the drawer, which a phone does not have (M3 navigation bar overflow into a modal bottom sheet, 56dp list rows; Sales Activity's Lainnya) | `MoreSheet` and `UnitChoice` (`role="radio"` in a `radiogroup` labelled "View data for") in `components/layout/mobile-nav-bar.tsx`; `BottomSheet` and `SheetRow` (`aria-current="page"` when `active`) in `components/ui/bottom-sheet.tsx`; `useCompany().switchCompany`; `useAppTransit` and `salesMissionUrl` from `components/layout/app-switcher.tsx`; `roleLabel` in `lib/navigation/app-nav.ts`, shared with the drawer |
| Pages announce their chrome | A page says what its top app bar shows (the title, where back goes, its overflow menu) through a small client marker that announces on mount and withdraws on unmount, so the shell never guesses and a page never writes its header twice; menu items are data (label, icon, `href` or `onSelect`, `danger` last), compared by label and link so a re-render does not re-announce, and each handler calls through to the page's latest render, so an Export run from the ⋮ reads the list's current filters (Sales Activity's `PageChrome`, the same mechanism). What each page announces: Contacts and Companies their title and Export / Import; Dashboard "Dashboard", and in the ⋮ Views, Print / Save PDF, AI Analyze, Ask AI and How the numbers are counted (no Edit: arranging the widgets is desk work, see The dashboard on a phone); Pipeline the active pipeline's name, and in the ⋮ Switch pipeline below `md` (where the pipelines' side panel is not drawn, for whoever has more than one pipeline or may create one), Import leads and Export to XLSX (whoever may create leads) then Rename pipeline, Manage stages, Clone pipeline and Delete pipeline (whoever may manage pipelines), what its header's two menus held, since that row is desk-only and its lead count moves into the toolbar (on a phone, the line above the cards); every page with `SettingsPageHeader` its title, and a page below Settings its back arrow to the nearest parent; a lead, a contact and a company "Lead", "Contact", "Company" with back to Pipeline, Contacts or Companies: the record's name stays in the page, where it is edited in place, and the lead's header back arrow is desk-only (one door each), where a contact's and a company's desk headers have the parent link ("← Contacts", "← Companies") instead; a contact's ⋮ holds what its desk header holds, Edit (whoever may change it), Email (when it has an address), Previous contact and Next contact (when there is one) and Delete last (whoever may delete), and a company's Edit, Previous company, Next company and Delete (see Record pages). A page that announces nothing is titled by the destination its address is in | `PageChromeProvider`, `usePageChrome`, `PageChrome`, `ChromeMenuItem` in `components/layout/page-chrome.tsx` (provider in `main-layout.tsx`); `ListPageHeader phoneMenu`; `SettingsPageHeader`; `analytics-dashboard.tsx` (`phoneMenu`), `lead-dashboard.tsx` (`phoneMenu`, `openNewLead`, the root `h-full`), `lead-detail-page.tsx`, `contact-detail-page.tsx`, `company-detail-page.tsx` |
| The dashboard on a phone | Below `lg` the dashboard reflows to the screen instead of being the desk's 900px board scrolled sideways (M3 canonical layouts: content reflows to one column in a compact window; the Google Analytics, HubSpot and Salesforce mobile apps show a dashboard as a stream of cards). **Cards**: the view's cards in the order the person arranged them on the desk (top to bottom, then left to right), the hidden ones left out. Number cards two to a row (three from `md`), as tall as their content, a row as tall as its tallest card; the label wraps between words only ("Incoming / Lead", never "Incomi / ng"), the value stays large, the delta chip and the supporting lines stay, a supporting label wrapping rather than being cut. Every list or chart one to a row at full width: a list (the funnel, Sales Performance, Top Revenue, Lead Source, the two donuts) as tall as its rows up to the height the person gave it on the desk, kept between 300 and 460px, and past that it scrolls inside its card, never the page; a chart at that height, because a chart fills the box it is given. Cards take 16px sides, the page a 16px gutter (24px from `sm`). **Charts**: the monthly revenue chart labels every other month when twelve do not fit (a label that would come within 12px of its neighbour is dropped), keeps its y-axis compact through `fmtAxis` and on one line ("Rp 2.2B", never "Rp" over "2.2B"; the same on a desk, where the chart had its own formatter without the currency), and drops the value printed on each bar for the tooltip a tap opens, which stays inside the chart; the funnel's stage names take 104px instead of 140px and wrap; legends wrap; the pickers in a card's header wrap under its title. **Filters**: below `md` the lists' pattern, rule for rule (see List pages; Sales Activity's `FilterBarFrame`; M3 filter chips in a modal bottom sheet), and no desk selects. Under the factual line, one outlined 44px "Filter" button carrying how many filters differ from how the dashboard opens (the default pipeline, the page's own pick: the one marked default, else the first; every business unit; This Quarter) opens a bottom sheet titled "Filter" ("The dashboard updates as you choose.") built as the Pipeline's Filter sheet is, in two levels (Sales Activity opens a facet's own list the same way): one 56dp row per filter (`FilterFieldRow`, the Pipeline's field row: the filter's icon, its name, what it is set to under it, a check in the primary colour while it differs from how the dashboard opens, a chevron), so the sheet is three rows however many pipelines and units the group has, and no list of units stands between the reader and the dates (it did when every choice sat inline in one long sheet). The rows: Pipeline ("Group Lead 2026 · Default"; drawn with two pipelines or more), Business unit ("All business units" or the unit; drawn in a holding view with more than one unit; named as the More sheet names the units, with its glyph, the globe for every unit and a building for one, because Companies in the navigation bar are clients; the desk's select still says "Company"), Date range ("This Quarter", "1 May – 31 May 2026"). A row opens that filter's own sheet over the first: Pipeline, a radio list of 56dp rows (`SheetChoice`, the Switch pipeline sheet's rows: the chosen one tonal with a check, "Default" under the default one) under a line saying a pipeline named for a year also sets the date range to that year; Business unit, "All business units" then each unit on the same rows, with a search above more than eight units, as the Pipeline's value lists have, which opening the sheet never focuses (see Bottom sheets); Date range, the date sheet's own panel: the quick ranges as choice chips, then one 56dp row, "Custom range…", which opens one month of 40px days under it only when tapped (the row says the custom range in effect, with a check, else "Tap a start day, then an end day", and once a start day is tapped, that the end day is next), the sheet's body scrolling itself, never the page, until the month is in view; its supporting line says the range is set aside while a month is picked on the revenue chart. A pipeline, a unit or a quick range applies at once and closes its sheet, back to the three rows; a custom range applies on its end day and closes the same way; choosing a pipeline realigns the period as it does on a desk. The Filter sheet's footer carries "Clear all" as a text button while anything is applied (the chip row's own action), then "Done". Every sheet's header is opaque and the body scrolls beneath it (see Bottom sheets). What is applied repeats under the button as 32px input chips with an ✕ ("Pipeline: Group Lead 2025", "Business unit: …", "Date range: This Year", then any month or bar picked on a chart, which a desk shows as "Exploring"; these are not counted, being no filter the sheet sets), in one row that scrolls sideways, bleeds to the screen's edge and fades at each edge it can still scroll toward, then "Clear all", which returns every filter to how the dashboard opens and drops the chart picks; the pipeline chip's ✕ goes back to the default pipeline the way choosing it does. Between `md` and `lg` pipeline, company and date range stay one row of 32px chips under the header that scrolls sideways with its scrollbar hidden and fades at each edge it can still scroll toward, and the date range opens its popover. The ⓘ, a hover tooltip a finger cannot reach, is not drawn below `lg`. **Header**: the top app bar says "Dashboard"; the desk's first row (the greeting, Tools, the view switcher, Edit Dashboard) is not drawn; the factual line ("Performance Dashboard · pipeline · Updated …") stays as one 12px line cut with an ellipsis and gives way once the cards scroll, as the desk's does. **The ⋮** (M3 top app bar overflow; Sales Activity's Ringkasan): Views, a bottom sheet with the saved views as a radio list (the current one tonal with a check, "Default" under the default one, "Default view" when none is saved), "Save changes to this view" while the filters differ from it, and one sentence saying that arranging the widgets and renaming, duplicating or deleting views is done on a computer; Print / Save PDF; AI Analyze; Ask AI; How the numbers are counted, a sheet with each number card's explanation, the text the desk's ⓘ tooltips hold. No Edit layout: arranging a twelve-column grid is desk work (Sales Activity: "a phone stacks the cards; arranging is a desk job"), and an edit begun on a desk is kept, not lost, if the window narrows. The AI panels sit above the navigation bar and below the top app bar, span the screen less 8px each side below `md`, and give their close and minimise buttons 44px targets. From `lg` the desk board is unchanged | `DashboardGrid` in `features/leads/components/dashboard-grid.tsx` (`useCompact`; the stream `[data-dashboard-flow]`, `grid-cols-2 md:grid-cols-3`, a list's cell `maxHeight`, a chart's `height`; `lg:min-w-[900px]` on its root; no edit controls below `lg`); `flowItems`, `flowKind`, `flowHeight`, `gridRowsToPx`, `CONTENT_SIZED_WIDGETS` in `features/leads/lib/dashboard-flow.ts`, tested in `dashboard-flow.test.ts`, with `GRID_MARGIN` in `dashboard-layout.ts`; `DashboardFlowProvider` / `useDashboardFlow` in `features/leads/components/dashboard-widgets/dashboard-flow-context.tsx`, read by `SingleKPIWidget` (full tier, `break-normal hyphens-none`, no ⓘ), `RevenueChartWidget` (`interval="preserveStart"`, `minTickGap={12}`, no `LabelList`; `oneLine(fmtAxis(…))` at every size), `PipelineWidget` (`nameWidth`), `SalesPerfWidget` and `TopRevenueWidget` (header `flex-wrap`); `SectionCard` `max-md:px-4` in `dashboard-widgets/shared.tsx`; `AnalyticsDashboard` in `features/leads/components/analytics-dashboard.tsx` (`data-fluid-page`, `phoneMenu`, the first row `max-lg:hidden`, the phone's factual line `lg:hidden`, `handleClearAllFilters`, the desk filter row's `max-md:hidden` and `max-lg:` classes with `useEdgeFade`, `#dashboard-content` `px-4 sm:px-6 lg:px-8`); `DashboardViewsSheet` and `DashboardCountingSheet` in `features/leads/components/dashboard-phone-sheets.tsx` on `BottomSheet`; `DashboardPhoneFilters` and `UnitChoices` in `features/leads/components/dashboard-phone-filters.tsx` on `PhoneFilterFrame` (`clearAllInSheet`) and `FilterFieldRow` (`components/shared/phone-filter-frame.tsx`), each filter's sheet a `BottomSheet` over the Filter sheet with `SheetChoice` rows; `dashboardFilterRows`, `unitsMatching`, `dashboardFilterDiff`, `dashboardFilterCount`, `defaultPipelineId` in `features/leads/lib/dashboard-filters.ts`, tested in `dashboard-filters.test.ts`; `DateRangeFilter` (`useBelowMd`) and `DateRangeChoices` (the "Custom range…" `SheetRow`, the month brought into view by the body's own `scrollBy`) in `components/shared/date-range-filter.tsx`, the quick ranges and their labels in `lib/date-range-presets.ts` (`DATE_RANGE_PRESETS`, `dateRangeLabel`, `activeDateRangePreset`, `isCustomDateRange`, `DEFAULT_DASHBOARD_PERIOD`), tested in `lib/__tests__/date-range-presets.test.ts`; `FloatingPanel` in `dashboard-widgets/floating-panel.tsx` |
| The Pipeline on a phone | Below `md` the Pipeline is stage tabs over a list of lead cards, not the kanban board scrolled sideways: M3 has no phone kanban (it gives a compact window lists, and primary tabs for sibling views of one destination), and Pipedrive's mobile app shows a pipeline the same way, a scrolling row of stages with the chosen stage's deals under it. From `md` the board is unchanged. **Tabs**: M3 primary tabs, scrollable, 48dp, flush under the top app bar and pinned there while the list scrolls (Sales Activity's Laporan tabs), opaque on the surface with a hairline under them; the row fades at each edge it can still scroll toward (see Chips), so a stage cut by the edge reads as more to come, while the surface and the hairline belong to the pinned bar around it, which the mask does not reach; each tab is the stage's name over its lead count and total estimated value in compact currency ("5 · Rp 2.3B", the value left out at zero), the numbers following the search and filters as the board's column headers do; the chosen one in the primary ink with the 3dp indicator under it. The chosen tab stands in the middle of the row by the row's own scroll, never the page's (no `scrollIntoView`), and so clear of both fades with some of its neighbours in view; at either end of the row the fade on that side is off, and a tab wider than the row less both fades keeps its start clear; arrow keys, Home and End move between tabs. The stage in view is `?stage=` in the address, replaced rather than pushed, so Back from a lead and a reload return to it; with none chosen the page opens on the first open stage with leads (else the first stage with leads, else the first open stage) and holds it while leads move or a search narrows; a stage of another pipeline is dropped, and switching pipelines clears it. Choosing a stage while reading far down a list brings the new list's start back under the tabs. **Under the tabs**: the lists' phone filter pattern (`PhoneFilterFrame`): the search (44px) and one outlined Filter button carrying the count of what narrows the leads (the search included, as on the lists), the applied filters as 32px input chips with an ✕ in a row that scrolls sideways and fades at its edges, then Clear all (the search and every filter), then one line saying what the list counts, "12 in Proposal Sent · 245 in all stages" (both after the search and filters), with the sort menu at its end; then the cards, 12px apart, the page ending clear of the FAB. **Filter**: a bottom sheet ("Filter", "Tap a field to narrow the leads; the tabs and cards update as you go.", "Done"), never the desk's popover of rule rows, listing every field the desk's Filter can use as a 56dp row (`FilterFieldRow`, which the Dashboard's Filter sheet shares: the field's icon, its name, what it is set to under it, a check once applied, a chevron) under M3 subheaders: People (PIC Sales, Account Manager), Details (Category, Grade, Stage, Main Stream, Stream Type, Event Format, Subsidiary, Lead Source, Business Purpose), Amounts (Estimated Value, Confirmed Value, Pax Count), Dates (Target Close Date, Event Start Date, Event End Date, Event Date (any day), Revenue Recognition Month, Received Month) and Text (Client, Project, Referral Source). A row opens that field's own sheet over the first (Sales Activity opens a facet's checklist the same way): how it compares as choice chips (Is any of / Is none of; = > ≥ < ≤; After / Before / On; Contains / Does not contain / Is), then the values the pipeline's leads hold as a checklist of 56dp rows with an 18dp checkbox (a search above more than eight values), or one 44px field for an amount, a day or text. Every change applies at once; Clear drops the field, Done goes back to the list of fields. The rules are the desk's own (`PipelineFilterState`, `applyFilters`, untouched), so a filter set on either shows on the other: a field's sheet edits the field's first rule, and a rule whose values are all taken off goes rather than staying as an empty row. A chip reads as the lists' do: "Grade: A, B", "Category: not Cold", "Estimated Value: ≥ 300,000,000", "Target Close Date: after 1 Sep 2026", "Client: “bank”", "3 selected" past two values. **Cards**: the Contacts and Companies anatomy (see Cards on a phone): the project name as the headline and the link, the client company under it ("No client company"), the chips the person chose in Card Settings on a desk (business unit, grade, temperature, source, stream, format) as 24px tonal chips with 8dp corners that wrap onto a second line and cut a long label with an ellipsis, their tones tokens (grade A the success tone, B and C the warning tone, D the danger tone; Hot the danger tone, Warm the warning tone, Cold the primary tint; the rest neutral); the fact line, the estimated value then the date the board shows for the stage's state: "Closing 12 Sep 2026", in the warning ink within three days and "Overdue 12 Sep 2026" in the danger ink once passed (the word says it, not only the colour), "Won …" in the success ink, "Closed …" or "Updated …" muted; a hairline; the PIC sales at the leading edge ("No PIC sales") and the ⋮ at the trailing edge, 44px. The card opens the lead; the ⋮ never does. **The ⋮** holds what the board's card menus hold, less what needs a pointer: Move to stage… (whoever may update leads, in a pipeline of two stages or more), Edit (the same side sheet), Delete (to the Recycle Bin, confirmed); no selection. **Moving**: no drag on a phone. Move to stage… opens a bottom sheet of the pipeline's stages as radio rows, the lead's own checked, a closed stage saying "Closed as won" or "Closed as lost", and a choice moves the lead through the board's own path, the one its drop takes: a transition rule's prompt (fields, a note, an attachment) or the warning before a move backward first, then the same server action, the "Moved to …" message, and the card put back with an error if the server refuses. **Switching pipelines**: the side panel is not drawn below `md`; the top app bar's ⋮ has Switch pipeline, a sheet of the pipelines as radio rows (the current one checked) with New pipeline for whoever may manage them (M3 navigation: every destination keeps a door on a phone) | `PipelinePhoneView` in `features/leads/components/pipeline-phone-view.tsx` (the pinned bar `sticky top-0 z-20 border-b bg-background` around the tab row `edge-fade no-scrollbar relative flex overflow-x-auto` with `useEdgeFade`, `role="tablist"`, tabs `h-12 px-4`, the indicator `after:h-[3px] after:bg-primary`; the count row; `PipelineLeadCard`, `LeadCardMenu`, the Move sheet on `BottomSheet`; `?stage=` written with `history.replaceState`), rendered by `LeadDashboard` in `features/leads/components/lead-dashboard.tsx` when `useBelowMd()`, while the chrome switches in CSS so the first paint is right (the side panel, the header rows and the desk's filter pills `max-md:hidden`; the root `max-md:block max-md:h-auto max-md:overflow-visible`, so `<main>` scrolls and the tabs pin in it, with `data-fluid-phone`; `stagesPipelineId`, so the tabs never pair one pipeline's stages with another's leads; `KanbanSortMenu`, shared with the desk toolbar; the Switch pipeline sheet); `SheetChoice` in `features/leads/components/sheet-choice.tsx`; `useStageMove` in `features/leads/hooks/use-stage-move.tsx`, which the board's drop and its card's Move menu use too, with `topOfStageSortOrder` in `features/leads/lib/stage-transitions.ts`; `useKanbanCardConfig` in `features/leads/hooks/use-kanban-card-config.ts` (Card Settings, shared with the board); `leadInStage`, `summarizeStages`, `defaultStageId`, `resolveStageId`, `readStageParam`, `withStageParam`, `scrollLeftToCenter` (given `fade: EDGE_FADE_PX`), `stageOutcome`, `leadDateFact` (the board's card reads its dates through it too), `formatDay`, `leadChips`, `stageCountLabel` in `features/leads/lib/pipeline-phone.ts`, tested in `features/leads/lib/__tests__/pipeline-phone.test.ts`; `PipelinePhoneFilters` and its `FacetSheet` in `features/leads/components/pipeline-phone-filters.tsx` on `PhoneFilterFrame`, `FilterSheetSection`, `FilterFieldRow`, `SheetCheck` and `SheetChoiceChip`; `facetGroup`, `setFieldRule`, `clearField`, `removeRule`, `ruleForField`, `appliedRules`, `ruleValueLabel` in `features/leads/lib/pipeline-filter-facets.ts`, tested in `features/leads/lib/__tests__/pipeline-filter-facets.test.ts`; `FILTER_FIELDS` and `OPERATORS` exported from `pipeline-filters.tsx`, whose popover stays the desk's; `app/(app)/leads/loading.tsx` draws the tabs and cards below `md`. Still open: the desk kanban card's chips and dates keep their palette colours; a second rule on one field, which only the desk's popover can add, shows as its own chip but is not edited from the field's sheet; archived pipelines and archiving stay desk work; a lead with no stage id (from before stage ids) cannot be moved from its card, as on the board |
| Record pages (Contact, Company): the approved design | A record's page is the design approved in Figma (file `5njD1dMLifRA1GNl7rQCpp`, frames "Contact detail — Desktop 1440" `2:2` and "— Phone 390" `3:2`), built as HubSpot's, Zoho CRM's, Pipedrive's and Salesforce's record pages are (a header of who and how to reach them, a composer over what is due and what happened, the properties and associated records beside them) with Material 3's rules, one scroll in the shell's `<main>`. The Contact and the Company are done; the Lead page is still open. **Type and spacing**: Plus Jakarta Sans at the frames' sizes and the font's own line height ("normal", about 1.26, where Tailwind's sizes carry 14/20, 12/16, 24/32), so every row measures as the frame does; the name is M3 headline small (24px semibold), a fact's label M3 label medium (12px medium, muted), a card's title 15px semibold, values 14px, a row's second line 13px; a 4dp grid: 4 or 8 inside a group, 12 to 24 between groups (16dp gutters on a phone, 32 on a desk, 24 between the columns, 20 between cards). **One content box**: the header, the tabs and every tab's body share the page's margins (32dp on a desk, 16 on a phone), so the header's ⋮ ends where the cards below end and its back arrow starts where they start; the edge icon buttons align their icon, not their transparent container, with the margin (M3's top app bar: the icon on the keyline, the container reaching into the margin), since a ghost ⋮ centred in its 36dp box read as a row that stopped short of the cards. Checked at 1280, 1440 and 1920: the ⋮'s icon and the right-hand card end on the same pixel. Surfaces are tokens: the page `--background`, the header, the tabs and the cards `--card`, hairlines `--border`, cards 12dp corners. **Header** (from `lg`, on the card's surface, 16dp top, 32dp sides): one row, centred on the avatar: back to the list as a leading 40dp icon button (←, 20dp, on-surface; tooltip and name "Back to Contacts" or "Back to Companies"; M3's top app bar puts navigation there, never as a breadcrumb stacked above the name, which the parent link "← Contacts" was), 8dp, the identity: a 48dp initials avatar (round for a person, a 12dp-cornered tile for an organisation), 16dp, then the name and, 4dp under it, the supporting line (a contact's job title in the muted ink · its company as a link in the primary; a company's sector · line industry · city); no status chip, since neither record has a status field. At the trailing edge, 8dp apart: ‹ › to the record before and after by name (36dp round icon buttons named by their tooltips), then 36dp buttons with 8dp corners in 14px semibold: Call and Email outlined (tel: and mailto:, disabled with a tooltip saying why when the number or the address is missing; a company has no email field, so no Email), Edit outlined (the record's Edit sheet), New lead filled (whoever may create leads) and ⋮ (Delete, to the Recycle Bin after a confirmation; greyed with a tooltip for whoever may not). 24dp below, the facts under the name (102dp in: the back button's 30 past the margin, 8, the avatar's 48, 16), 48dp apart, 20dp above the tabs: Owner (the person, a 20dp avatar 8dp before the name), Phone (tel:), Email or, for a company, Website (the primary ink), Last activity, and Business unit only for someone who sees more than one unit. **No icon beside a labelled fact**: the label names it (the frames; HubSpot's highlights). **Last activity** is the newest note, call, meeting or email by when it happened (the time the person logged it for, not when they typed it), as "Call · 2 days ago", or "No activity yet" in the muted ink: a follow-up is a plan, not contact, and a field changed or a file uploaded is not contact with anyone (HubSpot's last activity date counts the same kinds). **Tabs**: M3 primary tabs, Activity · Leads n · Files n (a company also Contacts n, second), Activity the default, on the card's surface with a hairline under them, pinned to the top of the scroll (under the phone's top app bar, which is outside it); the active one semibold in the primary over a 3dp indicator, the others medium and muted. On a desk they sit at the leading edge, 43dp, the indicator as wide as the tab; on a phone they share the width, 48dp, the indicator 40dp under the label, and when they do not fit they scroll sideways and fade at their edges (see Chips), the chosen tab brought into view by the row's own scroller. The tab is `?tab=` in the address, replaced rather than pushed (the old `?tab=overview`, `?tab=timeline` and a company's `?tab=notes` open Activity); a tab chosen far down the page starts at its top. **One Activity tab** where Overview and Activity were two: they repeated each other (Recent activity was the first five of Activity's feed, and the composer on one tab logged into a history read on the other); HubSpot, Pipedrive and Salesforce put the composer, what is due and what happened in one column beside the record. **Activity, desk**: the main column (the composer, Upcoming, History) beside a side column of 380px (320px between `lg` and `xl`, where 380 left the composer 336px): About this contact or company, then the related records: a contact's Company (a 40dp tile with 10dp corners, the name, "line industry · city", "Open ↗") and Leads; a company's Contacts (avatar, name, job title, "+ Add" with the company filled in), Leads, and its group when it has one ("Parent company", "Subsidiaries" or both, each a row to that company). **One door for logging**: one composer, at the top of Activity (on a phone behind "Add a note…" and the header's Note, in a bottom sheet with the same kinds and fields); no "Log activity" button anywhere, since the composer is always on the tab that holds the history. **The composer** (HubSpot's, Pipedrive's and Salesforce's logging with M3's rules): one card; type pills Note · Call · Meeting · Email · Follow-up (13px semibold, 8dp corners, the chosen on `--tonal`; one radio group, arrow keys move and choose; one row that scrolls sideways and fades when narrow), and under them the fields that kind asks for, as M3 filled fields (on `--field`, the outline role `--input` at their edge; see Form fields have a visible edge) with sentence-case labels over them at 13px medium and a red * on what is required: **Note**, the text alone (at least 76dp, growing as it is written), to the notes table as before; **Call**, Outcome as M3 filter chips, one of Connected · No answer · Busy · Call back requested (required), Date and time (a date picker and a time field, now by default and never in the future) and Notes; **Meeting**, a line under the pills ("Planned visits are scheduled in Sales Activity; this logs a meeting that happened.", since a visit is planned there and only its report lands here), Where as chips In person · Online (required), then Location for in person or Meeting link for online (optional; typed without https it opens with it; not a link, it says so), Date and time, Outcome or summary; **Email**, Subject (required), Date and time, Summary; **Follow-up** (what Task was, and never the legacy `lead_tasks`), Title (required), Due date (required, tomorrow by default), Assigned to (the person writing by default; a searchable picker of the active people who can see the record's business unit, its members and a holding company's, the writer always among them) and Notes. A "when" nobody chose follows the clock, so a composer left open for an hour still logs now; an error shows under a field only once it holds something wrong (a time in the future, a link that is not one), never for a field still empty. Under the fields, "Ctrl + Enter to save" (hidden to a finger) beside the filled button that says what it will do, Save note, Log call, Log meeting, Log email or Add follow-up, greyed (M3 disabled, see Buttons) until the required fields are valid; Ctrl or ⌘ + Enter saves from any field. After a save the fields clear and the kind stays. The kinds are written `call`, `meeting`, `email` and `follow_up` with typed columns (`occurred_at`, `outcome`, `meeting_mode`, `location`, `meeting_url`, `subject`, `due_at`, `assignee_id`, `completed_at`, `completed_by`), and the rows written before them (`Call`, `Email`, `Meeting`, `Task`, `Note`, a Sales Activity visit's `meeting`) read as the same kinds. **Upcoming**: the open follow-ups, the one due first on top, in a card titled Upcoming that is not drawn when there are none: each an M3 checkbox (18dp, a 48dp target) that marks it done, its title (14px semibold), then when it is due ("Due today", "Due tomorrow", "Due 3 Oct"; once passed "Overdue · 20 Sep" in `--danger-foreground`, the word saying it for whoever does not see the colour) · who it is for (a 20dp avatar and the name, "Hanung P." on a phone) and its notes (13px, two lines). A tick shows at once (the row leaves Upcoming for History before the page's data catches up) and its toast offers Undo. Its author, the person it is assigned to and an admin may tick it; to anyone else the box is greyed. **History**: everything that happened, newest first by when it happened, in a card titled History: M3 filter chips choosing one kind, All · Notes · Calls · Meetings · Emails · Follow-ups · Files · Changes, only those with something in them offered (one row that scrolls sideways and fades on a phone, wrapping from `lg`), then the rows: a line icon (16dp, in a 32dp neutral circle on a desk, bare on a phone), the title in 14px semibold saying what it was ("Call · Connected", "Meeting · In person · The Manhattan Square", "Meeting · Online", "Email · Proposal v2", "Follow-up done · Send the proposal"; a row from before says its kind, "Call") and, after it in the muted ink, who ("Hanung P." on a phone), when at the trailing edge ("2 days ago" for a week, then the day; "2d" on a phone; the whole date and time in its title), and the text whole under it (an online meeting's link under it in the primary, opening in a new tab). A follow-up joins History when it is done, at the time it was done, under the name of whoever ticked it, its checkbox ticked, and unticking reopens it. The notes are taken whole from the notes table in place of the database's 100-character copies. A row a person logged (a note, a call, a meeting, an email, a follow-up) has a ⋮ (on hover or focus with a pointer, always to a finger; every row keeps its place so the times line up) with Edit, which opens the composer's own fields for that kind in the row, Save changes and Cancel beside them, and Delete, which asks first ("Delete this call?"); both for its author or an admin, what row security allows. The rows the database and the app write for themselves (a field changed, a note's copy, a file, a Sales Activity visit) have none: they are history. An old task without a due date may be deleted but not edited, since it has no follow-up's fields. **Row security** (migration `20260925120000_record_activity_fields.sql`): a contact's or a company's activity and notes are visible, and may be added, exactly when the contact or client company is visible to the person (the policy reads the parent under its own row security, so the unit in `fn_user_company_ids()`, holding access, a record with no unit and a trashed one for an admin all carry over); they were open to every signed-in person. Changed or deleted by the author or an admin (`fn_user_is_admin()`), only the kinds a person logs; the person a follow-up is for may tick it done or reopen it and nothing else (a trigger, which also sets who ticked it). **About this contact / company**: every field as label : value, the label 13px muted in a 112px column beside the 13px value on a desk and 12px over a 14px value on a phone; a value is itself the button that edits it in place (its whole row the target, a pencil on hover or focus and always to a finger), and a value that is a link (email, phone, website) opens and keeps its own pencil beside it; "Edit" opens the record's Edit sheet; the empty fields a person fills in fold under "Show N empty fields". A contact's Full name shows the salutation before the name (the header shows the name alone, as the frames do); the frames' rows come first (Full name, Job title, Email, Phone, Address, Contact source, Communication style) and then Company and Owner, which are edited here and nowhere else on the page. The DISC reading keeps its badge, meaning, note and signature (see A field reading is a badge, signed). **Leads card**: "2 open · Rp 1.2B · 1 won" in compact currency, then the open leads (five at most), each its name over its stage chip (tonal, 12px semibold, 6dp corners) with its value at the trailing edge; "+ New" and "View all". A stage chip's tone is where the lead stands (open on `--tonal`, won `--success`, lost `--danger`, closed muted), never the pipeline's stage colour, which is a hex. **New lead** opens the Pipeline's own Add lead sheet and form with the client company filled in and, on a contact with a company, the contact person too (its picker lists a company's people), in the pipeline the person last had open on the Pipeline page, else the first active one; saving opens the new lead. **Single-pane tabs take the whole width**: Leads, Contacts and Files have no side column, so their content runs between the same margins as the header, never in a reading column that stops at the main column's width and leaves the side column's space empty; only Activity splits the width. **Leads tab**: one card, New lead at its edge, the summary line, the leads ten at a time as a Data table from `md` (sentence-case headers, 52dp rows opening the lead) and one row per lead on a phone, the search past ten. **Contacts tab** (a company): the same card and table, Add contact at its edge. **Files tab**: one card, Upload at its edge, the list that is also the drop zone. **Phone** (below `lg`): the top app bar says "Contact" or "Company" with back to the list, and its ⋮ holds Edit, Email (a contact with an address), Previous and Next, and Delete; the header centred on the card's surface (20dp top, 16dp sides and bottom, 14dp between its parts): a 64dp avatar, the name at 22px, the job title (14px muted) and the company (14px medium, primary, a link), or a company's sector line; then the quick actions, M3 labelled icon buttons 12dp apart, a 64×40dp tonal pill over a 12px label: Call, WhatsApp, Email, Note for a contact, Call, Website, Note for a company, one without a number, an address or a site disabled (M3's treatment: the pill on-surface at 12%, icon and label at 38%; see Buttons: disabled is on-surface) and saying why; Note opens the composer as a bottom sheet ("Log activity", the same kinds and fields, its button row held at the sheet's foot while the fields scroll). Activity, 12dp top and between cards, 16dp sides: Key facts (Owner with avatar, Phone, Email or Website, Last activity, each a 12px label over its 14px value; the desk's facts row, which the phone's header has no room for, so it stays first), "Add a note…" (a 48dp row that opens the same sheet), Upcoming, History, then the side cards stacked below: the contact's company or the company's group as a row with a chevron, then About (Leads and Contacts are their own tabs on a phone). Every target is at least 48dp; a text action, a choice or a pencil smaller than that reaches 48dp through a hit area around it that moves nothing. No sideways page scroll at 360px: both pages and their loading skeletons carry `data-fluid-page` (see A phone gets the real width). **Never** `scrollIntoView` or a bare `.focus()`: a tab chosen far down sets `scrollTop` on `<main>`, the tab row and a chip row scroll themselves, a row opened for editing gives its first field the focus with `preventScroll`, where it already is, and its menu does not take the focus back. (Figma file `5njD1dMLifRA1GNl7rQCpp`; HubSpot's and Zoho CRM's record pages; HubSpot's activity composer (log a call with its outcome, a meeting, an email, create a task) and its upcoming and history, Pipedrive's planned and done activities, Salesforce's activity timeline with Upcoming & Overdue above the past; Google Contacts and Zoho CRM mobile for the quick actions; M3 type scale (headline small, title, label medium), spacing on the 4dp grid, top app bar, primary and scrollable tabs, cards, lists, labelled icon buttons, filter chips, text fields, checkboxes, menus, dialogs, bottom sheets, one scroll region) | The shared parts in `components/shared/record-page.tsx`: `RECORD_TYPE` (the frames' line heights: `leading-[normal]` and the `--text-*--line-height` variables set to normal on the page's root and the composer sheet), `RECORD_GUTTER` / `RECORD_GUTTER_DESK` (the one content box: `px-4 lg:px-8`, the header and the tabs taking the desk half), `RecordHeader` (the back button a `Tooltip`-wrapped `Link` in a ghost 40dp `Button`, `-ml-2.5`; the facts `pl-[102px]`), `RecordFact`, `PersonLine`, `FactLink`, `FactNone`, `RecordStepper`, `HeaderLinkButton`, `OUTLINED_BUTTON`, `FILLED_BUTTON`, `ICON_BUTTON`, `MORE_BUTTON` (the header's ⋮, `-mr-2.5`), `RecordHero`, `QuickAction`, `RecordTabs` (`sticky top-0 z-20`, `data-record-tabs`, `edge-fade` on the row, `reveal`), `RecordPanel` (a tab's body in `RECORD_GUTTER`, no max width; Activity's two columns through its `className`), `RecordCard`, `CardAction`, `KeyFactsCard`/`KeyFact`, `AddNoteRow`, `ACTIVITY_ICON`, `ActivityIcon`, `ActivityWhen`, `AboutCard`, `RelatedCompanyCard`, `RelatedListCard`, `RecordLeadsCard`, `StageChip`, `STANDING_TONE`; the composer in `components/shared/record-composer.tsx`: `ActivityComposer` (`variant` card, sheet or inline; `initial`, `submitLabel`, `onCancel` for editing a row; `useNow` moves the clock every 30s; the keys a portalled picker sends are its own), `ComposerSheet`, `ChoiceChips` (the outcome, the where and History's chips; `scroll` for the edge-faded row), `KindPills`, `ComposerEnv` (the note's subject, the viewer, the assignees and their loader); the pickers are `DatePickerField` (`maxDate` today for a "when") and `SearchableSelect`, which take an `id` and `aria-labelledby` so the label above names them, on `bg-field`; `RecordActivityFeed` in `record-activity-feed.tsx` (Upcoming and History from the rows, the notes and `people`; the tick held in `override` against the rows it was made on until the refreshed rows arrive; `RowMenu`, which keeps the focus in the editor; `FollowUpCheck`; `UpcomingRow`, `HistoryRow`; one Delete dialog); `RecordLeadsTable` in `record-leads-table.tsx`; `RecordFiles` in `record-files.tsx` (both records' stores in `FILE_STORES`); `RecordPageSkeleton` in `record-page-skeleton.tsx`, the `loading.tsx` of both routes; `FieldShell`, `FieldRow`, `EditTrigger` and the `Inline*Field` editors in `components/shared/inline-edit-field.tsx` (one layout, the About card's; `href` for a value that is a link; `save` for a page's own action); `InitialsAvatar` sizes `header` (48) and `hero` (64), square corners by size; `useRecordActivity` (`log`, checked against `composerSchema` again before it writes; `editNote`, `editActivity`, `deleteNote`, `deleteActivity`, `setFollowUpDone`; a write row security refuses returns no row and is said as a failure), `useRecordAssignees`, `contactActivityTarget`, `companyActivityTarget` in `hooks/use-record-activity.ts`; `listRecordAssigneesAction` in `app/actions/record-activity-actions.ts` (the record read as the caller first) on `assignableUsersForUnit` in `lib/assignable-users.ts` (`includeHolding`; shared with a lead's tasks, `listAssignableUsersForLeadAction`); `readRecordViewer` and `readRecordPeople` in `lib/record-activity-server.ts`, read by both routes; the typed half in `lib/record-activity.ts` (`activityKind`, `isLoggedActivity`, the same test as the database's `fn_record_activity_is_logged`, `isOpenFollowUp`, `isFollowUp`, `activityPeopleIds`, `isAdminRole`, `canManageActivity`, `canCompleteFollowUp`, `canManageNote`, `COMPOSER_KINDS`, `CALL_OUTCOMES`, `MEETING_MODES`, `notePlaceholder`, `localDay`, `localTime`, `shiftDay`, `combineLocal`, `dueAtFromDay` (noon, local), `emptyDraft`, `withCurrentTime`, `normalizeMeetingUrl`, `composerSchema` (zod, one per kind), `parseDraft`, `visibleErrors`, `activityColumns`, `draftFromActivity`), tested in `lib/record-activity.test.ts`; the columns, the policies and the `fn_record_activity_write` trigger in `supabase/migrations/20260925120000_record_activity_fields.sql`; `NewLeadSheet` in `features/leads/components/new-lead-sheet.tsx` with `LeadForm`'s `prefill` and `pickPipelineId` in `features/leads/lib/new-lead-pipeline.ts` (tested). The pure half in `lib/record-page.ts` (`readRecordTab`, `withRecordTab`, `splitEmptyFields`, `emptyFieldsToggleLabel`, `joinFacts`, `firstName`, `formatCalendarDay`, `formatDayTime`, `telHref`, `mailtoHref`, `whatsAppHref`, `externalHref`, `websiteLabel`, `leadStanding`, `summarizeLeads`, `leadSummaryLabel`, `ENGAGEMENT_KINDS`, `activityTitle`, `isMirroredNote`, `buildFeed`, `buildUpcoming`, `dueLabel`, `PeopleById`, `feedByline`, `shortPersonName`, `activityTime`, `lastActivityLabel`, `FEED_FILTERS`, `filterFeed`, `feedFilterOptions`), tested in `lib/record-page.test.ts`. The Contact: `ContactDetailPage` in `features/contacts/components/contact-detail-page.tsx` (`data-fluid-page`, the two columns `contents` below `lg` and reordered with `order-*`, `people` and `viewer` from the route, `composerEnv`, `EmailText`, `DiscValue`, `PageChrome` with `phoneMenu`, `AddContactModal`, the Recycle Bin dialog through `deleteContactsAction`); `features/contacts/lib/contact-record.ts` (`readContactTab`, `withContactTab`, `nameWithSalutation`, `showBusinessUnit`, `secondaryValues`, `socialLinks`, `formatCustomValue`, `readDisc`, `discSummary`), tested in `features/contacts/lib/__tests__/contact-record.test.ts`; `app/(app)/contacts/[contactId]/page.tsx` (the timeline, the notes and the file count read with the contact, the viewer beside it and the people it names after it; leads in the Recycle Bin left out). The Company: `CompanyDetailPage` in `features/companies/components/company-detail-page.tsx` (`people`, `viewer` and `composerEnv` as the Contact's; the name saved through `updateClientCompanyAction`, Delete through `deleteClientCompaniesAction`, `AddCompanyModal`, `AddContactModal` with `preselectedCompanyId`), `CompanyContactsTable` in `company-contacts-table.tsx`, `features/companies/lib/company-record.ts` (`readCompanyTab`, `withCompanyTab`, `companySupportingLine`, `companyCardLine`, `companyAddress`, `companyGroupTitle`), tested in `features/companies/lib/__tests__/company-record.test.ts`; `app/(app)/companies/[companyId]/page.tsx` (the contacts, the timeline, the notes and the file count read with the company, the viewer and the people as the Contact's; contacts, leads and subsidiaries in the Recycle Bin left out). Still open: the Lead page keeps its own panes and header, and its tasks (`lead_checklists`) are not follow-ups; nothing yet lists a person's follow-ups across records; the follow-up's checkbox is the shared `Checkbox`, whose outline at rest is overridden here because its own is a hex; the inline editors are popovers on a phone too, where M3 would open a bottom sheet; About keeps a fixed order rather than the tenant's form layout |
