# Design direction, Sales Activity

Written down because the direction was previously only in someone's head, which
is how a form ends up with a wall of checkboxes and nobody able to say whether
that was a decision or an accident.

## Design Read

A field-operations tool for sales reps and the appointment team. Most screens
are read standing up on a phone with one hand, or worked through at a desk on a
wide monitor. Nothing here is marketing: every screen either answers "where am I
going next" or captures what happened.

**Dial: ENERGY 1 / RHYTHM 1 / MOTION 1.**

Calm, uniform, and near-static on purpose. A rep checking an address outside a
client's office is not an audience for personality, and motion on a slow field
connection reads as lag. Variation earns its place only where the content
genuinely differs (the live board is loud because it is read across a room).

## Where the rules come from

**Material Design 3 is the rulebook, not the skin.** We take its logic for
structure, spacing, hierarchy and component behaviour. We take none of its
palette, none of its shape system, and none of its component visuals. Swapping
our logo out must not leave something that looks like a Google product.

What we actually adopted:

| Concern | Rule taken | How it lands here |
|---|---|---|
| Touch target | 48dp minimum for anything tappable | Form controls and list rows are 48px. Where a control is drawn smaller for density (a chip's remove button at 28px), the hit area is expanded to 48px instead of inflating the control |
| Spacing | 4dp grid | Label to control 8px, field to field 20px, section to section 24px |
| Text fields | Label above, control, supporting text below; error text takes the supporting text's place | `FieldShell` in the mission form |
| Multi-value input | Input chips, each carrying its own remove affordance | Supporting sales |
| Menu vs search | A list short enough to read needs no search box | Search appears from 7 options up |
| Buttons | Filled is the primary action and sits at the trailing edge; secondary is outlined | Simpan filled, Batal outlined. On a phone the primary sits lowest, nearest the thumb |
| Surfaces | Three tones, each one step off the last: page, card, field. A card groups one subject; a divider separates a card's header from its body | Page `--background`, section cards `--card`, fields `--field`. Each form section is its own card |
| Text field outline | The outline is held to 3:1 against the surface it sits on (M3 `outline` role, WCAG 1.4.11) | `--input` is #7F8993: 3.56:1 on the card, 3.28:1 on the field fill. Focus swaps it for `--ring` |
| Status vs action | A status is a passive label; a thing the reader can do is a button. They never share a shape | Status is a colour dot plus sentence-case text, no container. Anything actionable (Terima, Join) is a real button in the Aksi column or on the card |
| Icons | Material Symbols (M3's own set), Rounded style, weight 400, drawn on the 24dp grid; an icon never carries meaning a label does not also carry | Generated into `components/icons` from `scripts/icons/map.json` by `npm run icons:generate` at the repo root. Export names are the ones the code already used, so a swap of the set is one map edit, not 200 file edits. Brand marks (four social icons) are not in Material Symbols and keep their previous stroke drawings |
| Records list, dashboard summarises, tabs switch between them | A record type gets a list view of its own (filter, sort, open in context) separate from the analytics over it; the two are sibling views of one destination, so they are M3 primary tabs under one page title, not two navigation items (Salesforce list views vs dashboards, Pipedrive Activities vs Insights) | Laporan: tab Daftar (`/workspace/reports`) and Ringkasan (`/workspace/reports/ringkasan`); a row opens the mission at its report card; the mission list offers "Lihat laporan" as the outlined action on a reported row |
| Chips are M3 chips, not pills | 32dp tall, 8dp corners, outlined at rest, tonal (secondary container) with a leading check when selected, 48dp touch target from spacing; the full-round pill is for buttons and segmented buttons only, and the primary fill is for the one main action, never for a chosen answer | `ChoiceChip` / `ChipRow` on the report form and the mission filters; `--tonal` / `--tonal-foreground` tokens shared with the navigation indicator |
| Photos: camera first, thumbnails, one control per photo | A photo field starts with an outlined button that opens the camera on a phone and the picker elsewhere, a second button for the gallery; each photo is a thumbnail with its own remove control; a linear progress bar per upload; errors as supporting text; drag-and-drop only as a desktop extra; files downscaled on the device (Jobber, Zoho FSM; Google Forms file upload limits) | `PhotoField` on all three forms, `PhotoGallery` on detail pages; private bucket per company folder, signed URLs, files purged with the rows |
| Recordings: the phone records, the app keeps | An audio field never opens a microphone. Safari on iOS stops capture the moment the screen locks, a call comes in or the person switches apps, and a 40-minute meeting cannot be retaken; the phone's own recorder (Memo Suara) survives all of that. So the field is one outlined "Unggah rekaman" button that opens the picker, a row per recording with its length, size, the native player and its own remove control, a linear progress bar per upload that reports real bytes (a 30 MB file on mobile data deserves more than a spinner) with a cancel, errors as supporting text. The helper carries the consent reminder ("Beri tahu klien bahwa pertemuan direkam") and the admin may reword it. The supporting text names the rule before anyone picks: count, formats (M4A, AAC, MP3), size, in that order (M3 supporting text conveys requirements up front and the error replaces it; Google Forms' "Upload 1 supported file: PDF or image. Max 10 MB"). WAV is refused at pick time with the recorder setting to change, since uncompressed audio (10 MB a minute) never fits a meeting into the cap and a size error after the upload would come too late. The detail page lists recordings with the native player and a download that keeps the file's own name, because the next stop is a coaching tool that wants the file. Same access as photos: the unit's folder, signed URLs, purged with the rows (HubSpot and Salesforce attach call recordings to the record; Jobber's attachments) | `AudioField` on all three forms, `AudioList` on detail pages; `lib/audio/*` mirrors `lib/photos/*`; `sales_mission_audio` bucket under the same folder policy; `visit_audio` core field in the report's Lampiran card |
| Dialog: headline and actions stay, content scrolls | A dialog never grows past the window; only the region between the headline and the action row scrolls, with a divider at each end while there is more, and a thin scrollbar inside the rounded surface | `DialogBody` in the shared dialog: `-mx-6 px-6 min-h-0 flex-1 overflow-y-auto`, dividers appear only when scrollable; every form dialog wraps its fields in it |
| Editable after send, never silently | A sent record can be changed by its author inside a window and by an admin at any time; every change asks a reason and keeps the outgoing version (Salesforce field history, HubSpot activity edits, Google Forms "allow editing after submit"). Sending a record back with a note is the admin's other move | Laporan kunjungan: "Ubah laporan" with an inline reason, the window set in Aturan mission, "Minta klarifikasi" dialog for admins, and "Diubah N×" with the version list on the card |
| Data table column widths are decided, not discovered | The identifier column takes the remaining width and never wraps to three lines; every other column has a width sized to its content; lower-priority columns leave at narrower window classes before the table scrolls; truncated text carries its full value on hover (Gmail, HubSpot and Salesforce list views: fixed widths, the name column widest, secondary columns hidden or scrolled) | Prospek and Mission tables use a fixed layout with the widths on the header cells, each sized to what the cell holds (Jadwal 130, a facepile 210, a status with its second line 200, one button 170): a generous width is paid for by the name column on a 1280px laptop, where Status 240 + Aksi 230 once left the name at 150px while the two sat half empty; Pemegang / Sales utama leave below xl, Dibuat / Lokasi below 2xl; the phone list shows cards instead |
| Bulk entry beside single entry | A list that people build from a spreadsheet gets a paste-a-list path next to the one-at-a-time path, one item per line (Google Forms, HubSpot, Salesforce picklists); a multi-line paste into the single field is read as a list too | Editor opsi: "Tempel daftar" with a preview count and duplicates skipped; "Hapus semua" clears the draft with an inline Batalkan instead of a dialog, because nothing is written until Simpan |
| Carry known facts forward | A record made from an earlier step starts with that step's facts filled in and marked with their origin; the user confirms or replaces, never retypes (Salesforce event → contact, HubSpot meeting attendees) | Laporan kunjungan: the appointment contact is Kontak 1 with a "Dari janji temu" mark, removable, and offered back with one button when absent |
| A reading is a badge, signed, behind a switch | A judgement about a person (how they communicate) is shown as a small tonal badge beside the name with its meaning one step away, never as a filled status chip and never as a fact: it carries who read it and when, and the words are the training's own so every rep reads the same advice (Crystal Knows and Humantic AI beside a HubSpot or Salesforce contact; M3 assist chip vs filter chip, supporting text). Recording it is two rows of M3 filter chips (one of four each; the second row disabled until the first is chosen and never offering the same letter), the approach line for what was chosen, and one free line; a reading carried in from an earlier visit is marked with its origin and a known reading for a freshly typed name is offered with one tap, not applied silently. The whole block exists only while the unit's switch is on, so a team that did not train on it never meets it, and it stays internal: no public page, no export | `DiscBlock` in `VisitReportForm` (`ChoiceChip` rows Utama / Pendamping, `describeDisc`, note `Input`, “Dari kunjungan sebelumnya · dinilai oleh …”, the Pakai offer from `lastDiscForCompany`); the `DISC` badge and approach line under Ketemu siapa on the activity page; `contactDiscEnabled` SwitchRow under Laporan kunjungan in Pengaturan → Aktivitas; `lib/contacts/disc.ts` holds the letters and the training's words; LeadEngine shows the same badge as Communication style (DISC) on the contact page from `custom_fields.disc` |
| The call is the button; the log follows it | A prospect's action is "Hubungi", not "Catat …": reps call from WhatsApp outside the app and never came back to write it down, and "kontak" already means a person here. Hubungi opens a menu of the prospect's channels (HubSpot, Pipedrive, Close put Call / WhatsApp / Email on the record): WhatsApp with the unit's opening line filled in, which opens the app on a phone and WhatsApp Web or Desktop on a desk; Telepon, which dials on a phone and on a desk copies the number and says so, since most computers have nothing to dial with (Salesforce and Zoho without a dialer: show the number, log after); Email. "Catat follow-up" stays last, for what happened outside the app. Each channel remembers the follow-up it started, and when the tab is seen again the app asks "Bagaimana hasilnya?": on a phone a modal bottom sheet with a scrim, the same surface as the Hubungi menu, on a desk a centred dialog with the same scrim (six answers is not a snackbar; two cuts as a card, above the bar and at the desk's corner, sat on the same white as the prospect cards and rows and read as one more row, since in M3's light theme a floating surface is told apart by its container colour as much as its shadow, and a scrim leaves no doubt; HubSpot and Pipedrive ask "Log call?" in a modal), one tap for the outcomes that need nothing else, the full dialog prefilled for those that need a date or a reason, "Nanti" to put it away, and the note expires on its own (HubSpot's "Log this call?"). The button is tonal: important on every row, but not the screen's one filled action | `ContactMenu` (`ResponsiveMenu`, `Button variant="tonal"`), `follow-up-pending.ts` (sessionStorage, 30 min), `FollowUpPrompt` (visibilitychange / focus / a moment after, `BottomSheet` below md and `Dialog` above, `ChoiceChip` outcomes, `logProspectAttempt` with the suggested status), `DialogTarget.prefill`; `whatsapp-greeting.ts` renders the template (`{sapaan} {kontak} {sales} {perusahaan}`, default "{sapaan} {kontak}, saya {sales} dari {perusahaan}.") the admin edits in Pengaturan → Aktivitas → Prospek (`mission_settings.whatsapp_greeting`) |
| Inbox before the record | A stage before the main object lives in its own list with passive statuses and a logged history, and converts into the main object rather than being edited into it (Pipedrive Leads Inbox, Salesforce Lead → Opportunity) | Prospek: statuses are dot + label, every contact attempt is a row, Confirmed is reached only by scheduling the visit, and a converted prospect shows its mission's fate |
| Admin-editable vocabulary over fixed semantics | Labels, colours and order are the team's; the kind the code acts on is locked at creation and shown with a lock and its reason | Pengaturan → Status prospek: rename, recolour, reorder, add, archive; never delete; one open and one won anchor always active |
| Destructive actions | Deleting is reversible by default: it moves to a bin, and only removing from the bin needs a confirming dialog that names the count. An admin restores; retention is 30 days | Mission list "Ke sampah", Pengaturan → Sampah with Pulihkan and Hapus permanen; the clear-all zone empties into the bin |
| Sign-in | Text fields 48dp, outlines at 3:1, the visibility toggle a focusable 40dp icon button with its label in the product's language, the filled button without a resting shadow, helper text at full contrast. Surfaces from the tokens (`--background`, `--field`, `--card`), never a literal white. On a phone the form starts near the top so the fields stay in view when the keyboard rises (Google, Microsoft, Slack sign-in); on a desk it sits at the panel's centre | Both apps' login pages; Sales Activity's `pt-10 lg:items-center` |
| A table row opens its record | The row is the link, with the name styled as one for the keyboard and a new tab; there is no "open" control at the end of the row. The old arrow (↗) was an icon button whose icon means "opens elsewhere", which it did not, and people did not find it; M3 asks an icon button's icon to be universally read, and Gmail, HubSpot, Linear and Pipedrive all open the record from the row. Clicks on controls, text selections and modifier keys are honoured, not swallowed. The people column shows the same faces as the phone's card (M3 list items lead with an avatar; HubSpot and Linear owner columns) | `useRowLink` on the Prospek and Mission table rows, `cursor-pointer`, the name as a `Link` with `hover:underline`; `TeamFacepile` in Sales utama; the Aksi column holds only the row's own action (Join, Terima, Isi laporan, Hubungi) |
| Rows needing attention | A list item that needs the reader is toned at its edge, not shouted at | 4px `--warning-foreground` left edge on the row or card; the demand itself is the button beside it |
| Labels | Sentence case. M3 dropped all-caps button and chip labels in 2021 | No uppercase, wide-tracked pills anywhere in a list. Uppercase stays on page eyebrows only |
| Picking a time | Day first, then time; busy blocks drawn and named, never hidden; a clash warns, it does not block (Calendly's two-panel shape, Google Calendar's "find a time") | `SchedulePicker`: month on the left, the chosen day as an hour timeline on the right with each assignee's visits as named blocks, the travel buffer shaded around them, the candidate drawn on top in primary or danger |

## Izin

The permission matrix is the only place access is decided, and it must be
readable without the code. Three rules keep it that way:

- **Every record-bound action asks `canPerformOn`**, never a hard-coded seat.
  The grant (Lihat/Buat/Ubah/Hapus) and the reach (Cakupan) come from the same
  row; `lib/access/record-scope.ts` defines who owns what, and nothing else
  does. A supporting sales keeps participation rights (join, leave, answer,
  propose, note) because those are not ownership.
- **A refusal uses the matrix's words.** "Peran Anda hanya boleh mengubah
  mission miliknya sendiri" points the admin at the Cakupan control; it never
  says "hanya sales utama" when the rule is a scope.
- **"Admin" means one thing:** Ubah on Pengaturan mission (`isSettingsAdmin`),
  which opens the settings screens and the bin. It is not a record scope; a
  supervisor's reach over reports comes from Laporan kunjungan → Ubah with a
  Cakupan of Tim or Semua.

## Istilah

The product is **Sales Activity**; the record a person schedules, joins and
reports on is an **aktivitas**. The code, the routes' internals, the
`sales_mission` schema and the `sales_mission_*` module ids still say
"mission" on purpose: that layer changes in its own migration, and nothing
a person reads depends on it.

| On screen | Stays | Never |
|---|---|---|
| Sales Activity; aktivitas / Aktivitas ("Aktivitas baru", "Ubah aktivitas", "Buka aktivitas", "Tim aktivitas", "Jenis aktivitas"); Pengaturan aktivitas; Riwayat perubahan (the audit log, renamed so it does not collide) | kunjungan, laporan kunjungan, jadwal kunjungan (a visit is one kind of aktivitas); sales utama, sales pendukung; prospek; papan live; lead | "mission" in any sentence a person reads |

The product name lives in `lib/brand.ts`; every in-app URL is built by
`lib/paths.ts` (`/workspace/activities/…`), and `next.config.ts` redirects
the old `/workspace/missions/…` addresses. The phone's calendar subscription is **Kalender saya** on screen and `calendar_tokens` plus `/kalender/<token>/aktivitas.ics` underneath.

## What is ours, not Material's

- **Colour** comes from `app/globals.css` tokens only. Every pairing is held to
  WCAG AA against the surface it actually lands on, not against white by default.
- **Type** is Plus Jakarta Sans, chosen for its Indonesian-language legibility at
  small sizes, not for a Material type scale.
- **Radius** is the existing `rounded-md` / `rounded-xl` pair. No pill-everything.
- **Uppercase wide-tracked labels** belong to page-level breadcrumbs only. A
  heading inside a form is sentence case, because it is a heading and not a tag,
  and so is the label above a card's headline ("Laporan kunjungan", "Tim
  aktivitas", "Tampilan bulan"): `text-xs font-semibold text-muted-foreground`,
  M3 label-medium, as Linear, Notion, HubSpot and Google Workspace set their
  section labels. The tracked capitals exist once, on the page eyebrow.

## Language

Indonesian throughout the product surface. English is for code and comments.
One word per concept: a field is a "field", not sometimes "kolom".
| Filtering a list | Search always visible; each facet a button that opens a checklist (values OR, facets AND); state in the URL (Linear, HubSpot, Notion). On a desk the bar shows only what is narrowing the list: the two or three everyday facets always, then each other facet only while it is in use, then "+ Filter" listing the rest; a facet picked there lands in the bar with its list open and leaves again if closed empty. An active facet's button carries its value ("Industri: Belum diisi", or the label with a count), so the bar is its own summary and there is no chip row under it, only "Bersihkan semua" (Linear, Notion, Jira; M3 filter chips, menu) | `MissionFilterBar`: Status, Sales always; Tanggal, Laporan, Dibuat oleh, Lokasi, Industri (with "Belum diisi" pinned above a divider), Jenis as `FacetSpec`s. `ReportFilterBar`: Status, Sales utama, Tanggal always; Hasil, Minat, Next action, Peluang, Lead as specs. `FilterBarFrame` `more`/`onClearAll`, `AddFilter`, `FacetButton` value summary, `?q=&status=&sales=…` |
| Acting on many rows | Checkbox per row and in the header; a selection bar appears with the count and only the actions that apply to a set (Gmail, Linear). On a phone the checkboxes are not there until asked for: a permanent column of them steals width from the name and makes a list read as a form, so a long press on a card, or "Pilih aktivitas" in the top bar's overflow, enters a selection mode in which the cards show their boxes and a tap ticks instead of opening; Batal leaves it (Gmail, Google Files, Photos; Android's contextual action bar) | `MissionTable` with `canDelete`: bar reads "N aktivitas dipilih · Ke sampah · Batal"; confirm dialog states what cascades. `SelectionModeProvider` shared by `ActivitiesPhoneMenu` and the cards, `useLongPress` on the card body (pointer events; a scroll cancels it, the click after it is swallowed, the browser's link menu is suppressed) |
| Reversible, so not confirmed | An action a person can take back gets no confirming dialog; it happens on one tap and the snackbar that follows carries "Batalkan" for a few seconds. A dialog is for what cannot be undone or costs money (M3 snackbar with action vs dialog; Gmail Urungkan, Google Calendar one-tap RSVP, Slack join/leave). Where the action has a side effect on other people, the undo inside the window is silent: it removes what the action told them, rather than telling them again. And a per-row action in a list is outlined, never filled: a filled button on every row is the loudest thing on the screen and the easiest to hit while scrolling; the filled version belongs to the record's own page | `JoinButton`: outlined in `MissionTable` rows and cards, `emphasis="filled"` on the activity page; the toast names the client and offers Batalkan for 6 s; `undoJoinMission` → `sales_mission.fn_undo_join` deletes the assignment, the unread MISSION_JOINED notifications and the history line within 2 minutes, else falls back to `leaveMission` |
| Destroying everything | Type-to-confirm in a bordered danger zone, away from the list (GitHub) | Pengaturan → Data: "HAPUS SEMUA MISSION" phrase, checked again on the server |
| Audit trail | One line per action in plain words, who and when beside it, before/after a click away; filters for who, what, which, when (Salesforce Setup Audit Trail, HubSpot activity log) | Pengaturan → Riwayat aktivitas; rows written by database triggers, grouped per transaction |
| Dashboard vs screen | One configuration, two renderings: set it up where you can click, send exactly that to the display (Geckoboard, Grafana TV mode). The in-app view uses the app's tokens; only the screen inverts | Papan live: control bar (segmented range, facets, panel chips) → same options in the URL → "Tampilkan di layar" and "Buat tautan layar". Privacy (client names) is bound to the token, never to the URL |
| Selection vs action controls | Controls that change what is shown look different from controls that do something (M3 segmented button, filter chips vs filled/outlined buttons). A chosen option, in a chip or a segmented button, is tonal (secondary container) with a check, never the primary fill: that fill is for the one main action of a screen, and a filled "Bulan ini" beside a filled Ekspor read as two things to press | Segmented Hari ini/Minggu ini, facet buttons, toggle chips on the left; one filled action and one outlined action on the right; `Segmented` and `ChoiceChip` active state `--tonal` |
| Surface layering | All surfaces from one neutral ramp tinted toward the primary, stepped a few percent apart: page < nav panel < card. Dividers are outline-variant (tinted), never black alpha. The nav's active item is the secondary-container indicator: tinted fill, primary text, no shadow (M3 tonal surfaces, navigation drawer) | `--background` #F6F8FB, `--sidebar`/`--muted` #EEF2F7, `--card` #FFFFFF, `--border` #D9E0E8, `--sidebar-primary` #D9E4F5. Same tokens in LeadEngine so switching apps does not change the material |
| Edit vs reschedule | One door for anyone who may change a thing; a separate "request" only for those who may not (Google Calendar: Edit + "send update to guests?"; Calendly: Reschedule as a request) | Ubah carries the schedule for whoever the matrix lets change the mission (its owners, and anyone whose Cakupan reaches them; the primary alone per tenant rule), and asks for an optional reason when it changed; Usulkan jadwal lain remains only for supporting sales and centrally-scheduled units. No "Pindahkan jadwal" button |
| Switching apps, loading a page | Feedback at the click, one branded screen across the origin change, no white flash, content arrives once without shifting (Slack workspace switch, Google app launcher, Notion/Linear splash then skeleton; GitHub/YouTube top bar). M3: circular indeterminate for one whole screen, linear at the top edge for a page load, none for very short waits, fade-through between unrelated destinations, reduced motion honoured | `AppTransit` drawn by the switcher on click (overlay) and by each app's root `loading.tsx` on arrival; `html`/`body` carry the background inline; sidebar fold and width come from parent-domain cookies so the first HTML is already right; `TopLoader` shows after 120ms; every new animation has a reduced-motion branch |
| Phone shell | Compact window class (below `md`): a navigation bar of 3–5 top destinations with icon, label and a tonal active pill, 80dp plus the safe area; the rest behind "Lainnya" in a bottom sheet; a small top app bar carrying the page title, back on sub-pages, one trailing action; one extended FAB for the screen's primary action, above the bar; the bar steps aside for a form's own action bar (M3 navigation bar, top app bar, FAB, bottom sheet, window size classes) | `MobileNavBar`, `MobileTopBar` fed by `PageChrome`, `Fab` from `WorkspacePage`'s `primaryAction`, `FormActionBar` announces `hideNav`; `h-dvh`, `viewport-fit: cover`, `env(safe-area-inset-*)`, one page scroller `#page-scroll` |
| Touch targets | 48dp is the target, not the glyph: a 16dp checkbox or a 20dp icon keeps its look and grows its hit area; below `md` every button size steps up one notch (M3 accessibility, touch target size) | `buttonVariants` responsive heights, `Checkbox` `after:-inset-3`, 40dp dialog/sheet close, 44dp menu rows, chip removes and pager arrows |
| Installed app | The web app is the app: manifest, standalone display, home-screen icon, theme colour; a service worker that caches hashed static assets only and never a page or a datum, versioned per build so no deploy can serve a stale UI (PWA; the LeadEngine kill-switch is the lesson) | `app/manifest.ts`, `public/icons`, `public/sw.js?v=<build>`, `PwaRegister`, `/workspace/pasang` with the Android prompt and the iOS steps |
| Lists on a phone | Search stays; the facets go behind one "Filter" button that carries the active count and opens a bottom sheet, so a seven-facet bar costs one row, not four; active filters remain visible as one sideways-scrolling chip row; every chip row that scrolls sideways bleeds to the screen edge and fades at its right edge, the fade lifting once the end is reached, so a chip cut by the edge reads as "more this way" rather than as a fault (M3 chip carousel; YouTube, Google Maps); a long list grows with "Muat lagi" instead of turning pages, up to the largest page size, and only then steps to the next page (M3 filter chips in a modal bottom sheet, list pagination on compact windows; Gmail, Google Maps filters) | `FilterBarFrame` around the three filter bars (`useCompact`), `.chip-scroll` in globals.css on the quick-filter, calendar, guide and active-chip rows, `MissionPagination` compact branch stepping `size` through `PAGE_SIZES`; the bottom sheet holds the everyday facets and every `more` facet together, and the chip row exists only here; desktop keeps the pager |
| Content before chrome on a phone | A list on a phone opens on its records, not on its furniture: search is the first control (Gmail, Google Maps, Linear), the one-tap chips sit under it and narrow what it found, and everything a rep in a lobby never needs (export, import, bulk selection) goes behind the top app bar's overflow menu rather than standing as buttons above the list. A sentence that only teaches what a coach mark already teaches is not shown there either; a description that states facts (a record's type and place) still is. On a desk the header keeps its buttons and its sentence (M3 top app bar overflow, filter chips as secondary narrowing) | `WorkspacePage phoneDescription={false} phoneAction={false}` on Aktivitas; `ActivitiesPhoneMenu` announces Export, Import dari .xlsx and Pilih aktivitas through `PageChrome menu` (items may carry `onSelect`, not only `href`); `FilterBarFrame quick` places `QuickFilterChips` under the search on a phone and above the bar on a desk. The same on Prospek (`ProspectsPhoneMenu`: Import, Pilih prospek) and Laporan (Ekspor; on Ringkasan the exports and Atur widget, announced by `DashboardEditor`, which owns the edit mode); `ReportTabs` sits flush under the phone's app bar, where M3 puts primary tabs. `SelectableCardBody` is the shared card body (link, or tick in selection mode, long press to enter it) for the activity and prospect cards |
| Detail page on a phone: one door each | Each action has one door on a phone. Ubah is the bottom bar's pencil and the overflow's "Ubah aktivitas", not also a button in the facts card; Join is the bottom bar's one filled button, not also the team card's; a destructive secondary action (Batalkan aktivitas) is the last, danger-inked row of the overflow, as Google Calendar's Delete and HubSpot's and Salesforce's mobile record actions, its dialog unchanged; the record's id and the description line that repeats the facts card are desk-only. A state is said once: while the tenant's rule keeps the report closed, the card body says when it opens, and no footer repeats it (M3 top app bar overflow, bottom app bar) | `ActivityPhoneMenu` (links + Batalkan via a controlled `CancelMissionButton`), `max-lg:hidden` on the card's Ubah, id and cancel row, `JoinButton className` when `compactAction === "join"`, `phoneDescription={false}` |
| A calendar's count sits under the date on a phone, at the corner on a desk | On a phone a month cell is 44px wide; a badge pinned to its corner covers a two-digit date, so the count sits in flow under the number (M3 badge on a date; Google Calendar's month dots). From `md` the cell is wide and, when the card is short, only one line tall: there the in-flow badge fell below the cell's edge and was clipped, so it pins to the top-right corner as it always had. The once-only actions (public link, phone feed) wait in the phone's overflow | `MonthGrid` badge `mt-0.5 self-center` below `md`, `md:absolute md:top-1 md:right-1` above; `CalendarPhoneMenu` |
| A long form on a phone | The sticky chrome is the app bar and the section chips, nothing between (the facts line is desk-only); the chip row fades at its edge like every scrolling chip row. The bottom bar carries the one next step and the state that gates it ("Belum lengkap: …", "Laporan siap dikirim", the draft's sync); Kembali is the app bar's arrow and a destructive Buang draf is the overflow's danger row, both only from `sm` up as buttons in the bar (M3 bottom app bar; Google Forms' single Submit). Attachments close the form as their own last section, so no section, and no chip, appears twice. A helper line says what to do, once: the control's own limit and the admin's hint share one line, and a label, a checkbox and a helper never restate each other | `VisitReportForm`: `PageChrome menu` Buang draf, `hidden sm:inline-flex` on Kembali and the text button, `Lampiran` in `CORE_SECTIONS`, `FieldShell hint={null}` when the control draws its helper, `PhotoField hint` "Maks N foto · …"; `SectionChips` `chip-scroll`; the report page `phoneDescription={false}` |
| Who is on it, at the card's foot | A card's bottom row is supporting content at the start and the one action at the end (M3 card). On the activity and prospect cards the start holds who: stacked avatars, the lead first, the lead's name written beside them and "+N" for the rest, because most people have no photo and two initials alone say less than a name. "These people → Join" is the pairing Google Meet and Chat draw; Trello and Jira put the members at the foot of a card too. Every card has the row, so "who" is always in the same place, and nothing in it is tappable: the body stays the one link | `TeamFacepile` (`PersonAvatar size="sm"`, `-space-x-2`, `ring-2 ring-card`, max 3) in `MobileMissionCard` (the lead, then `assigneeIds` with photos from the unit's `people`) and the prospect card (the holder); the body's grey name line is gone |
| An agenda groups by day | A list of what is coming shows each date once, as a group header, and each row only its time, so the name keeps the width a repeated "21 Sep, 09.00" would take; what was called off is not "next" (Google Calendar schedule view, Outlook agenda) | Hari ini → Berikutnya: `upcomingDays` grouped by `missionDayKey`, "Besok, Sen, 21 Sep" headers, a 44px time column, cancelled visits excluded (the ones awaiting a new date have their own list) |
| A grid always names its columns | Every layout grid declares `grid-cols-1` at the base size even when it only fans out at a breakpoint. Without it the single column is an implicit `auto` track whose minimum is the content's min-content width, and a `truncate` (nowrap) client name makes that the full name, so the card grows past a phone's edge and truncation never happens. `grid-cols-1` is `minmax(0, 1fr)`, which lets the track shrink and the ellipsis do its work (CSS Grid intrinsic sizing; Tailwind's `grid-cols-*` uses `minmax(0, 1fr)`) | `grid grid-cols-1 gap-… sm:grid-cols-…` on every page grid; `TodayCard` on Hari ini was the case that surfaced it |
| Detail and forms on a phone | A detail page has one bar at the bottom and one next step on it, chosen by state (answer, then report, then join, then edit); the cards reorder for the rep (facts, answer, report, contact, team, notes) and the top bar's overflow menu carries the links worth reaching without a scroll. A long form gets a sticky row of section chips that shows where you are and marks what is still empty; "Belum lengkap" names fields that scroll to themselves; the schedule strip fits the screen and a tap on it sets the start (M3 bottom app bar, secondary tabs, top app bar overflow, 48dp targets) | `FormActionBar until="lg"` on the activity page with `compactAction`, `max-lg:order-*` with `max-lg:contents` columns, `PageChrome menu`; `SectionChips` in both forms, `jumpTo` in the report form, `SchedulePicker` 44dp days, `max-h-[45dvh]` strip with `onClick` |
| Teaching empty states | An empty list teaches: one sentence on what the list is, one to three numbered steps that fill it, the first step as a button, and "Pelajari" into the guide. The first-time user learns the product here, at the moment of need, not from a tour (Notion, Linear, Figma empty states; M3 empty state pattern) | `EmptyState` with `steps`, `learnHref`, `icon`; the activities, prospects and reports lists; Hari ini's empty day links to the guide |
| Coach marks | No sequential tour; one-step marks in the shape of M3's rich tooltip: a title, one sentence, "Mengerti" (and "Pelajari"), anchored to the control the first time it matters, one per screen, after the screen settles, never locking it, dismissed by any outside tap. "Once" means once per account, on every device (Google Workspace feature discovery) | `HintsProvider` in the shell fed by `listSeenHints`, `useCoachMark`/`CoachMark`, `sales_mission.user_hints` (self-only RLS) with localStorage fallback; marks on Lainnya, Pasang, the Aktivitas FAB, Join, Kirim laporan |
| The guide | One page, a few short parts, each ending in the link that does the thing; anchors so every "Pelajari" lands on its part; reachable from Lainnya, the sidebar under Pengaturan, and every teaching empty state (in-product help centres of Linear and Notion) | `/workspace/panduan`, `paths.guide`, `paths.guideSection(id)` |
| Quick filters and remembered views | The everyday narrowings sit as filter chips above the list, one tap each, keeping the rest of the query; the sheet holds the rest. A list reopens the way you left it (a cookie per list, redirected on a bare open; the URL stays honest), and a calendar has a "Hari ini" jump (M3 filter chips, secondary navigation state; Gmail, Linear, Jira, Todoist, Google Calendar) | `QuickFilterChips` (Semua · Saya · Hari ini · Minggu ini · Mendatang · answer lenses: whose, then when, so "Saya" is never the chip past a phone's edge), `sales=me` resolved server-side, `RememberView`/`ViewLink` + `rememberedView`, calendar "Hari ini" |
| Calendar out | The phone's own calendar carries the schedule: a one-way iCalendar feed per person on a secret URL shown once, and a second feed for the team when the role sees beyond its own (the same reach as the list, resolved for that person by `fn_visible_owner_ids_for`, the person's name first in each event) (the board-link pattern: hash only, one active link, retire by replacing), no OAuth, cancelled visits kept as CANCELLED so subscribers see the change; plus "Tambah ke kalender" per visit (Google template link, .ics for iPhone and Outlook). Two-way sync is a later tier | `/kalender/<token>/aktivitas.ics`, `lib/calendar/*`, `sales_mission.calendar_tokens` (self-only RLS), `/workspace/kalender-saya`, detail-page menu |
| Hand-off to the CRM | Required fields are enforced in the form ("Belum lengkap" naming each, one tap to it), never only by a greyed button; the one-way step (one push per activity) goes behind a summary dialog that shows the lead as the CRM will, then commits. Pick lists that belong to the CRM (category, grade) are read from the CRM's Master Options and never copied; the report's interest level only suggests (M3 dialog for consequential, irreversible actions; Salesforce Convert Lead, HubSpot) | `PushLeadPanel` with `missing`/`jumpTo`, `ChoiceChip` categories from `/api/v1/master-options`, the confirmation `Dialog` (a bottom sheet on a phone), `suggestCategory`, `lead_pushes.category/grade_lead` for Ringkasan |
| One vocabulary across the hand-off | A value that names the same thing in both apps (industry) has one list, edited in one place and matched to the CRM's by the admin; it flows prospect → activity → CRM company without being typed again, fills only what is empty, and never overwrites what a person or the CRM already set (HubSpot property sync rules) | `withSharedIndustry` (activity form reads the prospect form's list), `fillIndustry` in the forms, `industry` on the company create call, `fn_find_or_create_client_company(p_industry)` fills an empty CRM industry only |
| Wall board | A wall has to make people want to look, so it is composed around one focal point, not laid out as a table (M3 Expressive: hero moment, emphasised type, extra-large shapes; airport FIDS for the schedule; Geckoboard/Grafana TV for the progress). The spotlight card on top holds the visit happening now, else the next one with a countdown ("dalam 45 menit"), else a done state that says so; below it the schedule is a timeline (a node per visit: check when done, a live ring for now, hollow for later) of rounded tonal rows, and beside it the day's progress (big "2 / 6 selesai", a progress bar, three small counts) and the team, each person with an initials avatar in an identity colour and "Jadwal selesai" once their day is done. The date is the headline and the clock is large, because on a wall it is ambient information people glance at. Past rows step down in tone so the first full-contrast row is the next visit. The list ends with "Tidak ada jadwal lagi" instead of a void; with no visits at all the spotlight takes the whole column. A list taller than its box scrolls on its own and returns to the top. Everything sized in `em` from one root that follows the screen, so 1080p, 4K and a portrait panel show the same composition | `app/board/board-view.tsx`: `SpotlightCard`, `StatsCard`, `VisitRow` + `TimelineNode`, `TeamRow` + `Avatar`; `.board-root`, `.board-backdrop`, `.board-ring` in globals.css; `AutoScroll`, `BoardClock`; in portrait the rail becomes a two-card band under the schedule and the Sales column drops |
| Drawer rail vs account menu | The navigation drawer holds 3 to 7 destinations and one Administrasi group for admin settings; personal and supporting pages (Kalender saya, Panduan, Yang baru), the panel toggle and Keluar live behind the account menu at the foot of the drawer, not as rows (M3 navigation drawer: destinations only; Slack, Notion, Linear keep help and release notes off the rail) | `workspace-shell.tsx` `adminNav` (Pengaturan only) and the `DropdownMenu` on the profile block; on a phone the same items sit in the Lainnya sheet |
| Settings matrix layout | One pane: the selector on top as choice chips, the matrix below at full width; two panes only when the screen is genuinely wide. The page is the only scroller; a table row is a list item (headline, one supporting line, the rule behind an info icon), secondary actions in a trailing overflow menu (M3 list-detail collapsing below expanded width, choice chips, list item, data table; HubSpot Roles, Salesforce profiles) | Role & Izin: role chips above the card, hierarchy named in the card header, fixed-layout table (min 760px) whose Modul column takes what the switches leave, presets in a per-row menu, the reading guide folded under one button |
| Integration settings: written keys, tested connections, chosen from what the endpoint reports | A connection to an outside service is set up on one page in two cards, the connection (endpoint, key) then what it offers (models). The key field is empty on every visit and names the stored key only by its last four characters, because a secret is written and never read back (HubSpot integrations, Zapier connections, Vercel environment variables); "Uji koneksi" makes one real request and reports the outcome as a sentence with a check or a cross, and it is the request that fills the pickers, so a person chooses among models the endpoint actually serves rather than typing a name from memory (Metabase's database test, Grafana data sources); a picker falls back to a text field when the endpoint lists nothing, and a stored value the endpoint no longer lists stays selectable rather than being dropped silently. Saving tests again, so the stored status describes what was stored. The same connection is shared by both apps and shown identically in each (M3 text fields with supporting text, list item with trailing control) | `AiSettingsForm` under Pengaturan → AI (`app/workspace/settings/ai`), `testAiConnection` / `saveAiSettings` in `app/actions/ai-settings-actions.ts`, `lib/ai/ai-proxy.ts` (`normalizeEndpoint`, `listModels`), `lib/ai/ai-settings.ts` (Vault-backed key via `fn_ai_store_key` / `fn_ai_read_key`, service role only); LeadEngine mirrors it under Settings → AI |
| AI-written content is labelled, dated, sourced, optional, and never ahead of what the page is for | A sentence a model wrote sits in its own card marked as such (the tonal "Dibuat AI" chip in the card's title row), with when it was written, what it was written from ("dari N laporan hari ini", or "belum ada laporan hari ini") and a line saying it can be wrong, because a generated sentence is an estimate with a timestamp, not a figure (HubSpot and Salesforce mark generative summaries the same way; Google Analytics Insights). The model is handed counts the app computed, never rows, so every number in the card also exists on a widget where it can be checked; each point carries a kind icon (up, down, action, info) and a point a list can answer is a link into that list, the same drill-down the widgets use. The card never blocks the page: if nothing exists it asks after paint and says what it is doing. Its foot is the M3 card foot: the provenance sentence at the start, the one action (Buat ulang) at the end, never in the header; the model's id is audit data, not card copy. **Placement follows M3's layout hierarchy: what a page is for is seen first.** Ringkasan is the board, so the day's insight is one card in the grid, not a banner above it: it goes through Atur widget like every card (moved, resized, hidden), takes the first row at full width the first time a board meets it, and is about today whatever the toolbar says, which its description states (Google Analytics puts Insights among the dashboard's cards; a fixed AI block above the content would push the page's own content below the fold). A free question (Tanya AI) is a supporting pane, not a card: one "Tanya AI" button on the toolbar whose period and people it answers about opens a side sheet on a desk and a bottom sheet on a phone (M3 canonical supporting-pane layout; Gemini in Workspace, Einstein Copilot, Power BI Copilot and HubSpot Breeze all keep the conversation beside the content, never between the toolbar and the content). Inside: the everyday questions as chips (Ask Attio, Pipedrive Sales Assistant, Zia), the field at the foot, each answer under its question and marked as the model's, computed from the same cube rows as the widgets through the person's own session so the answer can never know more than the board, the last few turns kept so a follow-up reads in context, and the conversation kept while the pane is closed. Both exist only while the unit's switches are on, and who sees them, and whose numbers, is the Role & Izin matrix (module Insight AI, read scope), not a per-feature toggle | Built-in `ai_insight` in `lib/reporting/cube.ts` (`box`, `needsInsight`, `first`; `mergeLayout` drops it without the right and seats it first), body `InsightWidget` (`reports/ringkasan/insight-widget.tsx`) inside `WidgetShell` (`badge`); `lib/ai/insights.ts` (prompt, `parseInsightItems`, `dueTrigger`), `lib/ai/insight-facts.ts` (`assembleFacts`), `lib/ai/insight-view.ts` (scope from `sales_mission_ai` read scope, `canSeeInsight`); `ensureTodayInsight` / `regenerateTodayInsight`; the ten-minute run at `app/api/ai/insights/run` called by Supabase Cron; switches under Aturan aktivitas → Insight AI; `AskPanel` (`reports/ringkasan/ask-panel.tsx`) off `RingkasanToolbar`'s `onAsk`, `askSalesData` with `buildAskContext` in `lib/ai/ask-context.ts` |
| Who may see and change what | Action and reach are two settings on one page: per object View/Edit/Delete, each with Everything / Team only / Owned only (HubSpot); object CRUD plus a role hierarchy over record ownership (Salesforce) | Role & Izin: modul × Lihat/Buat/Ubah/Hapus, with a reach menu under Lihat (**Cakupan lihat**, enforced by row security so every list, RPC, export and KPI agrees) and under Ubah (**Cakupan ubah**, also Hapus and record-bound actions; never wider than Lihat). Owners are fixed per record: mission = sales utama + scheduler (assignees see it too), report = sales utama, prospect = holder (unowned is everyone's). Tim follows the Atasan chain from Settings → Users. A narrowed list says so in its description; every refusal names the Cakupan; availability for scheduling stays whole through a definer function |
| Board controls | Page action beside the title (as on every page); one toolbar row with what-to-show (segmented, facets) on the left and how-it-is-shown (live status, a "Tampilan" menu) on the right; view options in a menu, never as permanently-ticked chips (M3 top app bar, segmented button, menu) | `BoardActions` in the header slot, `BoardToolbar` under it; panel toggles are `DropdownMenuCheckboxItem`s |
| Dark distant screen | An M3 dark scheme from the brand blue: five surface tones carry the structure instead of borders (page, card, row, raised spotlight, track), `on-surface`/`on-surface-variant` inks, `primary-container` for the one thing the room should look at, tertiary amber as the live signal (the pulsing dot and ring), and a green custom colour meaning done (check marks, the progress bar, the all-done card). Status is a coloured dot plus a word, never a filled chip: a wall has no pointer, so nothing borrows an interactive shape. People get one of six identity colours, hashed from the name so a person keeps theirs, never used for status. There is no error role: a cancelled visit is taken off the board. A past row dims by stepping down in tone, not opacity. A faint primary glow in one corner keeps the dark from reading as a dead screen. One face, Material's own Roboto Flex with tabular figures, up to 600; no monospace (M3 dark theme, tonal surfaces, custom colours, type scale, reduced motion) | `--board-*` in `app/globals.css` (every text pair clears 4.5:1 on the surface it sits on); Roboto Flex loaded for `/board` only in `app/board/layout.tsx`; `StatusCell` and `avatarHue` in `board-view.tsx` |
| Postponed is not cancelled | A cancel dialog asks which kind of off this is: no follow-up, or postponed with a promised call-back date (Google Calendar's cancel vs propose a new time; Calendly's reschedule keeps the booking's history). A postponed item leaves the calendar and the board but stays on the to-do list until its replacement exists, and the two link to each other | Batalkan dialog: "Batal, tidak ada lanjutan" / "Ditunda, jadwal menyusul" plus a date (default three days out); Hari ini "Perlu dijadwalkan ulang" with the promise date, overdue in red; Jadwalkan lagi records `rescheduled_to_id` on the old mission, whose banner then says "Sudah dijadwalkan ulang" with a link |
| A calendar fits the screen | The month view fills the height under the page header and never scrolls the page (Google Calendar, Outlook): the week rows share the height, the day pane beside it is level at the top and scrolls only its own list. Two panes never share a height by stretching one to the other; they share the viewport. On a short screen the day cells keep a floor and the page scrolls rather than crushing them | Kalender: the section is `lg:h-full`; the month grid `grid-template-rows: auto repeat(n, minmax(0,1fr))` for the month's n weeks; from lg each day cell is a size container (`.cal-cell`), so its content never sizes the row and container queries show two chips, one chip, or only the count as the cell's height allows; the day pane `flex-col min-h-0` with the list `flex-1 overflow-y-auto` |
| Not before it happened | A record of an event cannot precede the event (HubSpot and Salesforce refuse a logged meeting in the future; field-service apps cannot complete a job before it starts). The rule is a tenant setting, on by default. M3 governs how it shows: the date picker stops at today, the supporting text says why, the action that is not available yet is replaced by the date it becomes available, never a dialog, and the server checks it again | `reportLocked` / `visitTimeInFuture`; Pengaturan → Aktivitas "Laporan hanya setelah kunjungan"; the detail page's report button reads "Laporan bisa diisi mulai Sen, 21 Sep" until 00.00 of the scheduled day; the form's date field has `max` today and the send button waits; draft save and submit refuse on the server |
| Undo a send, never delete it | A sent record can be taken back by whoever may edit it, with a reason: it returns to draft with its content, the thing it completed is reopened, the sent version is archived, the people who were told of the send are told of the undo (HubSpot and Salesforce let an owner or admin remove a logged activity with an audit trail; field-service apps let a supervisor reopen a completed job). Deleting is then the ordinary draft path with its confirmation. What crossed into another system stays there and the copy says so | `withdrawVisitReport`: "Tarik kembali" next to "Ubah laporan", the same `canEditSubmittedReport` verdict; the mission returns to the status the send took it from (`statusBeforeCompletion`); RESULT_WITHDRAWN notification; a pushed lead stays in LeadEngine |
| Facts as property rows | A record's facts are rows, not columns: leading icon, label in the muted ink, value in the regular body weight, each row as tall as its own value (M3 list item; Linear and Notion properties; HubSpot's About panel; Google Calendar's event detail). Columns make the tallest value stretch every neighbour. Labels are sentence-case label text, not tracked capitals (Material 3 retired "overline"); weight marks hierarchy once per card, in the title | `Fact` on the activity page: a `dl` of `icon · label · value` rows, label 7rem from sm, stacked on a phone; Tujuan kunjungan is one of the rows |
| Whose calendar | A shared calendar answers "what is the team doing" by default and "what am I doing" or "what is one person carrying" on request (Google Calendar's calendar list, Outlook's group calendars). The switch is the same filter-chip row and people facet the activity list uses, not a new control, and the choice is remembered per person; the month and the day are a place, not a view, and are never restored. No colour per person: with a dozen reps the colours stop meaning anything, and the day pane names the person | `CalendarFilter`: Semua / Saya chips + Sales facet on `sales=` with the "me" placeholder; cookie `sa-view-calendar` holds only `sales`; counts, chips and the day pane follow the filter |
| Dashboard widgets | A summary is a grid of cards, each answering one question; the reader arranges them, hides what they never read, and the arrangement is theirs on every device. Arranging is a mode entered from the toolbar's right side; handles and per-card menus appear only in it; the filters above apply to every card (Geckoboard, Grafana, HubSpot dashboards; M3 cards, menus, segmented button) | Ringkasan: period segmented + Sales facet in the URL, remembered as `sa-view-ringkasan` (the daily card's `day` is a place and never remembered); "Atur widget" → `DashboardEditor` over react-grid-layout (twelve columns, 40px rows, vertical compaction), `sales_mission.user_dashboards` with self-only RLS, `WidgetShell` menu in edit mode |
| Custom widgets | A person composes a card from what the platform already measures: one measure (or two to compare), one grouping, an optional split, and a chart shape that fits the grouping; the form offers only combinations that mean something, and the built-in cards are instances of the same engine (Metabase's question builder, HubSpot custom reports) | `fn_report_cube` (measure × group × series, WIB days, row security), `APPLICABILITY` + `validateWidget` in `lib/reporting/cube.ts`, `WidgetConfigurator`, up to 12 per person |
| Chart colours are tokens | Series colours come from `--chart-1..6`, derived from the brand and its semantic inks and each cleared at 3:1 on the card (WCAG 1.4.11); a measure keeps one colour on every card; interest kinds have fixed colours; "Lainnya" and the unset are the muted ink. Animations are off: a card is read, not watched | `widget-view.ts` assigns colours; recharts with `isAnimationActive={false}` and `accessibilityLayer` |
| Resizing a widget | A card is dragged by its title and resized from its corner in grid units, never below the smallest box it still reads in (Android home-screen widgets, Grafana). The board stays smooth because the page does not re-render while a card moves: positions are committed when the pointer is released, each card body is memoised, and chart animation is off. The default board fills whole rows so the bottom edge is level | `DashboardGrid` (`onDragStop`/`onResizeStop` commit, `onLayoutChange` only for compaction), `MIN_BOX` per `minSizeFor`, `clampBox` on read; the six defaults total 72 cells, six full rows |
| Names on an axis | A category axis of names never rotates its labels; when the names would collide the chart turns, so each name has a row to itself on the left, cut with an ellipsis past sixteen characters and given in full in the tooltip (Material data visualisation guidelines, Datawrapper, Apple HIG charts). Dates keep standing bars and thin their ticks instead | `DayBars horizontal` for any non-time grouping; `NameTick` truncates; time axes use `preserveStartEnd` |
| Chart shapes on offer | The form offers every shape that answers the question and puts the preferred one first: comparison → bars (standing for dates, lying for names, either on request), change over time → lines or area, part of a whole → donut or pie with one measure, one value → a number, or a number with the period's daily trend under it; a split adds stacking; two measures never become two pies (Material data visualisation guidelines; Metabase's chart picker). The cards speak the product's words: prospek, aktivitas, laporan | `chartsFor` in `cube.ts` with `CHART_LABELS`; `MEASURE_LABELS` |
| Bar list, not a meter | One measure over names is a bar list rather than an axis chart: label, value and share on one line with the bar under it, because the numbers read without a tooltip and a phone fits a dozen rows (Google Analytics 4, Plausible, Vercel Analytics; Material data visualisation on labelling values directly). The bar must still be a bar: 8dp tall, 2dp corners (4dp on 8dp is a pill), no track behind it (a full-width track is the linear progress indicator's shape and says "of 100%"), its length relative to the longest row, and its colour the measure's token so the card matches the axis chart of the same measure beside it. The top twelve rows stay, the rest fold into one muted "Lainnya (N …)" row so nothing is silently dropped, shares are of the whole total (never of the rows shown), "Lihat semua" opens every row in a bottom sheet (a dialog on a desk), and a row that a list can answer is a link into that list narrowed to the row and the period, marked with a trailing chevron because a phone has no hover (M3 list item with trailing icon; GA4 "View report", Plausible's detail modal). Which rows link is a rule, not a per-card switch: every grouping a list has a facet for (industri, jenis, sales, klien on Aktivitas; sales, klien, hasil, minat on Laporan, with peluang or lead pinned for those measures), never prospects (no period facet) and never a time grouping; the widget form states the rule under Bentuk instead of offering a toggle (GA4, Plausible, HubSpot link every answerable row; Metabase's per-card click behaviour is for authors choosing among destinations, which this board does not have). Same tab, always: this is navigation inside the app, Kembali returns to the board as it was, and on an iPhone running the app from the home screen a new tab would leave for Safari (M3 navigation, WCAG 3.2.5; GA4, HubSpot). The row is a real link, so Cmd/Ctrl+click and a long press still give a new tab when the person asks for one, the same convention as `row-link.ts` on the tables | `ListBars` / `BarRow` in `reports/ringkasan/charts/plain.tsx`; `presentCube` builds `rows`, `all`, `total`, `drill` in `lib/reporting/widget-view.ts` (`TOP_ROWS`, `listDrill`); `listDrill` / `drillParam` in `cube.ts` decide the list and the facet value (outcome and interest kinds → the tenant's codes via `LabelContext.choices`), `rowHref` builds the link with `date=custom`; `WidgetConfigurator` shows the rule under Bentuk |
| Peers side by side, not behind a tab | Two options a person must know about before choosing (a personal feed and a team feed) sit as two cards side by side on a wide window, each with its own state and action; the shared instructions come once below. Tabs would hide one peer, and a narrow column with an empty right half ignores the width class (M3 canonical layouts: expanded windows use two panes; tabs are for related content not needed together) | Kalender saya: `FeedSetup` × 2 in `lg:grid-cols-2`, a dashed placeholder card when the role has no team reach, `SubscribeSteps` in three columns under both |
| A default the admin sets by example | The unit's default board is not a separate editor: an admin arranges their own board, then publishes it as the default from the same menu (Grafana's "save as default", Notion's template from a page). A person's own arrangement always wins; reset returns to the unit's default, then to the built-ins | `publishDashboardDefault` copies the caller's `user_dashboards` row into `sales_mission.company_dashboards`; the page reads own ?? company ?? built-ins; the menu item is gated by Pengaturan → ubah |
| A public window, not a public app | Someone who only needs to look does not need an account: a secret, revocable link opens the very same component read-only, with every viewer-shaped thing removed — no "mine", no remembered view, no links into records, no buttons (Google Calendar's secret address, Calendly and Notion public pages). What the link opens and whether it shows client names are bound to the token; the month, the day and the filter stay in the URL, so a filtered link is shareable as it is | `board_tokens.kind` ('screen' | 'calendar') refused by the other route; `/jadwal/[token]` renders `MonthGrid` and `DayPane`, the same two components as `/workspace/calendar`, without `itemHref` or `action` |
| Pressing send always does something | A submit button is never greyed out because the form is incomplete: a disabled control cannot take focus, so it can never tell anyone why it is disabled, and the reader is left hunting. The press is always allowed and it lands the person on the first thing standing in the way, in page order — which is what the browser's own required check already does on a form of plain inputs, so a form of controlled fields writes the same jump by hand rather than inventing a second behaviour. A blocker that is not an empty field (a visit dated later than today) is named the same way and jumps to the same place. Only work already in flight disables the button (WCAG 3.3.1 Error Identification and 3.3.2 Labels or Instructions; M3 keeps error text in the supporting-text slot, and has nothing to say about greying a button) | Activity and prospect forms lean on native `required`; the report form's `blockers` array and `jumpTo`, with "Belum lengkap" naming every field at once above the button |
