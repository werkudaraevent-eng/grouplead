# Design direction, Sales Activity

Written down because the direction was previously only in someone's head, which
is how a form ends up with a wall of checkboxes and nobody able to say whether
that was a decision or an accident.

## Design Read

A field-operations tool for sales reps and the appointment team. Most screens
are read standing up on a phone with one hand, or worked through at a desk on a
wide monitor. Nothing here is marketing: every screen either answers "where am I
going next" or captures what happened.

**Dial: ENERGY 1 / RHYTHM 1 / MOTION 1.**

Calm, uniform, and near-static on purpose. A rep checking an address outside a
client's office is not an audience for personality, and motion on a slow field
connection reads as lag. Variation earns its place only where the content
genuinely differs (the live board is loud because it is read across a room).

## Where the rules come from

**Material Design 3 is the rulebook, not the skin.** We take its logic for
structure, spacing, hierarchy and component behaviour. We take none of its
palette, none of its shape system, and none of its component visuals. Swapping
our logo out must not leave something that looks like a Google product.

What we actually adopted:

| Concern | Rule taken | How it lands here |
|---|---|---|
| Touch target | 48dp minimum for anything tappable | Form controls and list rows are 48px. Where a control is drawn smaller for density (a chip's remove button at 28px), the hit area is expanded to 48px instead of inflating the control |
| Spacing | 4dp grid | Label to control 8px, field to field 20px, section to section 24px |
| Text fields | Label above, control, supporting text below; error text takes the supporting text's place | `FieldShell` in the mission form |
| Multi-value input | Input chips, each carrying its own remove affordance | Supporting sales |
| Menu vs search | A list short enough to read needs no search box | Search appears from 7 options up |
| Buttons | Filled is the primary action and sits at the trailing edge; secondary is outlined | Simpan filled, Batal outlined. On a phone the primary sits lowest, nearest the thumb |
| Surfaces | Three tones, each one step off the last: page, card, field. A card groups one subject; a divider separates a card's header from its body | Page `--background`, section cards `--card`, fields `--field`. Each form section is its own card |
| Text field outline | The outline is held to 3:1 against the surface it sits on (M3 `outline` role, WCAG 1.4.11) | `--input` is #7F8993: 3.56:1 on the card, 3.28:1 on the field fill. Focus swaps it for `--ring` |
| Status vs action | A status is a passive label; a thing the reader can do is a button. They never share a shape | Status is a colour dot plus sentence-case text, no container. Anything actionable (Terima, Join) is a real button in the Aksi column or on the card |
| Card header: title, status, then at most two actions | A card carries one or two actions; anything more goes behind ⋮ (M3 cards; Gmail, Drive and HubSpot mobile). The status is a label, so it sits with the title, not in the action row competing with buttons. On a desk the actions sit to the right of the title; on a phone they drop under it as one line, and the second visible action joins the overflow so the line never wraps into a stack that squeezes the title. Rare decisions (asking for clarification, taking a report back) are always in the overflow; their dialogs are unchanged, only the opener moved | Report card on the activity page: `ReportActions` (`report-actions.tsx`) with `ShareReportButton` and, on a desk, Ubah laporan visible; Minta klarifikasi and Tarik kembali in ⋮ via the controllable `RequestClarificationButton` / `WithdrawReportButton` (`open`, `onOpenChange`, `trigger={false}`); header `flex-col gap-3 sm:flex-row sm:items-center sm:justify-between`, `StatusBadge` inside the `h2` |
| A record opens on its highlights, then reads in bands | A CRM record page (HubSpot, Salesforce Lightning, Pipedrive) opens on a strip of the four or so facts a reader wants before the prose, then continues in sections with a heading each; it never spreads label-value pairs across the full width of a wide screen, where two facts end up a thousand pixels apart. The report card follows that: a highlights strip (Hasil, Tingkat minat, Peluang with its value, Waktu kunjungan) as tonal tiles, two per row on a phone and four on a desk, a value allowed two lines before it is clamped because a truncated fact is no fact; then Tindak lanjut with its button beside the heading, not at the far edge; then what was said (summary at a 72-character measure, needs, products, competitor, custom answers); then who was met; then attachments; then the CRM line. Bands are separated by a hairline, headings stay label-medium. Every field the report form asks is shown, empty ones left out except Peluang, which stays in the strip. On an expanded window (lg and up) the bands below the strip split into a body and a supporting pane, per the M3 canonical layouts: the body keeps Tindak lanjut and what was said, the prose still at its measure; the pane, 360dp wide and separated by a hairline (M3 puts a supporting pane at 360dp beside a fluid body), holds who was met, attachments and the CRM line, so a wide screen is used rather than left half empty (HubSpot's right sidebar cards, Salesforce's related lists). The pane exists only when it has content, and compact and medium windows keep the single stacked column in the same order; photo columns follow the gallery's own width (container query), three in the pane and six across a full card | Activity page report card: `Highlight` tiles, `FollowUpPanel` heading row, the `border-t pt-5` bands; `productInterest`, `opportunityExists` / `estimatedValue`, `competitorMentioned` and the next-action owner now rendered |
| An empty optional field is unanswered, not a No | A form field that nobody was required to fill carries no answer when it is empty; showing it as a negative claim turns an omission into a fact. The Ada peluang checkbox is optional, and it is also the gate to Kirim ke LeadEngine, so an empty box read as "Tidak ada" contradicts a Hot interest level in the tile beside it and gets misread as "no opportunity". Material's guidance on empty states and every CRM reference (HubSpot, Salesforce) render an unset field as unknown ("—", "Not set"), never as the false branch. So the Peluang tile and the {peluang} share variable read Belum ditandai when the box is empty, with the tile's hint saying the box was not ticked in the report; Ada with the value when it is. The same rule applies to any optional boolean added later: unset renders as unset | Activity page `Highlight` for Peluang; `reportShareValues().peluang` in `lib/missions/report-share.ts`; placeholder legend in `REPORT_SHARE_PLACEHOLDERS` |
| Icons | Material Symbols (M3's own set), Rounded style, weight 400, drawn on the 24dp grid; an icon never carries meaning a label does not also carry | Generated into `components/icons` from `scripts/icons/map.json` by `npm run icons:generate` at the repo root. Export names are the ones the code already used, so a swap of the set is one map edit, not 200 file edits. Brand marks (four social icons) are not in Material Symbols and keep their previous stroke drawings |
| Records list, dashboard summarises, tabs switch between them | A record type gets a list view of its own (filter, sort, open in context) separate from the analytics over it; the two are sibling views of one destination, so they are M3 primary tabs under one page title, not two navigation items (Salesforce list views vs dashboards, Pipedrive Activities vs Insights) | Laporan: tab Daftar (`/workspace/reports`) and Ringkasan (`/workspace/reports/ringkasan`); a row opens the mission at its report card; the mission list offers "Lihat laporan" as the outlined action on a reported row |
| Chips are M3 chips, not pills | At least 32dp tall (a label a phone must wrap grows the chip rather than spilling past its border; labels are written to fit one line), 8dp corners, outlined at rest, tonal (secondary container) with a leading check when selected, 48dp touch target from spacing; the full-round pill is for buttons and segmented buttons only, and the primary fill is for the one main action, never for a chosen answer | `ChoiceChip` / `ChipRow` on the report form and the mission filters, `ViewChip` for saved views; `--tonal` / `--tonal-foreground` tokens shared with the navigation indicator |
| Photos: camera first, thumbnails, one control per photo | A photo field starts with an outlined button that opens the camera on a phone and the picker elsewhere, a second button for the gallery; each photo is a thumbnail with its own remove control; a linear progress bar per upload; errors as supporting text; drag-and-drop only as a desktop extra; files downscaled on the device (Jobber, Zoho FSM; Google Forms file upload limits) | `PhotoField` on all three forms, `PhotoGallery` on detail pages; private bucket per company folder, signed URLs, files purged with the rows |
| Recordings: the phone records, the app keeps | An audio field never opens a microphone. Safari on iOS stops capture the moment the screen locks, a call comes in or the person switches apps, and a 40-minute meeting cannot be retaken; the phone's own recorder (Memo Suara) survives all of that. So the field is one outlined "Unggah rekaman" button that opens the picker, a row per recording with its length, size, the native player and its own remove control, a linear progress bar per upload that reports real bytes (a 30 MB file on mobile data deserves more than a spinner) with a cancel, errors as supporting text. The helper carries the consent reminder ("Beri tahu klien bahwa pertemuan direkam") and the admin may reword it. The supporting text names the rule before anyone picks: count, formats (M4A, AAC, MP3), size, in that order (M3 supporting text conveys requirements up front and the error replaces it; Google Forms' "Upload 1 supported file: PDF or image. Max 10 MB"). WAV is refused at pick time with the recorder setting to change, since uncompressed audio (10 MB a minute) never fits a meeting into the cap and a size error after the upload would come too late. The detail page lists recordings with the native player and a download that keeps the file's own name, because the next stop is a coaching tool that wants the file. Same access as photos: the unit's folder, signed URLs, purged with the rows (HubSpot and Salesforce attach call recordings to the record; Jobber's attachments) | `AudioField` on all three forms, `AudioList` on detail pages; `lib/audio/*` mirrors `lib/photos/*`; `sales_mission_audio` bucket under the same folder policy; `visit_audio` core field in the report's Lampiran card |
| Dialog: headline and actions stay, content scrolls | A dialog never grows past the window; only the region between the headline and the action row scrolls, with a divider at each end while there is more, and a thin scrollbar inside the rounded surface | `DialogBody` in the shared dialog: `-mx-6 px-6 min-h-0 flex-1 overflow-y-auto`, dividers appear only when scrollable; every form dialog wraps its fields in it |
| Editable after send, never silently | A sent record can be changed by its author inside a window and by an admin at any time; every change asks a reason and keeps the outgoing version (Salesforce field history, HubSpot activity edits, Google Forms "allow editing after submit"). Sending a record back with a note is the admin's other move | Laporan kunjungan: "Ubah laporan" with an inline reason, the window set in Aturan mission, "Minta klarifikasi" dialog for admins, and "Diubah N×" with the version list on the card |
| A closed visit keeps its facts, freezes its outcome | Once the report is sent the visit is history, and until now every field of it was locked: fixing a misspelt appointment name meant withdrawing the report, which cancels its follow-up and leaves a "Laporan ditarik kembali" trail, then sending it again. That is the wrong tool. HubSpot and Salesforce lock a closed deal's outcome, not its description: the contact, address and notes of a closed record stay editable and every change is logged. So a completed visit still offers Ubah aktivitas, scoped to its descriptive facts (appointment contact, address, industry, objective, custom answers) with the schedule and the team shown read-only and refused server-side if changed; the page says so in its description; a cancelled visit stays closed. Who was actually met is the report's Ketemu siapa, edited through Ubah laporan under its own window, and the plan's contact on the activity is a separate fact | `canEditDetails` in `missionGates` (any status but CANCELLED, same scope as `canEdit`); `updateMission` refuses schedule and team changes on a completed mission; the edit page's `frozen` mode renders `FrozenLine` for the team and the read-only schedule; `offersEdit` on the activity page |
| Page headers | One header for every page, the same in both apps, rule for rule. A 56dp row, level with the drawer's header, holds the title (20px, semibold, sentence case) and the page's actions centred on it; nothing sits above the title but, on a derived page, its parent. The drawer's header is that same 56dp, exactly, when the drawer is open (its collapse button fills the row's height rather than adding its own 56dp to the row's padding, which made it 72dp and set the page's title and buttons 8dp above the drawer's logo), so the two headers share one line and one bottom edge. **Top-level pages** (every item in the drawer, and the pages the account menu opens) carry no eyebrow: the drawer's active item and the title already say where you are, and the product's name is already in the drawer. **Derived pages** (a page under Settings, a record, a form that creates or edits one) carry their parent as one small line above the title inside the same row, in sentence-case label-medium (12px, medium, muted): "Pengaturan", "Pengaturan / AI", "Aktivitas"; "Settings", "Settings / AI". In LeadEngine, which has no Back button, the parent is a link; in Sales Activity, whose derived pages have "Kembali", it is text. **The description** under the row follows what it says: a sentence that teaches (what the page is for, how it works) shows with a ✕ right after its last word, never parked at a far column edge where it reads as belonging to nothing (the box hugs its text, up to the reading width), until the person closes it and then stays closed for them on every device, per page (`page-intro-<page>`; lists keep `list-intro-<list>`); a line that states facts (today's date, a record's type and place, its contact, "Updated 2m ago") is information and always shows. The title keeps its size when the page scrolls. (M3 top app bar: one title slot with its actions, the same bar in the same place on every screen; M3's type scale dropped M2's all-caps overline, so a location line is label-medium in sentence case; Linear, Notion, GitHub and Stripe put no product name above a page title, and Stripe and Vercel show a parent only on a page below it.) Two headers in one product, a 56dp row on the lists beside a taller block of eyebrow, title and description everywhere else, made the title jump on every move between drawer items. | `WorkspacePage` in `app/workspace/workspace-page.tsx`, one header for every page: from `lg` the `lg:min-h-14 lg:py-1.5` row with the parent line (`eyebrow`, `text-xs font-medium`, derived pages only: "Pengaturan" on every settings page, "Pengaturan / AI" on Pemakaian AI, "Aktivitas" on an activity and its new and edit forms, "Laporan kunjungan" on the report form, "Prospek" on a prospect and its forms) and the `text-xl font-semibold` title stacked on its left and the actions `items-center` on its right; the description under the row through `introKey` (`PageIntro` in `components/page-intro.tsx`; keys from `pageIntroKey` and `INTRO_PAGES` in `lib/hints/hint-key.ts`, `listIntroKey` for the three lists; written to `sales_mission.user_hints`, no migration) or, without a key, as a plain line (Hari ini's date, an activity's type · industry · location, a prospect's contact, the new-activity line that names the prospect or the visit being scheduled again). No eyebrow on Hari ini, Aktivitas, Prospek, Laporan (and its Ringkasan and Insight tabs), Kalender, Papan live, Pengaturan, Notifikasi, Panduan, Pasang, Yang baru or Kalender saya. On a phone the title is `PageChrome`'s; a page other than a list reads its description before its actions (`max-lg:order-first`). `app/workspace/loading.tsx` draws the same row |
| List pages | One shape for every list, the same in both apps (Sales Activity's Aktivitas, Prospek and Laporan; LeadEngine's Contacts and Companies), rule for rule. **Header**: one compact row, level with the drawer's 56dp header: the title at 20px and, centred on the same line, the one primary action filled at the trailing edge with the secondary actions (Export with the number of matches it will write, Import, where the list has them) as labelled outlined buttons beside it; no eyebrow or breadcrumb above it (the drawer's active item and the title already say where you are) and no padding beyond the row's own, because every pixel above the table is a row of records fewer (M3 top app bar: one row, the title and its actions centred on it). The description under the row only teaches (what the list holds, whose rows you see), so it shows with a ✕ icon button at its end (tooltip "Tutup" / "Dismiss") until the person closes it, and then stays closed for that person on every device, per list (M3: supporting text that has done its job should not cost every visit; Google Workspace feature discovery; Sales Activity's coach marks, "once per account, on every device"). An ⓘ beside the title was the other way and was not taken: it hides the sentence from exactly the newcomer who does not yet know to press it, and leaves a control in the title row for text a returning person never reads again. On a phone the title is in the top app bar (Sales Activity) or shares the row with the actions (LeadEngine), and the description is not shown. **Toolbar**: the search field (outlined, 8dp corners, leading icon, trailing clear, debounced), then the filter chips, "+ Filter" and "Clear all", in one group that wraps onto a second line when there is more than fits, so an applied filter is always in sight (M3 sets chips on a wide screen that way; scrolling is for a narrow one); "Clear all" clears the search too. **Saved views** are named URLs, one person's own, per list: M3 choice chips (8dp corners, outlined at rest, tonal with a leading check when chosen, a star on the default) in a row above the toolbar, shown only once a view exists; a chip is marked only when its view is exactly what the screen shows (search, filters, sort, page size, columns); choosing one writes its query and size into the URL and its columns into the columns menu. "Save view" appears once the list differs from how it first opens, "Save changes" once the view last chosen has been changed, and ⋮ beside the chips saves a copy, renames, sets the default and deletes; deleting a person's own shortcut asks nothing and the snackbar undoes it. **View tools** (save view, columns) are 40dp icon buttons named by their tooltip, at the trailing edge of the toolbar's first line. The columns button carries no badge (an M3 badge flags something new or waiting, not a setting): "N of M shown" is said inside the menu, where each column is a checkbox with a drag handle, with Reset (M3 menu with checkboxes; Gmail, HubSpot, Airtable); the choice is kept per list in the browser and travels in a saved view. **Table** (from `md`): the list fills the window under its toolbar and scrolls inside its card in both directions, so the header row stays at the card's top, the footer at its foot and the sideways scrollbar at its visible bottom edge rather than after the last row; the header row is opaque and sits over every body cell, frozen ones included, and its own frozen cells over the header cells that scroll under them (M3 data table; Airtable, HubSpot, Sheets); the selection box and the record's name frozen at the leading edge with one edge shadow, the row's own action frozen at the trailing edge, as wide as its widest button and carrying the mirror of the leading edge (the hairline and the soft shadow on its left), so a column scrolling under it reads as passing beneath rather than as a word cut off ("Indu", "Mec"), the name taking whatever width the other columns leave, the columns between them scrolling sideways inside the card, never the page; 52dp rows with one hairline between them; one line per cell, cut with an ellipsis rather than wrapped, the whole value in the cell's title; sentence-case headers at the body size in medium weight, carrying the label and the sort arrow and nothing else: the column that holds the default order shows its arrow muted and names the order in its tooltip, never in a caption beside the label; a header click steps ascending, descending, then back to the default order. **Footer**: rows per page 25, 50 or 100, the range with the total, previous and next; no page numbers. **State**: the view is the URL, replaced rather than pushed; a new search, filter or sort keeps the sort and the size and returns to the first page; a cookie per list remembers the last query (without page or size) and a bare open redirects to it; a default saved view chooses the view only on the list's first open in a browser, when nothing is remembered, so a link or a remembered view is never overridden. **Phone** (below `md`): the list opens on its records: one card per record, search first, the filter chips behind one "Filter" button carrying the count, which opens a bottom sheet with the saved views to choose from at its top; what is applied repeats under the search as sideways-scrolling input chips; saving a view and choosing columns are desk work (M3 top app bar, search, chips, menu with checkboxes, data table and pagination; Gmail, Linear, HubSpot, Airtable, Notion) Scrollbars are the thin, button-less kind in every browser (the standard properties apply only where the -webkit- ones do not, so Chromium on Windows draws no arrows into a rounded corner), and the table's vertical bar starts under its sticky header. The trailing action column's header is left-aligned like its buttons (M3: a header takes its column's alignment; only numbers go right). The paging footer is one slim 48px row and the page ends 16px under it. A menu never runs past the window (M3 menus): the columns menu caps its height at the room around its trigger with a 16px margin, keeps its title and reset in place, and scrolls only its list. | `WorkspacePage` (the header every page has, see Page headers, with the description under it through `introKey`: `PageIntro` in `components/page-intro.tsx` on `useHintSeen` / `useDismissHint` from the coach marks' store, `sales_mission.user_hints` with localStorage as the fallback, keys from `listIntroKey` in `lib/hints/hint-key.ts` (`list-intro-activities`, `list-intro-prospects`, `list-intro-reports`), no migration; `phoneAction={false}` with each list's `…PhoneMenu` for the ⋮; `fill`, which from `md` makes `#page-scroll` a column ending at the window's foot, its rows `shrink-0`, stopping above the FAB between `md` and `lg`, and keeping its own scroller only for a window too short for the card's 240px minimum); `FilterBarFrame` (on a desk the search, facets, "+ Filter" and "Bersihkan semua" in one wrapping group, `SavedViewsBar` above it, `ViewTools` at its trailing edge; on a phone the `BottomSheet` with `SavedViewsSheetSection` at its top) under `MissionFilterBar`, `ProspectFilterBar` and `ReportFilterBar`; `ListViewProvider` in `components/list-view/list-view-provider.tsx` (the URL, the columns and the saved views of one list, shared by the bar and the table; `useDrawnColumns`), `ViewChip` and the ⋮ (Simpan sebagai tampilan baru, Ubah nama, Jadikan bawaan / Bukan bawaan lagi, Hapus with "Batalkan" in the snackbar) in `saved-views-bar.tsx`, `ViewNameDialog`, `SaveViewButton` ("Simpan tampilan", only once the table exists) and `ColumnsMenu` ("Kolom", "N dari M tampil", "Susunan awal"; the name column listed first and locked; the handle also moves a column with the arrow keys) in `view-tools.tsx`; `lib/lists/list-columns.ts` (`mergeColumnState`, `visibleColumns`, `moveColumn`), `lib/lists/list-column-specs.ts`, `lib/lists/list-views.ts` (`viewConfigKey`, `markedView`, `defaultViewToApply`, `viewHref`, the zod schemas), tests beside them; `listSavedViews` in `lib/lists/list-view-queries.ts` (no views and no save control before the migration, never an error); `createListView`, `updateListView`, `setListViewDefault`, `deleteListView` in `app/actions/list-view-actions.ts`; `sales_mission.list_views` (migration `20260924090000_sales_mission_list_views.sql`: own rows in the person's own unit, one default per person, unit and list, names unique per list); the columns in localStorage `sa-list-columns-<list>` through `useStoredValue`; `ListTableFrame` (the card `flex-1 min-h-60`, its scroller `min-h-0 flex-1 overflow-auto isolate`, the footer under it), `frozen`, `CellBox`, `SelectBox`, `LIST_CELL`, `CellText` in `components/list-table.tsx`, the sticky `TableHeader` (`z-30`, `bg-muted`) over the frozen cells' `z-10`, and `.list-table` in globals.css (`[data-frozen-edge]` on the name, `[data-frozen-trailing]` its mirror on Aksi, through `edgeProps`); `SortHeader` (the header cell itself, carrying `aria-sort`; a muted arrow on the default column, `sortTitle` in `lib/lists/sort-title.ts` for its tooltip, the order also read out to a screen reader); `MissionPagination`; `RememberView`, `openListView` and `resolveRememberedView` (`fresh`) in `lib/view-cookies.ts` |
| Data table column widths are decided, not discovered, except the action's | Every data column has a width sized to what its cell holds, and that width is its floor: a longer value is cut inside the column, never widens it, and a header wider than the column does widen it, so a label is never cut. The action column is the one exception, as wide as the widest button on the page (M3: a column is as wide as its content), because what it holds is one of a few known buttons and a width set for the longest (Lanjutkan laporan, Catat follow-up with its ⋮) left an empty band on every page of Join buttons, about a hundred pixels the name could use. The name column has no width: it takes everything the others leave, down to its minimum, so a wide window never shows an empty band between the last column and Aksi; below the sum the table scrolls sideways inside its card under the frozen name instead of squeezing or dropping columns by breakpoint. Which columns show is the person's, from the columns menu; each list's defaults fit a 1280px laptop with the drawer open (about 984px of table once the drawer, the gutters, the card's border and the table's own scrollbar are taken) with the buttons most rows carry, and a 1366px laptop whatever a row carries, and the rest wait in the menu. What used to sit on a second line under a cell is a column of its own and, while that column is hidden, the title of the cell it came from, so a one-line row loses nothing (Gmail, HubSpot and Salesforce list views: fixed widths, the name widest, the rest chosen and scrolled; LeadEngine's Contacts). The `xl`/`2xl` hiding and the `table-fixed min-w-[960px]` squeeze this replaces took the name's width on a laptop and hid Lokasi and Dibuat with no way to ask for them; the fixed 180px Aksi that followed it gave the name's width to empty space and was still narrower than Catat follow-up with its ⋮, which spilled over the column beside it | `lib/lists/list-column-specs.ts`. Aktivitas: Aktivitas locked (its title adds Jenis); Jenis, Jadwal, Sales utama, Status shown; Lokasi, Hasil, Penugasan (join position and the team's answers, once the status's second and third lines), Industri, Dibuat oleh on request; the status's title keeps the lifecycle, the outcome and the answers; Aksi frozen at the trailing edge, as wide as its widest button (Join 91px to Lanjutkan laporan 193px). Prospek: Perusahaan locked (its title adds industry and city); Kontak (132; title: job, phone, email), Status (title: the contact history), Hubungi lagi (warning ink when due; holds the default order, "jatuh tempo dulu", which its tooltip names), Pemegang shown; Telepon, Email, Jabatan, Terakhir dihubungi, Industri, Lokasi, Dibuat on request; Aksi frozen, as wide as its widest button (Hubungi with ⋮ 187px, Catat follow-up with ⋮ 211px). Laporan: Perusahaan locked (title: kind and time of the visit); Waktu kunjungan, Sales utama, Hasil (title: interest), Next action (warning ink when its follow-up is late), Status (title: sent or changed, lead) shown; Jenis, Minat, Peluang, Tindak lanjut, Lead on request; no action column. `ListTableFrame` lays the table out from its content (automatic table layout): a `colgroup` with each data column's width, none for the name and 1px for Aksi, which its buttons widen; `CellBox` gives each body cell its column's floor and nothing more (a `minmax(0,1fr)` grid, so the content's own width drops out of the measure), `SelectBox` holds the selection column at exactly 44px, which the frozen name's offset needs. `ACTION_COLUMN_WIDTHS` (measured, usual and widest) and `tableMinWidth` serve the budget test in `lib/lists/list-columns.test.ts`; the layout reads neither. The phone shows cards |
| Bulk entry beside single entry | A list that people build from a spreadsheet gets a paste-a-list path next to the one-at-a-time path, one item per line (Google Forms, HubSpot, Salesforce picklists); a multi-line paste into the single field is read as a list too | Editor opsi: "Tempel daftar" with a preview count and duplicates skipped; "Hapus semua" clears the draft with an inline Batalkan instead of a dialog, because nothing is written until Simpan |
| Carry known facts forward | A record made from an earlier step starts with that step's facts filled in and marked with their origin; the user confirms or replaces, never retypes (Salesforce event → contact, HubSpot meeting attendees) | Laporan kunjungan: the appointment contact is Kontak 1 with a "Dari janji temu" mark, removable, and offered back with one button when absent |
| A reading is a badge, signed, behind a switch | A judgement about a person (how they communicate) is shown as a small tonal badge beside the name with its meaning one step away, never as a filled status chip and never as a fact: it carries who read it and when, and the words are the training's own so every rep reads the same advice (Crystal Knows and Humantic AI beside a HubSpot or Salesforce contact; M3 assist chip vs filter chip, supporting text). Recording it is two rows of M3 filter chips (one of four each; the second row disabled until the first is chosen and never offering the same letter), the approach line for what was chosen, and one free line; a reading carried in from an earlier visit is marked with its origin and a known reading for a freshly typed name is offered with one tap, not applied silently. The whole block exists only while the unit's switch is on, so a team that did not train on it never meets it, and it stays internal: never on a public page or a shared link, and inside the unit it travels with the record — the visit-report export carries it beside the person it is about, because an export is a read of the record by someone who may already read the record | `DiscBlock` in `VisitReportForm` (`ChoiceChip` rows Utama / Pendamping, `describeDisc`, note `Input`, “Dari kunjungan sebelumnya · dinilai oleh …”, the Pakai offer from `lastDiscForCompany`); the `DISC` badge and approach line under Ketemu siapa on the activity page; `contactDiscEnabled` SwitchRow under Laporan kunjungan in Pengaturan → Aktivitas; `lib/contacts/disc.ts` holds the letters and the training's words; LeadEngine shows the same badge as Communication style (DISC) on the contact page from `custom_fields.disc` |
| The call is the button; the log follows it | A prospect's action is "Hubungi", not "Catat …": reps call from WhatsApp outside the app and never came back to write it down, and "kontak" already means a person here. Hubungi opens a menu of the prospect's channels (HubSpot, Pipedrive, Close put Call / WhatsApp / Email on the record): WhatsApp with the unit's opening line filled in, which opens the app on a phone and WhatsApp Web or Desktop on a desk; Telepon, which dials on a phone and on a desk copies the number and says so, since most computers have nothing to dial with (Salesforce and Zoho without a dialer: show the number, log after); Email. "Catat follow-up" stays last, for what happened outside the app. Each channel remembers the follow-up it started, and when the tab is seen again the app asks "Bagaimana hasilnya?": on a phone a modal bottom sheet with a scrim, the same surface as the Hubungi menu, on a desk a centred dialog with the same scrim (six answers is not a snackbar; two cuts as a card, above the bar and at the desk's corner, sat on the same white as the prospect cards and rows and read as one more row, since in M3's light theme a floating surface is told apart by its container colour as much as its shadow, and a scrim leaves no doubt; HubSpot and Pipedrive ask "Log call?" in a modal), one tap for the outcomes that need nothing else, the full dialog prefilled for those that need a date or a reason, "Nanti" to put it away, and the note expires on its own (HubSpot's "Log this call?"). The button is tonal: important on every row, but not the screen's one filled action | `ContactMenu` (`ResponsiveMenu`, `Button variant="tonal"`), `follow-up-pending.ts` (sessionStorage, 30 min), `FollowUpPrompt` (visibilitychange / focus / a moment after, `BottomSheet` below md and `Dialog` above, `ChoiceChip` outcomes, `logProspectAttempt` with the suggested status), `DialogTarget.prefill`; `whatsapp-greeting.ts` renders the template (`{sapaan} {kontak} {sales} {perusahaan}`, default "{sapaan} {kontak}, saya {sales} dari {perusahaan}.") the admin edits in Pengaturan → Aktivitas → Prospek (`mission_settings.whatsapp_greeting`) |
| Share the report; do not post it | Teams post every visit to a WhatsApp group by hand, in a format that drifts between people. WhatsApp has no official way for a product to post into a group (the Business Platform is 1-to-1, templated and metered; group gateways are unofficial and get numbers banned), and Salesforce or HubSpot never try: they integrate Slack and Teams, which have channel APIs. The pattern for an app without one is the share sheet (Strava, Google Photos, Notion): "Bagikan ke WhatsApp" on a sent report composes the message from the report in the unit's template and opens the Web Share sheet with the text and the first photo; the rep picks the group and presses send. The template is the admin's, in Aturan aktivitas, with placeholders in the team's words, tap-to-insert chips and a live preview; a line whose placeholders all came back empty is dropped, so the message never says "Jabatan :" with nothing after it. The road is chosen by the device, not by what the browser offers: Chrome, Edge and Brave on Windows 11 offer Web Share too and open the Windows share sheet (Outlook contacts, "WhatsApp · Install"), which leads nowhere. Phone or tablet (iPad included, read from its touch points since Safari there calls itself a Mac): the share sheet with text and photo. Desk: the text is copied, the photo downloaded under a tidy name (client-date.jpg), and WhatsApp desktop is tried through its whatsapp:// link the way Zoom and Slack open their apps; a page cannot list installed apps, it can only try and watch, so the page losing focus within 2.5 s means the app opened, and nothing happening means WhatsApp Web opens instead with a toast that says why. Nothing may steal the window's focus before the probe (a spare tab opened early did, and read as the app opening), so WhatsApp Web is opened after the probe, still inside the click's activation window on Chromium browsers; the explanation and the "Buka WhatsApp Web" button are a banner in the report card, not a toast (see the snackbar row). The answer is remembered per browser and re-probed when it fails. Neither desk road carries the photo (both take text only), so it is dragged in, and nothing is recorded on the desk road since the app cannot know the message was sent. The photo is fetched when the button renders, because a share sheet or a download must happen inside the tap's activation window | `lib/missions/report-share.ts` (`renderReportShare`, `reportShareValues`, `REPORT_SHARE_PLACEHOLDERS`), `app/workspace/activities/[activityId]/share-report-button.tsx`, the "Laporan kunjungan" section of `mission-settings-form.tsx`, `mission_settings.report_share_template` |
| After send: the next step on the confirmation, never a dialog | A dialog is for a decision that must be answered before anything else (M3); an optional follow-up offered as a dialog is one more thing to close five times a day, and the "don't show again" switch it invites kills the feature silently. Gmail, Strava, Calendly and Typeform put the next action on the confirmation itself. So after Kirim laporan the author lands on the report with a success card ("Laporan terkirim. Bagikan ke grup WhatsApp sekarang?") whose one action is the share, and on a phone that share is the bottom bar's one next step (the same state rule as answer → report → join → edit). The offer ends when the share sheet completes, recorded on the report so the team sees "Dibagikan ke WhatsApp oleh … · 13.20" and nobody is asked twice, or when the author taps "Nanti saja", remembered per report on that device. Only the author is offered; the button in the card header stays for everyone. The unit's admin can turn the offer off for a team without a group | `ShareOfferCard` and `ShareOfferBar` in `share-offer.tsx` sharing one localStorage-backed store through `useSyncExternalStore`; `compactAction === "share"` on the activity page with the base action as the bar's fallback; `markReportShared` sets `visit_reports.whatsapp_shared_at/by` after `navigator.share` resolves; `mission_settings.report_share_prompt` as a switch in Aturan aktivitas → Laporan kunjungan |
| A next action is a task on the record, never a page of its own | The report always carried a next action (type, owner, day) and the KPI counted it "open", but nothing closed it and nowhere said what happened. CRMs (Salesforce, HubSpot, Pipedrive, Close) make the follow-up a task attached to the record with an owner, a due day and an outcome, surface it in the owner's "today", and close it with "log the activity, schedule the next" so a client is never left without a step (Pipedrive's rule). So: sending a report opens a follow-up; it appears in Hari ini for its owner (due or late first, undated after, since those would otherwise never surface); on the report card the chain reads as a timeline with the open one carrying "Catat tindak lanjut"; the dialog asks how (channel chips), how it went, when, a note, and, optionally by the unit's choice, the next step, which opens the next row chained to this one; an outcome of kind "dropped" ends the chain. The report list speaks for the tracked follow-up (Terbuka / Lewat / Selesai · outcome · day · n langkah). Editing the report moves only its own, still-open follow-up; withdrawing cancels the open ones and keeps the closed as history. Owner, author or a supervisor may act; a follow-up handed to someone else notifies them. The option lists are the admin's with locked kinds, in the same editor as the report's choices; a switch turns the module off A closed item's body never repeats its status label: a logged one shows the outcome, channel and note, a cancelled one only its reason, then who closed it and when (Linear and Jira show the resolution once, in the status) | `sales_mission.follow_ups` (chain via `parent_id`), `lib/missions/follow-ups.ts` (states, schemas), `follow-up-queries.ts`, `follow-up-sync.ts` (from submit and withdraw), `app/actions/follow-up-actions.ts`; `FollowUpPanel` on the activity page (`#tindak-lanjut`), "Tindak lanjut hari ini" on Hari ini, `FollowUp` cell in `report-table.tsx`; `follow_up_channel` / `follow_up_outcome` on `report_choices`, Pengaturan → Tindak lanjut; `mission_settings.follow_up_enabled` |
| Inbox before the record | A stage before the main object lives in its own list with passive statuses and a logged history, and converts into the main object rather than being edited into it (Pipedrive Leads Inbox, Salesforce Lead → Opportunity) | Prospek: statuses are dot + label, every contact attempt is a row, Confirmed is reached only by scheduling the visit, and a converted prospect shows its mission's fate |
| Admin-editable vocabulary over fixed semantics | Labels, colours and order are the team's; the kind the code acts on is locked at creation and shown with a lock and its reason | Pengaturan → Status prospek: rename, recolour, reorder, add, archive; never delete; one open and one won anchor always active |
| Destructive actions | Deleting is reversible by default: it moves to a bin, and only removing from the bin needs a confirming dialog that names the count. An admin restores; retention is 30 days | Mission list "Ke sampah", Pengaturan → Sampah with Pulihkan and Hapus permanen; the clear-all zone empties into the bin |
| Sign-in | Text fields 48dp, outlines at 3:1, the visibility toggle a focusable 40dp icon button with its label in the product's language, the filled button without a resting shadow, helper text at full contrast. Surfaces from the tokens (`--background`, `--field`, `--card`), never a literal white. On a phone the form starts near the top so the fields stay in view when the keyboard rises (Google, Microsoft, Slack sign-in); on a desk it sits at the panel's centre | Both apps' login pages; Sales Activity's `pt-10 lg:items-center` |
| A table row opens its record | The row is the link, with the name styled as one for the keyboard and a new tab; there is no "open" control at the end of the row. The old arrow (↗) was an icon button whose icon means "opens elsewhere", which it did not, and people did not find it; M3 asks an icon button's icon to be universally read, and Gmail, HubSpot, Linear and Pipedrive all open the record from the row. Clicks on controls, text selections and modifier keys are honoured, not swallowed. The people column shows the same faces as the phone's card (M3 list items lead with an avatar; HubSpot and Linear owner columns) | `useRowLink` on the Prospek, Mission and Laporan table rows, `cursor-pointer`, the name as a `Link` with `hover:underline`; `TeamFacepile` in Sales utama; the Aksi column holds only the row's own action (Join, Terima, Isi laporan, Hubungi), and the Laporan table has none, so it has no Aksi column: the Lihat button and the ↗ both opened the same page the row now opens |
| Rows needing attention | A list item that needs the reader is toned at its edge, not shouted at | 4px `--warning-foreground` left edge on the card, and on a table row's first cell (`data-attention` and `.list-table` in globals.css: a row's own shadow is painted under its cells' opaque backgrounds, which the frozen columns need); the demand itself is the button beside it |
| Labels | Sentence case. M3 dropped all-caps button and chip labels in 2021 | No uppercase, wide-tracked pills anywhere in a list, and table headers are sentence case at 13px medium like LeadEngine's (they were tracked capitals until the two apps' tables were aligned). Uppercase stays on the drawer's group labels and the Pengaturan list's section subheaders only; no page header has it (see Page headers) |
| Picking a time | Day first, then time; busy blocks drawn and named, never hidden; a clash warns, it does not block (Calendly's two-panel shape, Google Calendar's "find a time") | `SchedulePicker`: month on the left, the chosen day as an hour timeline on the right with each assignee's visits as named blocks, the travel buffer shaded around them, the candidate drawn on top in primary or danger |

## Izin

The permission matrix is the only place access is decided, and it must be
readable without the code. Three rules keep it that way:

- **Every record-bound action asks `canPerformOn`**, never a hard-coded seat.
  The grant (Lihat/Buat/Ubah/Hapus) and the reach (Cakupan) come from the same
  row; `lib/access/record-scope.ts` defines who owns what, and nothing else
  does. A supporting sales keeps participation rights (join, leave, answer,
  propose, note) because those are not ownership.
- **A refusal uses the matrix's words.** "Peran Anda hanya boleh mengubah
  mission miliknya sendiri" points the admin at the Cakupan control; it never
  says "hanya sales utama" when the rule is a scope.
- **"Admin" means one thing:** Ubah on Pengaturan mission (`isSettingsAdmin`),
  which opens the settings screens and the bin. It is not a record scope; a
  supervisor's reach over reports comes from Laporan kunjungan → Ubah with a
  Cakupan of Tim or Semua.

## Istilah

The product is **Sales Activity**; the record a person schedules, joins and
reports on is an **aktivitas**. The code, the routes' internals, the
`sales_mission` schema and the `sales_mission_*` module ids still say
"mission" on purpose: that layer changes in its own migration, and nothing
a person reads depends on it.

| On screen | Stays | Never |
|---|---|---|
| Sales Activity; aktivitas / Aktivitas ("Aktivitas baru", "Ubah aktivitas", "Buka aktivitas", "Tim aktivitas", "Jenis aktivitas"); Pengaturan aktivitas; Riwayat perubahan (the audit log, renamed so it does not collide) | kunjungan, laporan kunjungan, jadwal kunjungan (a visit is one kind of aktivitas); sales utama, sales pendukung; prospek; papan live; lead | "mission" in any sentence a person reads |

The product name lives in `lib/brand.ts`; every in-app URL is built by
`lib/paths.ts` (`/workspace/activities/…`), and `next.config.ts` redirects
the old `/workspace/missions/…` addresses. The phone's calendar subscription is **Kalender saya** on screen and `calendar_tokens` plus `/kalender/<token>/aktivitas.ics` underneath.

## What is ours, not Material's

- **Colour** comes from `app/globals.css` tokens only. Every pairing is held to
  WCAG AA against the surface it actually lands on, not against white by default.
- **Type** is Plus Jakarta Sans, chosen for its Indonesian-language legibility at
  small sizes, not for a Material type scale. A time or a count that must line
  up with the ones above it uses the face's tabular figures (`tabular-nums`),
  never a monospace face: monospace is for identifiers a person may copy (an
  ID, a field's key, a token, a URL), and a column of "09.00" in it read as
  code beside the names (Kalender's day pane, Papan live's rows, the times on
  Notifikasi and on supporting notes).
- **Radius** is the existing `rounded-md` / `rounded-xl` pair. No pill-everything.
- **Uppercase wide-tracked labels** name a group of destinations only: the
  drawer's group labels and the section subheaders of the Pengaturan list. A
  page header has none: a derived page's parent above its title is sentence
  case, label-medium (see Page headers), because M3's type scale has no
  all-caps overline. A heading inside a form is sentence case, because it is a
  heading and not a tag, and so is the label above a card's headline
  ("Laporan kunjungan", "Tim aktivitas", "Tampilan bulan"): `text-xs
  font-semibold text-muted-foreground`, M3 label-medium, as Linear, Notion,
  HubSpot and Google Workspace set their section labels.

## Language

Indonesian throughout the product surface. English is for code and comments.
One word per concept: a field is a "field", not sometimes "kolom".
| Filtering a list | Search always visible; each facet a button that opens a checklist (values OR, facets AND); state in the URL (Linear, HubSpot, Notion). On a desk the bar shows only what is narrowing the list: the two or three everyday facets always, then each other facet only while it is in use, then "+ Filter" listing the rest; a facet picked there lands in the bar with its list open and leaves again if closed empty. An active facet's button carries its value ("Industri: Belum diisi", or the label with a count), so the bar is its own summary and there is no chip row under it, only "Bersihkan semua" (Linear, Notion, Jira; M3 filter chips, menu). The count beside it reads "X dari Y" on every list in one sense: X is every match across all pages, Y the same list with no facets. Changing a filter keeps the sort and the page size and goes back to the first page, on Aktivitas, Prospek and Laporan alike. A facet with a value shows a leading check in the primary ink beside its tint, as does a toggle chip when on ("Butuh follow-up" on Prospek; "Saya", "Butuh jawaban" and "Menunggu tim" on Aktivitas; "Saya" on Kalender), one shared chip; the applied-filter chips a phone shows under the Filter button are one shared `FilterChip` with 8dp corners. LeadEngine's Contacts and Companies use the same chip, search field and header actions | `MissionFilterBar`: Status, Sales, then as `quick` the "Saya" toggle, Tanggal and the answer-lens toggles always; Laporan, Dibuat oleh, Lokasi, Industri (with "Belum diisi" pinned above a divider), Jenis as `FacetSpec`s. `ToggleChip` in `components/toggle-chip.tsx` (the Prospek toggle and the Aktivitas ones). `ReportFilterBar`: Status, Sales utama, Tanggal always; Hasil, Minat, Next action, Peluang, Lead as specs. `FilterBarFrame` `more`/`onClearAll`, `AddFilter`, `FacetButton` value summary, `?q=&status=&sales=…` |
| Acting on many rows | Checkbox per row and in the header; a selection bar appears with the count and only the actions that apply to a set (Gmail, Linear). On a phone the checkboxes are not there until asked for: a permanent column of them steals width from the name and makes a list read as a form, so a long press on a card, or "Pilih aktivitas" in the top bar's overflow, enters a selection mode in which the cards show their boxes and a tap ticks instead of opening; Batal leaves it (Gmail, Google Files, Photos; Android's contextual action bar) | `MissionTable` with `canDelete`: bar reads "N aktivitas dipilih · Ke sampah · Batal"; confirm dialog states what cascades. `SelectionModeProvider` shared by `ActivitiesPhoneMenu` and the cards, `useLongPress` on the card body (pointer events; a scroll cancels it, the click after it is swallowed, the browser's link menu is suppressed) |
| Reversible, so not confirmed | An action a person can take back gets no confirming dialog; it happens on one tap and the snackbar that follows carries "Batalkan" for a few seconds. A dialog is for what cannot be undone or costs money (M3 snackbar with action vs dialog; Gmail Urungkan, Google Calendar one-tap RSVP, Slack join/leave). Where the action has a side effect on other people, the undo inside the window is silent: it removes what the action told them, rather than telling them again. And a per-row action in a list is outlined, never filled: a filled button on every row is the loudest thing on the screen and the easiest to hit while scrolling; the filled version belongs to the record's own page | `JoinButton`: outlined in `MissionTable` rows and cards, `emphasis="filled"` on the activity page; the toast names the client and offers Batalkan for 6 s; `undoJoinMission` → `sales_mission.fn_undo_join` deletes the assignment, the unread MISSION_JOINED notifications and the history line within 2 minutes, else falls back to `leaveMission` |
| Snackbar at the bottom, one or two lines; a banner for what must be read | M3 places a snackbar at the bottom of the screen: centred above the navigation bar on a phone, left- or centre-aligned on a larger screen, never a top corner, which is where notification libraries put them by habit. It holds one or two lines and at most one short action, and it goes away on its own. Anything longer, or anything whose action must wait for the person (a tab the browser held back, a road they should understand), is a banner: in the card it concerns, under the header, with one or two text buttons (never filled: the card's own primary action stays the loudest thing on the page, and the dismissive one sits left of the affirmative), until they close it (M3 banner; the "Admin meminta klarifikasi" box already is one) | `AppToaster`: `bottom-center` on a phone, `bottom-left` on a desk. The desk road of Bagikan ke WhatsApp: a short snackbar ("Teks tersalin, foto diunduh") and `DeskShareNotice`, a tonal banner in the report card announced by the button through a window event, with Buka WhatsApp Web and Tutup |
| Destroying everything | Type-to-confirm in a bordered danger zone, away from the list (GitHub) | Pengaturan → Data: "HAPUS SEMUA MISSION" phrase, checked again on the server |
| Audit trail | One line per action in plain words, who and when beside it, before/after a click away; filters for who, what, which, when (Salesforce Setup Audit Trail, HubSpot activity log) | Pengaturan → Riwayat aktivitas; rows written by database triggers, grouped per transaction |
| An error that escapes the page reloads once, then explains | Every workspace page has its own error screen ("Halaman ini gagal dimuat"), but the workspace layout sits above that boundary, and an error there fell through to Next.js's bare English sentence, "Application error: a client-side exception has occurred", with nothing to press. The errors that reach that depth are almost never the page's own: a tab that kept the previous build's scripts across a deploy, or a stream the server could not finish (React #419) after which the client re-renders with modules that no longer match (React #310). Both are cured by loading the page again, which is what people did by hand, several times. So the outermost boundary (`global-error`) reloads the page itself once, remembered in sessionStorage for a minute so a genuinely broken page cannot loop, and only on the second failure shows a screen in the same shape as the page-level one, with the digest for the server logs, Muat ulang and Kembali ke dashboard. Two smaller rules in the same commit: the browser's own fetches of the web manifest, the service worker and the icons bypass the session proxy, because a manifest request carries no cookies and was being redirected to the login page as HTML (26 "Manifest: syntax error" lines per session, and a worker that never installed); and every hook in a component is called before any early return (`MobileNavBar` announced a hidden nav with a hook below the return), which the React rules-of-hooks check now passes across the app | `app/global-error.tsx` (`RELOAD_KEY`, `RELOAD_WINDOW_MS`); `proxy.ts` `config.matcher`; `components/mobile-nav-bar.tsx` |
| Usage is one row per person per day, read on an admin page, never a tracker | The unit's admin could see every write (Riwayat perubahan) but not whether anyone opens the app. Salesforce's Lightning Usage App, Notion's workspace analytics and Microsoft 365's active-users report answer that with the same small shape, and so does this: one row per person per WIB day (first and last seen, pages opened, the last page) and one counter per page per day. Daily rows, not an event stream: a day row answers "is this person using it", a page counter answers "which screens earn their place", and neither can be replayed into a trail of what someone did, which is the line between usage and surveillance. The browser calls the server at most once per 30 seconds and batches the pages opened in between, so a quick list → detail → report is three opens in one request and none is lost; nothing is sent while the tab is hidden; after five minutes on one page a heartbeat marks the person as still here (last seen moves, no page is counted), but only if they tapped, typed or scrolled since the last call, so a rep working Hari ini all morning is seen at 12:00 and a tab left open on an empty desk is not. Paths are normalised in the browser and again on the server (uuid, numeric and opaque segments become `:id`, the query string and fragment are dropped), so a record id never lands in a usage table and every activity's page counts as one "Detail aktivitas"; names come from the product's own words at render time, and an unknown path shows as itself rather than a guess. Reading is for settings admins only, the same gate as Riwayat: row security keeps the rows inside the unit, a person writes only their own day row, nobody can delete one. The page reads as those products do: people active today, in 7 and in 30 days against the headcount (M365's active-users cards), then "Pengguna aktif per hari", a bar per WIB day of the people who opened the app, because a trend is read from a shape and not from three numbers (Lightning Usage App's daily active users, Microsoft 365's Active users chart): every day of the period is drawn and a day nobody came is 0, weekends are in the muted ink and weekdays in `chart-1` so the weekly rhythm shows at a glance (the board's legend under the plot names both), the tooltip is one line ("Sel 22 Sep · 5 orang"), and with no rows at all the axis still draws while the "Belum ada catatan" sentence under the tiles stays the only empty message; then one line per person with Terakhir aktif as a relative time and the exact stamp on hover (Slack, Teams), days active in 30, pages opened in 7, and an eight-week line of days active per rolling week on a fixed 0–7 scale so rows compare (a per-row scale would draw one day a week as a full week); someone not seen for 7 days carries a muted "Tidak aktif 7 hari" chip and someone never seen reads "Belum pernah"; everyone with access is listed, seen or not, so the table accounts for the counts above it. Then the pages opened most, as the house bar list. One period (7, 30 or 90 days, in the URL) governs the daily chart and the pages alike, and the same segmented button sits on each of those two cards rather than once at the top of the page, because the tiles and the people table between them count fixed windows and a page-level control would read as narrowing them too. On a phone the people table is the card list the other lists use. Honest disclosure: the page's description, the guide's "Siapa boleh apa" and the changelog all say what is recorded (which page, when) and what is not (nothing typed, nothing viewed, no location) | Migration `20260923090000_usage_tracking.sql`: `sales_mission.usage_days`, `sales_mission.usage_pages`, `record_usage` (invoker, atomic upserts, heartbeat as `p_views = 0`), `usage_last_seen`. `lib/usage/usage-path.ts` (`normalizeUsagePath`, `isUsagePath`, `usagePageLabel`), `lib/usage/usage-beacon.ts` (`createUsageBeacon`, `collapseVisits`, `USAGE_MIN_INTERVAL_MS`, `USAGE_HEARTBEAT_MS`), `lib/usage/usage-schema.ts`, `lib/usage/usage-stats.ts` (`summarizeUsage`, `dailyActiveSeries`, `usageDayWindow`, `usageDayLabel` / `usageDayLongLabel`, `rollupPeople`, `weeklyActiveDays`, `rollupPages`, `lastSeenLabel`), `lib/usage/usage-queries.ts`, with tests beside them; `recordUsage` in `app/actions/usage-actions.ts`; `UsageBeacon` in `components/usage-beacon.tsx`, rendered by `WorkspaceShell`; `app/workspace/settings/usage/` (`page.tsx`, which reads day rows for eight weeks or the period when that is longer; `usage-active-chart.tsx` with `UsageActiveChart`, Ringkasan's `DayBars` fed a stacked weekday/weekend pair of which a day fills one, with its `tooltipLine`; `usage-people.tsx` with `UsageTrend`; `usage-period.tsx`, rendered on both period cards) reusing `ListBars` from Ringkasan; `DayBars` never draws a bar wider than its day's slot, which ninety days on a phone needed; the card on `app/workspace/settings/page.tsx`; `paths.settings.usage` |
| Dashboard vs screen | One configuration, two renderings: set it up where you can click, send exactly that to the display (Geckoboard, Grafana TV mode). The in-app view uses the app's tokens; only the screen inverts | Papan live: control bar (segmented range, facets, panel chips) → same options in the URL → "Tampilkan di layar" and "Buat tautan layar". Privacy (client names, and the outcome of reported visits: with a name it is the pipeline) is bound to the token as two switches at link creation, never to the URL; the admin's own preview shows both. A third switch pairs the screen with a calendar link the wall draws as a QR (same masking, same filters, its own row and revocation); it is never the screen link, which is the board's credential |
| Selection vs action controls | Controls that change what is shown look different from controls that do something (M3 segmented button, filter chips vs filled/outlined buttons). A chosen option, in a chip or a segmented button, is tonal (secondary container) with a check, never the primary fill: that fill is for the one main action of a screen, and a filled "Bulan ini" beside a filled Ekspor read as two things to press | Segmented Hari ini/Minggu ini, facet buttons, toggle chips on the left; one filled action and one outlined action on the right; `Segmented` and `ChoiceChip` active state `--tonal` |
| Surface layering | All surfaces from one neutral ramp tinted toward the primary, stepped a few percent apart: page < nav panel < card. Dividers are outline-variant (tinted), never black alpha. The nav's active item is the secondary-container indicator: tinted fill, primary text, no shadow (M3 tonal surfaces, navigation drawer) | `--background` #F6F8FB, `--sidebar`/`--muted` #EEF2F7, `--card` #FFFFFF, `--border` #D9E0E8, `--sidebar-primary` #D9E4F5. Same tokens in LeadEngine so switching apps does not change the material |
| Edit vs reschedule | One door for anyone who may change a thing; a separate "request" only for those who may not (Google Calendar: Edit + "send update to guests?"; Calendly: Reschedule as a request) | Ubah carries the schedule for whoever the matrix lets change the mission (its owners, and anyone whose Cakupan reaches them; the primary alone per tenant rule), and asks for an optional reason when it changed; Usulkan jadwal lain remains only for supporting sales and centrally-scheduled units. No "Pindahkan jadwal" button |
| Switching apps, loading a page | Feedback at the click, one branded screen across the origin change, no white flash, content arrives once without shifting (Slack workspace switch, Google app launcher, Notion/Linear splash then skeleton; GitHub/YouTube top bar). M3: circular indeterminate for one whole screen, linear at the top edge for a page load, none for very short waits, fade-through between unrelated destinations, reduced motion honoured | `AppTransit` drawn by the switcher on click (overlay) and by each app's root `loading.tsx` on arrival; `html`/`body` carry the background inline; sidebar fold and width come from parent-domain cookies so the first HTML is already right; `TopLoader` shows after 120ms; every new animation has a reduced-motion branch |
| Phone shell | Compact window class (below `md`): a navigation bar of 3–5 top destinations with icon, label and a tonal active pill, 80dp plus the safe area; the rest behind "Lainnya" in a bottom sheet; a small top app bar carrying the page title, back on sub-pages, one trailing action; one extended FAB for the screen's primary action, above the bar; the bar steps aside for a form's own action bar (M3 navigation bar, top app bar, FAB, bottom sheet, window size classes) | `MobileNavBar`, `MobileTopBar` fed by `PageChrome`, `Fab` from `WorkspacePage`'s `primaryAction`, `FormActionBar` announces `hideNav`; `h-dvh`, `viewport-fit: cover`, `env(safe-area-inset-*)`, one page scroller `#page-scroll` |
| Touch targets | 48dp is the target, not the glyph: a 16dp checkbox or a 20dp icon keeps its look and grows its hit area; below `md` every button size steps up one notch (M3 accessibility, touch target size) | `buttonVariants` responsive heights, `Checkbox` `after:-inset-3`, 40dp dialog/sheet close, 44dp menu rows, chip removes and pager arrows |
| Installed app | The web app is the app: manifest, standalone display, home-screen icon, theme colour; a service worker that caches hashed static assets only and never a page or a datum, versioned per build so no deploy can serve a stale UI (PWA; the LeadEngine kill-switch is the lesson) | `app/manifest.ts`, `public/icons`, `public/sw.js?v=<build>`, `PwaRegister`, `/workspace/pasang` with the Android prompt and the iOS steps |
| Lists on a phone | Search stays; the facets go behind one "Filter" button that carries the active count and opens a bottom sheet, so a seven-facet bar costs one row, not four; active filters remain visible as one sideways-scrolling chip row; every chip row that scrolls sideways bleeds to the screen edge and fades at its right edge, the fade lifting once the end is reached, so a chip cut by the edge reads as "more this way" rather than as a fault (M3 chip carousel; YouTube, Google Maps); a long list grows with "Muat lagi" instead of turning pages, up to the largest page size, and only then steps to the next page (M3 filter chips in a modal bottom sheet, list pagination on compact windows; Gmail, Google Maps filters) | `FilterBarFrame` around the three filter bars (`useCompact`), `.chip-scroll` in globals.css on the Aktivitas row of everyday narrowings (`FilterBarFrame quick`), the calendar, guide and active-chip rows, `MissionPagination` compact branch stepping `size` through `PAGE_SIZES`; the bottom sheet holds the everyday facets and every `more` facet together, with the saved views to choose from at its top (`SavedViewsSheetSection`, which closes the sheet on a pick), and the chip row exists only here; desktop keeps the pager |
| Content before chrome on a phone | A list on a phone opens on its records, not on its furniture: search is the first control (Gmail, Google Maps, Linear), the one-tap chips sit under it and narrow what it found, and everything a rep in a lobby never needs (export, import, bulk selection) goes behind the top app bar's overflow menu rather than standing as buttons above the list. A sentence that only teaches what a coach mark already teaches is not shown there either; a description that states facts (a record's type and place) still is. On a desk the header keeps its buttons, and its sentence until the person closes it (M3 top app bar overflow, filter chips as secondary narrowing) | `WorkspacePage phoneDescription={false} phoneAction={false}` on Aktivitas; `ActivitiesPhoneMenu` announces Export, Import dari .xlsx and Pilih aktivitas through `PageChrome menu` (items may carry `onSelect`, not only `href`); `FilterBarFrame quick` places the everyday narrowings (Saya, Tanggal, the answer lenses) in a row under the search on a phone and inside the bar, after the everyday facets, on a desk; the phone's applied-filter chips leave out what that row already shows. The same on Prospek (`ProspectsPhoneMenu`: Import, Pilih prospek) and Laporan (Ekspor; on Ringkasan the exports and Atur widget, announced by `DashboardEditor`, which owns the edit mode); `ReportTabs` sits flush under the phone's app bar, where M3 puts primary tabs. `SelectableCardBody` is the shared card body (link, or tick in selection mode, long press to enter it) for the activity and prospect cards |
| Detail page on a phone: one door each | Each action has one door on a phone. Ubah is the bottom bar's pencil and the overflow's "Ubah aktivitas", not also a button in the facts card; Join is the bottom bar's one filled button, not also the team card's; a destructive secondary action (Batalkan aktivitas) is the last, danger-inked row of the overflow, as Google Calendar's Delete and HubSpot's and Salesforce's mobile record actions, its dialog unchanged; the record's id and the description line that repeats the facts card are desk-only. A state is said once: while the tenant's rule keeps the report closed, the card body says when it opens, and no footer repeats it (M3 top app bar overflow, bottom app bar) | `ActivityPhoneMenu` (links + Batalkan via a controlled `CancelMissionButton`), `max-lg:hidden` on the card's Ubah, id and cancel row, `JoinButton className` when `compactAction === "join"`, `phoneDescription={false}` |
| A calendar's count sits under the date on a phone, at the corner on a desk | On a phone a month cell is 44px wide; a badge pinned to its corner covers a two-digit date, so the count sits in flow under the number (M3 badge on a date; Google Calendar's month dots). From `md` the cell is wide and, when the card is short, only one line tall: there the in-flow badge fell below the cell's edge and was clipped, so it pins to the top-right corner as it always had. The once-only actions (public link, phone feed) wait in the phone's overflow | `MonthGrid` badge `mt-0.5 self-center` below `md`, `md:absolute md:top-1 md:right-1` above; `CalendarPhoneMenu` |
| A long form on a phone | The sticky chrome is the app bar and the section chips, nothing between (the facts line is desk-only); the chip row fades at its edge like every scrolling chip row. The bottom bar carries the one next step and the state that gates it ("Belum lengkap: …", "Laporan siap dikirim", the draft's sync); Kembali is the app bar's arrow and a destructive Buang draf is the overflow's danger row, both only from `sm` up as buttons in the bar (M3 bottom app bar; Google Forms' single Submit). Attachments close the form as their own last section, so no section, and no chip, appears twice. A helper line says what to do, once: the control's own limit and the admin's hint share one line, and a label, a checkbox and a helper never restate each other | `VisitReportForm`: `PageChrome menu` Buang draf, `hidden sm:inline-flex` on Kembali and the text button, `Lampiran` in `CORE_SECTIONS`, `FieldShell hint={null}` when the control draws its helper, `PhotoField hint` "Maks N foto · …"; `SectionChips` `chip-scroll`; the report page `phoneDescription={false}` |
| Who is on it, at the card's foot | A card's bottom row is supporting content at the start and the one action at the end (M3 card). On the activity, prospect and report cards the start holds who: stacked avatars, the lead first, the lead's name written beside them and "+N" for the rest, because most people have no photo and two initials alone say less than a name. "These people → Join" is the pairing Google Meet and Chat draw; Trello and Jira put the members at the foot of a card too. Every card has the row, so "who" is always in the same place, and nothing in it is tappable: the body stays the one link. The report card has the same anatomy (name and status; "Jenis · Hari, tgl · jam"; "Hasil · Minat", each said once; a divider; the sales utama at the start of the foot) and, since the Laporan table offers no row action, the end of its foot holds the follow-up as a chip: "Follow-up Sel, 29 Sep", "Lewat Sel, 22 Sep" in the warning tone (the word says it, not only the colour), "Selesai 24 Sep" or "Dibatalkan" as quiet text. The chip is a label, so it is tonal with 8dp corners and no outline, never the outlined shape of the button the other cards carry there ("Status vs action"); the whole card, foot included, stays the one link. The grey "sales · peluang · lead" line and the "Terbuka · … · …" line it replaces said the sales name twice; the chip leaves the owner out, the foot already names who, and the whole line is in its title | `TeamFacepile` (`PersonAvatar size="sm"`, `-space-x-2`, `ring-2 ring-card`, max 3) in `MobileMissionCard` (the lead, then `assigneeIds` with photos from the unit's `people`) and the prospect card (the holder); the body's grey name line is gone. The report card in `report-table.tsx` (one `Link` around body and foot; `followUpChip` and `followUpLine` in `lib/reporting/report-follow-up.ts`, tested) |
| An agenda groups by day | A list of what is coming shows each date once, as a group header, and each row only its time, so the name keeps the width a repeated "21 Sep, 09.00" would take; what was called off is not "next" (Google Calendar schedule view, Outlook agenda) | Hari ini → Berikutnya: `upcomingDays` grouped by `missionDayKey`, "Besok, Sen, 21 Sep" headers, a 44px time column, cancelled visits excluded (the ones awaiting a new date have their own list) |
| A grid always names its columns | Every layout grid declares `grid-cols-1` at the base size even when it only fans out at a breakpoint. Without it the single column is an implicit `auto` track whose minimum is the content's min-content width, and a `truncate` (nowrap) client name makes that the full name, so the card grows past a phone's edge and truncation never happens. `grid-cols-1` is `minmax(0, 1fr)`, which lets the track shrink and the ellipsis do its work (CSS Grid intrinsic sizing; Tailwind's `grid-cols-*` uses `minmax(0, 1fr)`) | `grid grid-cols-1 gap-… sm:grid-cols-…` on every page grid; `TodayCard` on Hari ini was the case that surfaced it |
| Detail and forms on a phone | A detail page has one bar at the bottom and one next step on it, chosen by state (answer, then report, then join, then edit); the cards reorder for the rep (facts, answer, report, contact, team, notes) and the top bar's overflow menu carries the links worth reaching without a scroll. A long form gets a sticky row of section chips that shows where you are and marks what is still empty; "Belum lengkap" names fields that scroll to themselves; the schedule strip fits the screen and a tap on it sets the start (M3 bottom app bar, secondary tabs, top app bar overflow, 48dp targets) | `FormActionBar until="lg"` on the activity page with `compactAction`, `max-lg:order-*` with `max-lg:contents` columns, `PageChrome menu`; `SectionChips` in both forms, `jumpTo` in the report form, `SchedulePicker` 44dp days, `max-h-[45dvh]` strip with `onClick` |
| Teaching empty states | An empty list teaches: one sentence on what the list is, one to three numbered steps that fill it, the first step as a button, and "Pelajari" into the guide. The first-time user learns the product here, at the moment of need, not from a tour (Notion, Linear, Figma empty states; M3 empty state pattern) | `EmptyState` with `steps`, `learnHref`, `icon`; the activities, prospects and reports lists; Hari ini's empty day links to the guide |
| Coach marks | No sequential tour; one-step marks in the shape of M3's rich tooltip: a title, one sentence, "Mengerti" (and "Pelajari"), anchored to the control the first time it matters, one per screen, after the screen settles, never locking it, dismissed by any outside tap. "Once" means once per account, on every device (Google Workspace feature discovery) | `HintsProvider` in the shell fed by `listSeenHints`, `useCoachMark`/`CoachMark`, `sales_mission.user_hints` (self-only RLS) with localStorage fallback; marks on Lainnya, Pasang, the Aktivitas FAB, Join, Kirim laporan |
| Announcing a release | A release note nobody opens is not an announcement, and a WhatsApp message to the sales group did not land either. Slack, Notion, Figma and Gmail announce a release once per person with a dialog of two or three highlights, each with a way straight to the feature, and a text button to put it off; the rest is a dot on the What's new entry. Nothing sequential, nothing on every login, and never for every fix. The control is two layers, as those teams run it (a release team writes the highlights with the feature; a product manager switches them on from a panel, without a deploy): here a changelog entry may carry an `announcement` block, and Pengaturan → Pengumuman lists those with a switch each and Umumkan ulang. A switch is on by default when the change is every rep's business and off when it is not (Ubah aktivitas after a report is for whoever tidies records, so it ships off), which is what the panel is for. The dialog shows on Hari ini only, after the page settles, so a person who arrived at a record from a notification is never interrupted; on a phone it is a bottom sheet (M3 dialog on a desk, modal bottom sheet in a compact window); Nanti saja closes it for good and leaves the dot; Mengerti and Coba sekarang clear both. "Once" is once per account on every device, keyed on the release date until an Umumkan ulang and on the moment of the last one after, so Umumkan ulang makes it new for everyone while switching a release off and on never does (a switch leaves `announced_at` empty) (Google Workspace feature discovery, keyed the same way) | `announcement` on `ChangeEntry` in `lib/changelog.ts`; `announcementStates` / `pendingAnnouncements` / `unreadAnnouncements` in `lib/announcements/announcements.ts`; `sales_mission.release_announcements` (per unit: enabled, announced_at); seen and read marks in `user_hints` as `announce-<key>-<ts>` and `read-<key>-<ts>`; `AnnouncementDialog`, `WhatsNewDot`, `MarkAnnouncementsRead` in `components/announcements/announcement-dialog.tsx`, rendered from the shell; `AnnouncementList` under Pengaturan → Pengumuman with `setAnnouncementEnabled` / `reannounce` |
| The guide | One page, a few short parts, each ending in the link that does the thing; anchors so every "Pelajari" lands on its part; reachable from Lainnya, the sidebar under Pengaturan, and every teaching empty state (in-product help centres of Linear and Notion) | `/workspace/panduan`, `paths.guide`, `paths.guideSection(id)` |
| Quick filters and remembered views | The everyday narrowings are part of the filter row, each in exactly one place and outside the sheet, keeping the rest of the query: a toggle is one tap, a day two (Tanggal, then the day); the sheet holds the rest. On Aktivitas they were a row of chips of their own above the bar (Semua · Saya · Hari ini · Minggu ini · Mendatang · the answer lenses), and every one was already a value of something in the bar, so choosing "Hari ini" showed the same state twice, as a chip and as "Tanggal: Hari ini". Now the dates are the Tanggal facet's values and Tanggal is always in the bar; "Saya" is a toggle chip beside the facets (tonal with a check when on, like "Butuh follow-up"), and the Sales facet lists the other people, so "me" is not shown twice either; while the unit asks for answers, "Butuh jawaban" and "Menunggu tim" are toggle chips carrying their counts; "Semua" is gone because it is no filter, which "Bersihkan semua" already is (and it now clears the lens too). The parameters are the ones the chips wrote (`date`, `sales=me`, `filter`), so an old link, a saved view or a remembered view opens the same list: the map is the identity (M3 filter chips; Linear, Notion, Jira keep one control per state). A list reopens the way you left it (a cookie per list, redirected on a bare open; the URL stays honest); a saved default view chooses the view only on the list's first open in a browser, when there is no cookie at all (an empty one, left by "Bersihkan semua" or the first visit, is a memory of the plain list and wins), and a calendar has a "Hari ini" jump (M3 filter chips, secondary navigation state; Gmail, Linear, Jira, Todoist, Google Calendar) | `MissionFilterBar` `quick` (Saya · Tanggal · Butuh jawaban · Menunggu tim: whose, then when, then answers, so "Saya" is never the chip past a phone's edge), `ToggleChip`, `lib/missions/quick-filters.ts` (`toggleMe`, `salesOthers` / `withSalesOthers`, `toggleLens`, `countNarrowing`, `activityListParams`; the tests read every old chip's link), `sales=me` resolved server-side, `RememberView`/`ViewLink` + `rememberedView`, `openListView` / `resolveRememberedView` (`fresh`) on Aktivitas, Prospek and Laporan, calendar "Hari ini" |
| Calendar out | The phone's own calendar carries the schedule: a one-way iCalendar feed per person on a secret URL shown once, and a second feed for the team when the role sees beyond its own (the same reach as the list, resolved for that person by `fn_visible_owner_ids_for`, the person's name first in each event) (the board-link pattern: hash only, one active link, retire by replacing), no OAuth, cancelled visits kept as CANCELLED so subscribers see the change; plus "Tambah ke kalender" per visit (Google template link, .ics for iPhone and Outlook). Two-way sync is a later tier | `/kalender/<token>/aktivitas.ics`, `lib/calendar/*`, `sales_mission.calendar_tokens` (self-only RLS), `/workspace/kalender-saya`, detail-page menu |
| Hand-off to the CRM | Required fields are enforced in the form ("Belum lengkap" naming each, one tap to it), never only by a greyed button; the one-way step (one push per activity) goes behind a summary dialog that shows the lead as the CRM will, then commits. Pick lists that belong to the CRM (category, grade) are read from the CRM's Master Options and never copied; the report's interest level only suggests (M3 dialog for consequential, irreversible actions; Salesforce Convert Lead, HubSpot) | `PushLeadPanel` with `missing`/`jumpTo`, `ChoiceChip` categories from `/api/v1/master-options`, the confirmation `Dialog` (a bottom sheet on a phone), `suggestCategory`, `lead_pushes.category/grade_lead` for Ringkasan |
| One vocabulary across the hand-off | A value that names the same thing in both apps (industry) has one list, edited in one place and matched to the CRM's by the admin; it flows prospect → activity → CRM company without being typed again, fills only what is empty, and never overwrites what a person or the CRM already set (HubSpot property sync rules) | `withSharedIndustry` (activity form reads the prospect form's list), `fillIndustry` in the forms, `industry` on the company create call, `fn_find_or_create_client_company(p_industry)` fills an empty CRM industry only |
| Wall board | A wall has to make people want to look, so it is composed around one focal point, not laid out as a table (M3 Expressive: hero moment, emphasised type, extra-large shapes; airport FIDS for the schedule; Geckoboard/Grafana TV for the progress). The spotlight card on top holds the visit happening now, or, with ten people out, the two or three happening at once ("Sedang berlangsung · 3 kunjungan", the first three side by side, the rest counted and all of them lit in the timeline), else the next one with a countdown ("dalam 45 menit"), else a done state that says so; below it the schedule is a timeline (a node per visit: check when done, a live ring for now, hollow for later) of rounded tonal rows, and beside it the day's progress, which counts what happened rather than what was typed: big "4 / 6 sudah berlangsung" (reported, or the scheduled end has passed), a two-segment bar (green reported, amber happened-but-unreported) and three counts, Dilaporkan / Belum dilaporkan / Di lapangan and the team, each person with an initials avatar in an identity colour, ordered by the office's question (who is with a client now, in a lit row naming the client and place; then who is about to be, "Berikutnya 15.00 · …"; then "Jadwal selesai") and a count that says what it counts ("3 kunj."). The date is the headline and the clock is large, because on a wall it is ambient information people glance at. Past rows step down in tone so the first full-contrast row is the next visit. A row's last column says what the wall can know, not what the workflow calls it: "s.d. 11.00" for a visit still to come (never "Diterima" or "Ditugaskan", facts about the assignment nobody in the office acts on), "Sekarang", "Selesai", or "Belum dilaporkan" in amber once the end has passed with no report. The visit type sits beside the client as a small tonal chip; a reported visit's second line opens with its outcome (glyph and colour by kind: met, moved, absent, cancelled) when the link allows it, then the place. The list ends with "Tidak ada jadwal lagi" instead of a void; with no visits at all the spotlight takes the whole column. A list taller than its box turns pages on its own, each page starting at the first row the last one cut off so no row is ever shown in halves (a FIDS turns whole flights), lingers twice as long on a page holding what is happening now, and returns to the top. Everything sized in `em` from one root that follows the screen, so 1080p, 4K and a portrait panel show the same composition. The wall's one call to action is a QR to the public calendar, placed as signage places it (airport FIDS "scan for updates", Robin and Envoy room panels): persistent, in the supporting rail under the team with a verb ("Pindai untuk jadwal di ponselmu"), never in the hero, dark modules on a white tile whose padding is the quiet zone, sized for someone who walks up to it (about 6 em), and in the top bar beside the clock when the rail is a band (portrait) or absent | `app/board/board-view.tsx`: `SpotlightCard`, `StatsCard`, `VisitRow` + `TimelineNode`, `TeamRow` + `Avatar`; `.board-root`, `.board-backdrop`, `.board-ring` in globals.css; `AutoScroll`, `BoardClock`; in portrait the rail becomes a two-card band under the schedule and the Sales column drops |
| Drawer rail vs account menu | The navigation drawer holds 3 to 7 destinations and one Administrasi group for admin settings; personal and supporting pages (Kalender saya, Panduan, Yang baru), the panel toggle and Keluar live behind the account menu at the foot of the drawer, not as rows (M3 navigation drawer: destinations only; Slack, Notion, Linear keep help and release notes off the rail). The drawer has two states and no free width: 220px expanded or the 60px rail, switched by the panel toggle and kept in the shared `sidebar-collapsed` cookie (M3 gives drag handles to content panes, never to navigation). LeadEngine uses the same two widths and the same rule for what is a destination: its audit trail moved from the drawer into Settings, as Riwayat perubahan sits in Pengaturan here | `workspace-shell.tsx` `adminNav` (Pengaturan only) and the `DropdownMenu` on the profile block; on a phone the same items sit in the Lainnya sheet |
| Settings index: a grouped list of destinations, never a grid of cards | Pengaturan is a list of places to go, so it is an M3 list: subheaders name the groups (Form, Alur kerja, Komunikasi, Pemantauan, Sistem) with one supporting sentence each, and every page is one two-line list item at 72px (leading icon in a 40px neutral container, headline, supporting text clamped at two lines so a phone never cuts it to a few words, trailing chevron), read straight down. A three-column grid of equal cards made the eye zig-zag across fifteen items with no grouping, and M3 keeps cards for self-contained content of mixed kinds, not for a row of equal links; Android Settings, Google Account, Salesforce Setup, HubSpot, Linear, Notion and LeadEngine's own Settings are all grouped lists. The trailing mark is a chevron because the row opens a page inside the app; the ↗ arrow means leaving the app and is used only where that is true (the Roles & permissions link into LeadEngine under the list). Icons are neutral (on-surface-variant) and each one different: status colours (success, danger, warning) mean a status, so a green Form laporan or a red Data said something that was not true; Data sits last in Sistem instead, and its own page carries the danger zone. Only pages that exist are listed: a card saying "Belum tersedia" advertises what cannot be used. The list stops at a readable width (`max-w-4xl`) rather than stretching across a wide monitor. Same pattern, same measurements as LeadEngine's Settings | `app/workspace/settings/page.tsx` (`SECTIONS`, `SettingRow`); LeadEngine mirrors it in `app/(app)/settings/page.tsx` |
| Settings matrix layout | One pane: the selector on top as choice chips, the matrix below at full width; two panes only when the screen is genuinely wide. The page is the only scroller; a table row is a list item (headline, one supporting line, the rule behind an info icon), secondary actions in a trailing overflow menu (M3 list-detail collapsing below expanded width, choice chips, list item, data table; HubSpot Roles, Salesforce profiles) | Role & Izin: role chips above the card, hierarchy named in the card header, fixed-layout table (min 760px) whose Modul column takes what the switches leave, presets in a per-row menu, the reading guide folded under one button |
| Integration settings: written keys, tested connections, chosen from what the endpoint reports | A connection to an outside service is set up on one page in two cards, the connection (endpoint, key) then what it offers (models). The key field is empty on every visit and names the stored key only by its last four characters, because a secret is written and never read back (HubSpot integrations, Zapier connections, Vercel environment variables); "Uji koneksi" makes one real request and reports the outcome as a sentence with a check or a cross, and it is the request that fills the pickers, so a person chooses among models the endpoint actually serves rather than typing a name from memory (Metabase's database test, Grafana data sources); a picker falls back to a text field when the endpoint lists nothing, and a stored value the endpoint no longer lists stays selectable rather than being dropped silently. Saving tests again, so the stored status describes what was stored, and also asks each chosen model for one word, because a model can be listed and still not answer (a reasoning model given a small token budget answers with nothing); that failure belongs on the settings page with the model's stop reason in a sentence, never as a code in front of a salesperson. The same connection is shared by both apps and shown identically in each (M3 text fields with supporting text, list item with trailing control) | `AiSettingsForm` under Pengaturan → AI (`app/workspace/settings/ai`), `testAiConnection` / `saveAiSettings` in `app/actions/ai-settings-actions.ts`, `lib/ai/ai-proxy.ts` (`normalizeEndpoint`, `listModels`), `lib/ai/ai-settings.ts` (Vault-backed key via `fn_ai_store_key` / `fn_ai_read_key`, service role only); LeadEngine mirrors it under Settings → AI |
| The AI bill is shown where the connection is set, in tokens, as a pace | Every call to the proxy from either app writes one line to a shared log (app, feature, model, tokens in and out, ok), and Pengaturan → AI → Pemakaian (its own page off a header button, at the form's width, because a form ends at its Simpan and a read-only ledger is another task; OpenAI's and Anthropic's consoles keep Usage beside the keys, not under them) shows: the last 7 and 30 days as calls and tokens, then what a week and a month cost at the last week's pace, then the split per feature and per model, because the person paying wants "berapa juta token sebulan" before the invoice, not after (OpenAI's and Anthropic's usage pages, Vercel AI usage, Salesforce's Einstein request consumption all sit beside the credentials). Tokens, never rupiah: the price is the proxy's and would go stale here. A log younger than a week projects from the days it covers, an empty log says so instead of showing zeros, and failed calls are counted but carry no tokens. The card is one plain table set, not a chart: four rows and three splits read faster as numbers | `public.ai_usage` (migration `20260921090000_ai_usage.sql`, service role only), `recordAiUsage` / `summarizeUsage` / `readAiUsage` in `lib/ai/ai-usage.ts` (written by `askSalesData`, `generateInsight`, the Save probe), `UsageCard` on `app/workspace/settings/ai/pemakaian/page.tsx` (`paths.settings.aiUsage`); LeadEngine mirrors it at `/settings/ai/usage` with `AiUsageCard` |
| AI-written content is labelled, dated, sourced, optional, and never ahead of what the page is for | A sentence a model wrote sits in its own card marked as such (the tonal "Dibuat AI" chip in the card's title row), with when it was written, what it was written from ("dari N laporan hari ini", or "belum ada laporan hari ini") and a line saying it can be wrong, because a generated sentence is an estimate with a timestamp, not a figure (HubSpot and Salesforce mark generative summaries the same way; Google Analytics Insights). The model is handed counts the app computed, never rows to count, so every number in the brief also exists on a widget where it can be checked; each point carries a kind icon (up, down, action, info) and a point a list can answer is a link into that list, the same drill-down the widgets use. The card never blocks the page: if nothing exists it asks after paint and says what it is doing. Its foot is the M3 card foot: the provenance sentence at the start, the actions at the end, never in the header; the model's id is audit data, not card copy. **Placement follows M3's layout hierarchy: what a page is for is seen first, and a card is a door, not a container for a document.** Ringkasan is the board, so the brief's card is one card in the grid, not a banner above it: it goes through Atur widget like every card (moved, resized, hidden), takes the first row at full width the first time a board meets it, and is about today whatever the toolbar says (Google Analytics puts Insights among the dashboard's cards; a fixed AI block above the content would push the page's own content below the fold). As a door it shows at most three points and picks them by urgency, not by order written (Perlu ditindak first, then Rekomendasi, then Yang terdengar di lapangan), clamps a point that does not fit, and has no scroller of its own — a card that scrolls hides how much it holds and fights the board's own scrolling — with one text button, "Baca brief lengkap", into the page that holds the whole thing (M3 card action; Google Analytics' "View all insights"). The whole brief is Laporan's third tab, Insight, which exists only where the brief does (the unit's switch on and Insight AI readable), because a tab that leads to a refusal is worse than no tab. There it is M3 list-detail: on a desk the days that have a brief are a 280–320px pane on the left, newest first with Hari ini always leading, and the brief takes the rest; below the expanded width the pane collapses into a row of day chips above the brief, since two panes on a phone means neither is readable. The day is in the URL (`?day=`), so one day's brief can be linked to and sent. A free question (Tanya AI) is a supporting pane, not a card: one "Tanya AI" button on the toolbar whose period and people it answers about opens a side sheet on a desk and a bottom sheet on a phone (M3 canonical supporting-pane layout; Gemini in Workspace, Einstein Copilot, Power BI Copilot and HubSpot Breeze all keep the conversation beside the content, never between the toolbar and the content). Inside: the everyday questions as chips (Ask Attio, Pipedrive Sales Assistant, Zia), the field at the foot, each answer under its question and marked as the model's, drawn as paragraphs and lists with bold where the model bolded and never as raw Markdown marks (`parseAnswer`: the two shapes an answer honestly has, nothing a Markdown engine would let a model draw into a bubble), computed through the person's own session from the same cube numbers as the widgets plus the rows behind them (the period's activities and reports, today's and tomorrow's appointments whatever the period, prospects due, the roster), each list capped and saying so, so the answer can never know more than the Ringkasan, Aktivitas, Daftar and Prospek pages show that person and can always be checked there (Einstein Copilot and Power BI Copilot ground answers in the same data the page shows and name the source; a fixed bundle of capped rows is the cheap step before tool calling), the last few turns kept so a follow-up reads in context, and the conversation itself kept per account rather than per sitting (its own row below). Both exist only while the unit's switches are on, and who sees them, and whose numbers, is the Role & Izin matrix (module Insight AI, read scope), not a per-feature toggle | Built-in `ai_insight` in `lib/reporting/cube.ts` (`box`, `needsInsight`, `first`; `mergeLayout` drops it without the right and seats it first), teaser body `InsightWidget` (`reports/ringkasan/insight-widget.tsx`) inside `WidgetShell` (`badge`) with `teaserInsightItems`; the page `reports/insight/page.tsx` (list-detail, day list, `?day=`) with `BriefView` (`reports/insight/brief-view.tsx`); the tab in `reports/report-tabs.tsx` (`showInsight`); `lib/ai/insights.ts` (prompt, `generateInsight`, `listInsightDays`), `lib/ai/insight-brief.ts` (sections, `parseInsightItems`, `teaserInsightItems`, `briefShareText`), `lib/ai/insight-facts.ts` (`assembleFacts`), `lib/ai/insight-view.ts` (scope from `sales_mission_ai` read scope, `canSeeInsight`, `buildInsightView`); `ensureTodayInsight` / `regenerateTodayInsight`; the ten-minute run at `app/api/ai/insights/run` called by Supabase Cron; switches under Aturan aktivitas → Insight AI; `AskPanel` (`reports/ringkasan/ask-panel.tsx`) off `RingkasanToolbar`'s `onAsk`, `askSalesData` with `buildAskContext` in `lib/ai/ask-context.ts` (`ROW_CAPS`) and the row shapers in `lib/ai/ask-rows.ts` |
| The daily insight is an analyst brief: the app owns the numbers, the model owns the synthesis | A brief that only reads counts back ("12 laporan hari ini, naik dari 9") tells management what the widgets already show, so it is read once and then ignored; what a sales lead actually needs is what the reps heard and what to do about it. The split that makes that safe is the one Gong, Salesforce Einstein and HubSpot Breeze all use: every figure is computed by the application and handed over named (`insight-facts.ts`), and the prompt forbids arithmetic, so a number in the brief can always be checked on a widget, while the qualitative layer is reasoned over the reps' own words — the visit summary in full (up to 1500 characters, whitespace collapsed), the client's needs, the products asked for, the competitor named, who was met with their job title, whether they decide, and their DISC reading where the unit assesses it — plus the week's reports in shorter form, so a theme across visits can be seen at all. Money is passed as text the app formatted ("Rp 4.050.000.000") and the prompt may use no other form, because a model re-grouping digits is how a rupiah loses a zero. A comparison with a week that has no predecessor is marked as a baseline, so the brief says "14 laporan minggu ini" instead of "naik dari 0". The brief is three sections, in the order a decision is made: **Perlu ditindak** (named and concrete: a hot or HQL client with no follow-up or a late one, whose reports are outstanding, prospects past their contact date, opportunities not yet pushed to LeadEngine), **Yang terdengar di lapangan** (themes across the summaries, products in demand, competitors and at which client, what the people met signal, what changed since that client's previous visit), **Rekomendasi** (one to three actions phrased as decisions, each resting on named evidence). Two to four points per section, nine at most, one or two sentences each: a brief nobody finishes is not a brief. Because the model now reasons over text rather than reciting counts, each point may name the reports it was written from, and the app turns those ids into "Lihat laporan" plus the client's name links — ids that were not in the facts are dropped and the client's name is resolved through the reader's own session, so a link can never label a record that person may not open. The brief also leaves the app as plain text through the same two WhatsApp roads as a visit report (phone share sheet; desk app probe, then WhatsApp Web), because that is where a morning brief is actually read Money reaches the model only in its formatted form (the raw number is stripped before the facts are serialised), because a model handed both eventually writes the digits. On the page a point is plain prose with its ways out under it as small links (the list that answers it, the reports it rests on), never the sentence itself dressed as a dotted link; section labels are the same sentence-case label-medium the report card uses over its bands; the card foot stacks provenance over actions when there is no room for both on one line — on the brief page below the small breakpoint, on the Ringkasan card whenever the card itself is narrower than about 440px (a container query on the widget shell, since a card in a grid is narrow on a wide screen too), because a provenance sentence squeezed into a column beside two buttons reads as five short lines and is not read at all. **A brief being written survives navigation, and a dead claim never sticks.** Writing one takes a reasoning model a minute or more, so the row is claimed as "pending" before the call and the page is answered at once; a page that then sat on "Menyusun brief…" was lying the moment the person walked to another page and came back, so while the row is a claim the page asks again every five seconds and settles itself, the way Gmail's "Sending…" and Notion's background jobs finish without anyone pressing refresh — asking is cheap because a live claim is handed back as a read, never a second model call. The wait is the person's, not the clock's: a hidden tab stops asking and the time it slept does not count against it (`document.visibilityState`), and after four minutes of real waiting the page stops and says what it knows — the row's own failure sentence, or "Brief belum selesai dibuat; coba Buat ulang." — because a spinner with no end is worse than a sentence. While it waits, the M3 progress indicator carries supporting text that hands the wait back: "Boleh pindah halaman; brief tetap disusun di server." And the claim is a lease, not a lock: a server that dies mid-generation (function limit, crash, deploy) would otherwise leave the row pending for the rest of the day, with every later call and Buat ulang itself returning the stuck claim, so a claim last written more than three minutes ago is not honoured and the next caller — an open, the ten-minute schedule, or Buat ulang — takes it over and writes the brief again | `SYSTEM_PROMPT` and `generateInsight` in `lib/ai/insights.ts` (max 9 items, temperature 0.3); the facts in `lib/ai/insight-facts.ts` (`assembleFacts`: today's reports capped at 20 with `summary` at 1500 chars, `week.recentReports` at 40 with 400, `followUps.lateList`, `prospects.list`, `previousVisit`, `estimatedValueText`, `reportsBaseline`); sections, parsing and the share text in `lib/ai/insight-brief.ts`; evidence resolved by `buildInsightView` in `lib/ai/insight-view.ts` and rendered by `BriefView`; the shared roads in `components/whatsapp-share-button.tsx`; the claim lease and the wait's rules as pure helpers in `lib/ai/insight-brief.ts` (`STALE_CLAIM_MS`, `isStaleClaim`, `isLiveClaim`, `INSIGHT_POLL_MS`, `INSIGHT_POLL_LIMIT_MS`, `INSIGHT_UNFINISHED_MESSAGE`, tested in `lib/ai/insights.test.ts`), honoured by `generateInsight` and `dueTrigger` in `lib/ai/insights.ts` and by `ensureTodayInsight`; the waiting itself in `hooks/use-today-insight.ts`, used by both `BriefView` and `InsightWidget` |
| A chat pane remembers, per account, and its history is the person's own to keep or throw away | Tanya AI's thread used to live only in the open pane: closing it, or walking to another page, threw the conversation away, and the log the unit keeps for cost and quality (`ai_questions`) was never shown back to the person who asked, so the same question got asked twice. Every chat pane people come back to remembers — Copilot, ChatGPT's side panes, Gemini in Workspace — so a conversation is a row of that person's own: the first question as its title (cut at 80 characters, the width a list row reads), the turns as they were asked, nothing else. Own rows only: RLS is `user_id = auth.uid()` inside the person's own company, so history is never a manager's window into what their reps asked, and the context the model is given is read from the stored row on the server rather than from whatever the client sent, the same rule every other write follows. Opening the pane resumes the most recent conversation of today and otherwise starts empty, because a day's questions are one sitting and yesterday's are a different one. **Riwayat** lists the rest inside the pane rather than on a page of its own (M3 list: 56dp rows, tonal selected state on the one being read, label-medium heading in sentence case), each row saying title, when it was last used as a relative date ("Hari ini 09.05", "Kemarin", "18 Sep" — a history list answers "which one was that" faster with a relative date than with a timestamp) and how many questions it holds; **Percakapan baru** starts a fresh one. Hapus is a text button on the row that deletes on the tap, with a toast ("Percakapan dihapus.") and no confirm dialog, because M3 keeps dialogs for losses that are heavy and unrecoverable and this is one person throwing away their own note. A question is a note, not a record, so rows are swept after 90 days by the scheduled run — the one service-client query in the app with no `company_id` filter, because retention deletes by age and not by tenant, placed after the briefs so a failed sweep can never cost anybody their brief | `sales_mission.ai_conversations` in migration `20260922100000_ai_conversations.sql` (own-row RLS for select, insert, update and delete, `turn_count` generated from `turns`, `(user_id, updated_at desc)` index); the pure rules in `lib/ai/ask-conversations.ts` (`conversationTitle`, `parseTurns`, `appendTurn`, `historyTurns`, `conversationDateLabel`, `isFromToday`, `retentionCutoff`, `CONVERSATION_RETENTION_DAYS`) with `lib/ai/ask-conversations.test.ts`; `listConversations` / `getConversation` / `deleteConversation` in `app/actions/ai-conversation-actions.ts` and the `conversationId` branch plus `keepTurn` in `app/actions/ai-ask-actions.ts` (session client, RLS); the header row, the Riwayat list and its rows in `app/workspace/reports/ringkasan/ask-panel.tsx`; the 90-day sweep at the end of `app/api/ai/insights/run/route.ts`; the rule as the user reads it in `app/workspace/panduan/page.tsx` |
| Who may see and change what | Action and reach are two settings on one page: per object View/Edit/Delete, each with Everything / Team only / Owned only (HubSpot); object CRUD plus a role hierarchy over record ownership (Salesforce) | Role & Izin: modul × Lihat/Buat/Ubah/Hapus, with a reach menu under Lihat (**Cakupan lihat**, enforced by row security so every list, RPC, export and KPI agrees) and under Ubah (**Cakupan ubah**, also Hapus and record-bound actions; never wider than Lihat). Owners are fixed per record: mission = sales utama + scheduler (assignees see it too), report = sales utama, prospect = holder (unowned is everyone's). Tim follows the Atasan chain from Settings → Users. A narrowed list says so in its description; every refusal names the Cakupan; availability for scheduling stays whole through a definer function |
| Board controls | Page action beside the title (as on every page); one toolbar row with what-to-show (segmented, facets) on the left and how-it-is-shown (live status, a "Tampilan" menu) on the right; view options in a menu, never as permanently-ticked chips (M3 top app bar, segmented button, menu) | `BoardActions` in the header slot, `BoardToolbar` under it; panel toggles are `DropdownMenuCheckboxItem`s |
| Dark distant screen | An M3 dark scheme from the brand blue: five surface tones carry the structure instead of borders (page, card, row, raised spotlight, track), `on-surface`/`on-surface-variant` inks, `primary-container` for the one thing the room should look at, tertiary amber as the live signal (the pulsing dot and ring), and a green custom colour meaning done (check marks, the progress bar, the all-done card). Status is a coloured dot plus a word, never a filled chip: a wall has no pointer, so nothing borrows an interactive shape. People get one of six identity colours, hashed from the name so a person keeps theirs, never used for status. There is no error role: a cancelled visit is taken off the board. A past row dims by stepping down in tone, not opacity. A faint primary glow in one corner keeps the dark from reading as a dead screen. One face, Material's own Roboto Flex with tabular figures, up to 600; no monospace (M3 dark theme, tonal surfaces, custom colours, type scale, reduced motion) | `--board-*` in `app/globals.css` (every text pair clears 4.5:1 on the surface it sits on); Roboto Flex loaded for `/board` only in `app/board/layout.tsx`; `StatusCell` and `avatarHue` in `board-view.tsx` |
| Postponed is not cancelled | A cancel dialog asks which kind of off this is: no follow-up, or postponed with a promised call-back date (Google Calendar's cancel vs propose a new time; Calendly's reschedule keeps the booking's history). A postponed item leaves the calendar and the board but stays on the to-do list until its replacement exists, and the two link to each other | Batalkan dialog: "Batal, tidak ada lanjutan" / "Ditunda, jadwal menyusul" plus a date (default three days out); Hari ini "Perlu dijadwalkan ulang" with the promise date, overdue in red; Jadwalkan lagi records `rescheduled_to_id` on the old mission, whose banner then says "Sudah dijadwalkan ulang" with a link |
| A calendar fits the screen | The month view fills the height under the page header and never scrolls the page (Google Calendar, Outlook): the week rows share the height, the day pane beside it is level at the top and scrolls only its own list. Two panes never share a height by stretching one to the other; they share the viewport. On a short screen the day cells keep a floor and the page scrolls rather than crushing them | Kalender: the section is `lg:h-full`; the month grid `grid-template-rows: auto repeat(n, minmax(0,1fr))` for the month's n weeks; from lg each day cell is a size container (`.cal-cell`), so its content never sizes the row and container queries show two chips, one chip, or only the count as the cell's height allows; the day pane `flex-col min-h-0` with the list `flex-1 overflow-y-auto` |
| Not before it happened | A record of an event cannot precede the event (HubSpot and Salesforce refuse a logged meeting in the future; field-service apps cannot complete a job before it starts). The rule is a tenant setting, on by default. M3 governs how it shows: the date picker stops at today, the supporting text says why, the action that is not available yet is replaced by the date it becomes available, never a dialog, and the server checks it again | `reportLocked` / `visitTimeInFuture`; Pengaturan → Aktivitas "Laporan hanya setelah kunjungan"; the detail page's report button reads "Laporan bisa diisi mulai Sen, 21 Sep" until 00.00 of the scheduled day; the form's date field has `max` today and the send button waits; draft save and submit refuse on the server |
| Undo a send, never delete it | A sent record can be taken back by whoever may edit it, with a reason: it returns to draft with its content, the thing it completed is reopened, the sent version is archived, the people who were told of the send are told of the undo (HubSpot and Salesforce let an owner or admin remove a logged activity with an audit trail; field-service apps let a supervisor reopen a completed job). Deleting is then the ordinary draft path with its confirmation. What crossed into another system stays there and the copy says so | `withdrawVisitReport`: "Tarik kembali" next to "Ubah laporan", the same `canEditSubmittedReport` verdict; the mission returns to the status the send took it from (`statusBeforeCompletion`); RESULT_WITHDRAWN notification; a pushed lead stays in LeadEngine |
| Facts as property rows | A record's facts are rows, not columns: leading icon, label in the muted ink, value in the regular body weight, each row as tall as its own value (M3 list item; Linear and Notion properties; HubSpot's About panel; Google Calendar's event detail). Columns make the tallest value stretch every neighbour. Labels are sentence-case label text, not tracked capitals (Material 3 retired "overline"); weight marks hierarchy once per card, in the title | `Fact` on the activity page: a `dl` of `icon · label · value` rows, label 7rem from sm, stacked on a phone; Tujuan kunjungan is one of the rows |
| Whose calendar, where, what kind, and how the day reads | A shared calendar answers "what is the team doing" by default and "what am I doing" or "what is one person carrying" on request (Google Calendar's calendar list, Outlook's group calendars). The switch is the activity list's own "Saya" toggle chip and Sales facet, not a new control; there is no "Semua" chip, because the whole team is what shows with nothing chosen, the state "Bersihkan semua" returns to, as on Aktivitas (a "Semua" chip beside "Saya" was an option that was really the absence of one, and a second way to clear Sales). Beside them two more facets, Lokasi and Jenis, listing only what the month holds, because a day with twenty-six visits is read by city before by name; the toolbar stops there (Google Calendar and Outlook keep the calendar's bar light; the rest of the facets live on Aktivitas). The choice is remembered per person; the month and the day are a place, not a view, and are never restored. No colour per person: with a dozen reps the colours stop meaning anything; instead the day pane shows the primary sales' face as a trailing avatar with the name still in words (M3 list item with a trailing avatar; Google Calendar's schedule view, Linear, Asana). The day pane can be grouped by Lokasi or by Sales from one icon button in its header that opens an M3 menu of radio items (the widget size menu's shape): an M3 list subheader per group with its count, the grouped fact dropped from each row since the header says it, the unnamed last. The whole view is the URL, so the public calendar carries it and reads the same | `CalendarView` in `lib/missions/calendar-filter.ts` (`parseCalendarView`, `calendarHref`, `filterCalendarMissions`, `calendarFacetValues`, `groupDayMissions`); `CalendarFilter` (the "Saya" `ToggleChip` first, since the row scrolls sideways on a phone, then the Sales, Lokasi and Jenis facets, then "Bersihkan semua" while anything narrows) and `JadwalFilter` on the public calendar (the same without "Saya"); `DayPane` (`people`, `group`, `tools`) with `DayGroupMenu`; cookie `sa-view-calendar` holds sales, location, type, group; `publicCalendarHref` / `parsePublicView` on the public calendar |
| Dashboard widgets | A summary is a grid of cards, each answering one question; the reader arranges them, hides what they never read, and the arrangement is theirs on every device. Arranging is a mode entered from the toolbar's right side; handles and per-card menus appear only in it; the filters above apply to every card (Geckoboard, Grafana, HubSpot dashboards; M3 cards, menus, segmented button) | Ringkasan: period segmented + Sales facet in the URL, remembered as `sa-view-ringkasan` (the daily card's `day` is a place and never remembered); "Atur widget" → `DashboardEditor` over react-grid-layout (twelve columns, 40px rows, vertical compaction), `sales_mission.user_dashboards` with self-only RLS, `WidgetShell` menu in edit mode |
| Custom widgets | A person composes a card from what the platform already measures: one measure (or two to compare), one grouping, an optional split, and a chart shape that fits the grouping; the form offers only combinations that mean something, and the built-in cards are instances of the same engine (Metabase's question builder, HubSpot custom reports) | `fn_report_cube` (measure × group × series, WIB days, row security), `APPLICABILITY` + `validateWidget` in `lib/reporting/cube.ts`, `WidgetConfigurator`, up to 12 per person |
| Chart colours are tokens | Series colours come from `--chart-1..6`, derived from the brand and its semantic inks and each cleared at 3:1 on the card (WCAG 1.4.11); a measure keeps one colour on every card; interest kinds have fixed colours; "Lainnya" and the unset are the muted ink. Animations are off: a card is read, not watched | `widget-view.ts` assigns colours; recharts with `isAnimationActive={false}` and `accessibilityLayer` |
| Resizing a widget | A card is dragged by its title and resized from its corner in grid units, never below the smallest box it still reads in (Android home-screen widgets, Grafana). The corner handle is visible the whole time the board is in Atur widget, not only on hover: an affordance that appears on hover does not exist on a touch screen, and a person who cannot find it concludes the card cannot be made smaller, which is what happened. The mode opens with one line naming its two gestures. The quick sizes in each card's ⋮ are honest about the minimum: a number, a trend, a donut or a bar list may take Ringkas, a quarter of the width by four rows (~208px), and that is the size a number card is born at, because one figure never needed the 7-row cell that Kecil is; the custom-widget form picks Ringkas when a number shape is chosen and greys out any size a shape cannot fit. A plain number may still be dragged down to three rows (title, value, hint); a trend keeps four for its sparkline. The board stays smooth because the page does not re-render while a card moves: positions are committed when the pointer is released, each card body is memoised, and chart animation is off. The default board fills whole rows so the bottom edge is level | `DashboardGrid` (`onDragStop`/`onResizeStop` commit, `onLayoutChange` only for compaction), `.dashboard-grid.editing .react-resizable-handle` in `globals.css`, the hint line in `RingkasanToolbar`; `WIDGET_SIZES` with `compact`, `SIZE_BOX`, `MIN_BOX`, `NUMBER_MIN_BOX` and `minBoxFor` in `lib/reporting/cube.ts`; the number built-ins are `size: "compact"`; `sizeFits` disables presets in `WidgetShell` and `WidgetConfigurator`; `clampBox` on read; the six defaults total 72 cells, six full rows |
| Names on an axis | A category axis of names never rotates its labels; when the names would collide the chart turns, so each name has a row to itself on the left, cut with an ellipsis past sixteen characters and given in full in the tooltip (Material data visualisation guidelines, Datawrapper, Apple HIG charts). Dates keep standing bars and thin their ticks instead | `DayBars horizontal` for any non-time grouping; `NameTick` truncates; time axes use `preserveStartEnd` |
| Chart shapes on offer | The form offers every shape that answers the question and puts the preferred one first: comparison → bars (standing for dates, lying for names, either on request), change over time → lines or area, part of a whole → donut or pie with one measure, one value → a number, or a number with the period's daily trend under it; a split adds stacking; two measures never become two pies (Material data visualisation guidelines; Metabase's chart picker). The cards speak the product's words: prospek, aktivitas, laporan | `chartsFor` in `cube.ts` with `CHART_LABELS`; `MEASURE_LABELS` |
| Bar list, not a meter | One measure over names is a bar list rather than an axis chart: label, value and share on one line with the bar under it, because the numbers read without a tooltip and a phone fits a dozen rows (Google Analytics 4, Plausible, Vercel Analytics; Material data visualisation on labelling values directly). The bar must still be a bar: 8dp tall, 2dp corners (4dp on 8dp is a pill), no track behind it (a full-width track is the linear progress indicator's shape and says "of 100%"), its length relative to the longest row, and its colour the measure's token so the card matches the axis chart of the same measure beside it. The top twelve rows stay, the rest fold into one muted "Lainnya (N …)" row so nothing is silently dropped, shares are of the whole total (never of the rows shown), "Lihat semua" opens every row in a bottom sheet (a dialog on a desk), and a row that a list can answer is a link into that list narrowed to the row and the period, marked with a trailing chevron because a phone has no hover (M3 list item with trailing icon; GA4 "View report", Plausible's detail modal). Which rows link is a rule, not a per-card switch: every grouping a list has a facet for (industri, jenis, sales, klien on Aktivitas; sales, klien, hasil, minat on Laporan, with peluang or lead pinned for those measures), never prospects (no period facet) and never a time grouping; the widget form states the rule under Bentuk instead of offering a toggle (GA4, Plausible, HubSpot link every answerable row; Metabase's per-card click behaviour is for authors choosing among destinations, which this board does not have). Same tab, always: this is navigation inside the app, Kembali returns to the board as it was, and on an iPhone running the app from the home screen a new tab would leave for Safari (M3 navigation, WCAG 3.2.5; GA4, HubSpot). The row is a real link, so Cmd/Ctrl+click and a long press still give a new tab when the person asks for one, the same convention as `row-link.ts` on the tables | `ListBars` / `BarRow` in `reports/ringkasan/charts/plain.tsx`; `presentCube` builds `rows`, `all`, `total`, `drill` in `lib/reporting/widget-view.ts` (`TOP_ROWS`, `listDrill`); `listDrill` / `drillParam` in `cube.ts` decide the list and the facet value (outcome and interest kinds → the tenant's codes via `LabelContext.choices`), `rowHref` builds the link with `date=custom`; `WidgetConfigurator` shows the rule under Bentuk |
| Peers side by side, not behind a tab | Two options a person must know about before choosing (a personal feed and a team feed) sit as two cards side by side on a wide window, each with its own state and action; the shared instructions come once below. Tabs would hide one peer, and a narrow column with an empty right half ignores the width class (M3 canonical layouts: expanded windows use two panes; tabs are for related content not needed together) | Kalender saya: `FeedSetup` × 2 in `lg:grid-cols-2`, a dashed placeholder card when the role has no team reach, `SubscribeSteps` in three columns under both |
| A default the admin sets by example | The unit's default board is not a separate editor: an admin arranges their own board, then publishes it as the default from the same menu (Grafana's "save as default", Notion's template from a page). A person's own arrangement always wins; reset returns to the unit's default, then to the built-ins | `publishDashboardDefault` copies the caller's `user_dashboards` row into `sales_mission.company_dashboards`; the page reads own ?? company ?? built-ins; the menu item is gated by Pengaturan → ubah |
| A public window, not a public app | Someone who only needs to look does not need an account: a secret, revocable link opens the very same component read-only, with every viewer-shaped thing removed — no "mine", no remembered view, no links into records, no buttons (Google Calendar's secret address, Calendly and Notion public pages). What the link opens and whether it shows client names are bound to the token; the month, the day and the filter stay in the URL, so a filtered link is shareable as it is | `board_tokens.kind` ('screen' | 'calendar') refused by the other route; `/jadwal/[token]` renders `MonthGrid` and `DayPane`, the same two components as `/workspace/calendar`, without `itemHref` or `action` |
| Pressing send always does something | A submit button is never greyed out because the form is incomplete: a disabled control cannot take focus, so it can never tell anyone why it is disabled, and the reader is left hunting. The press is always allowed and it lands the person on the first thing standing in the way, in page order — which is what the browser's own required check already does on a form of plain inputs, so a form of controlled fields writes the same jump by hand rather than inventing a second behaviour. A blocker that is not an empty field (a visit dated later than today) is named the same way and jumps to the same place. Only work already in flight disables the button (WCAG 3.3.1 Error Identification and 3.3.2 Labels or Instructions; M3 keeps error text in the supporting-text slot, and has nothing to say about greying a button) | Activity and prospect forms lean on native `required`; the report form's `blockers` array and `jumpTo`, with "Belum lengkap" naming every field at once above the button |
| A long text field grows with what is written | A fixed-height textarea makes a person compose into a well: three sentences on a phone and the first one has sunk out of sight, so nobody rereads what they wrote and the summary that feeds the WhatsApp share, the export and the AI brief comes out shorter than the visit deserves. Material's multi-line text field grows with its content and carries no resize grip — the height is the content's job, not something to drag — and Gmail, Google Keep, HubSpot's note editor and Notion all grow the same way. So every field where a person writes prose is one component: `minRows` tall while it is empty (2 for a short note — a reason, a cancel or reschedule reason, a follow-up note, a contact-attempt note; 3 for prose — a summary, appointment notes, a custom long answer, a CRM remark), growing line by line, stopping at 14 rows and scrolling inside itself after that so the send button is never pushed off a phone screen. The height is measured by setting it to `auto` and reading `scrollHeight` against the computed line height and vertical padding, in a layout effect so there is no flash, and measured again whenever the value arrives from outside (a restored draft, a placeholder dropped in by a button), once the real font has loaded, and when the box's width changes. Nothing scrolls the page: growing the box keeps the caret in view by itself, and `scrollIntoView` would drag the whole shell with it (see the scrolling helper). Reps already structure a summary by typing "1." and "2." by hand, so the field carries the structure on: Enter at the end of an item opens the next one with the same marker, a number counting on (1. → 2., 9. → 10.), and Enter on an item nobody wrote anything in removes the marker and ends the list, which is how Keep and Apple Notes get out of one without reaching for the mouse; Shift+Enter is always a bare line break inside the item. The edit is made with `setRangeText` on the native element and announced with an `input` event, so the caret lands right and a controlled value still reaches `onChange`. Not rich text: the summary is read back as plain text by the WhatsApp share, the Excel and CSV export and the AI brief, Material has no rich-text component, and Salesforce's long text areas are plain for the same reason. The two template fields under Pengaturan grow but switch the continuation off, because a marker typed into a share format or an option name would become part of the value. | `AutoTextarea` in `components/ui/auto-textarea.tsx` (`minRows`, `maxRows`, `lists`, `AUTO_TEXTAREA_CLASS`, `resize-none`), marker logic in `lib/ui/list-continuation.ts` with its tests. Every prose field is one: Ringkasan pertemuan, the custom long answers and the DISC note "Cara menghadapi orang ini" — which stops being a single-line `Input` — in `visit-report-form.tsx`; Catatan janji temu and the custom long answers in `mission-form.tsx`; the prospect notes and custom long answers in `prospect-form.tsx`; the contact-attempt note in `prospect-dialogs.tsx`; the follow-up note and its cancel note in `follow-up-panel.tsx`; `supporting-notes.tsx`; the reschedule and cancel reasons; the clarification note and the withdrawal reason in `report-admin-actions.tsx`; the CRM remark in `push-lead.tsx`; and, with `lists={false}`, the WhatsApp greeting and the share format in `mission-settings-form.tsx` and the paste-a-list box in `options-editor.tsx` |
| An export carries the whole record, in the tenant's own words | A rep answers a form and an export used to hand back seventeen columns of codes: `visit_outcome`, `MET_DECISION_MAKER`, a count of the people met instead of the people. Nobody can act on that, so the work was done again by hand off the screen. An export is the record, so its columns are the record's columns — the report form's own fields, in the admin's order, headed with the admin's current labels and filled with the tenant's own choice labels, custom fields included automatically, exactly as the activity export already derives its columns from the form rather than hardcoding them (HubSpot and Pipedrive export every property of an object including the custom ones; Salesforce reports list fields, not column ids). The label is resolved from the frozen `reportingKey` at write time, never stored, so a renamed field is renamed in the file too. A report holds more facts than one row of columns, and it still has to be one row: the people met are written as numbered column groups inside that row — Kontak 1 · Nama, Kontak 1 · Jabatan, Kontak 1 · Telepon, Kontak 1 · Email, Kontak 1 · Pengambil keputusan, Kontak 1 · DISC, Kontak 1 · Catatan DISC, then Kontak 2 and on — as many groups as the widest report in the export and never fewer than one, so the header exists even when nobody was met, and a report with fewer people leaves the later groups' cells empty. That is the idiom a contact export already uses (Google Contacts writes Phone 1 - Value, Phone 2 - Value; Salesforce and HubSpot number repeated columns the same way): every fact is its own cell, so a phone number, a decision maker or a DISC letter can be filtered, sorted and pivoted, while the same people glued into one cell behind separators can only be read by eye. The group headers are fixed Indonesian rather than the field's label, because they are not that field's answer but the columns it stands for; they sit where the admin put Ketemu siapa, so reordering the field moves the whole block. What is still a list keeps its own sheet, keyed back by the report — Kontak for one row per person met, with the DISC note, who read it and when, which is the shape to analyse people in rather than in groups across a row; Catatan pendukung for what the rest of the team wrote — the same answer as a CRM export of associated records as separate files. Attachments cannot be posted into a spreadsheet and their bucket must stay private, so a photo or a recording is written as its file name and a signed link valid seven days, long enough to survive the weekend between download and reading, short enough that a forwarded file does not become a permanent public folder; every path of the whole export is signed in one batched pass rather than per report, and a path that fails to sign is still named. Because those links do expire, every row also carries the durable road back — an absolute link to the activity, which asks for a login and therefore never leaks. Times are WIB like everywhere else, written as a day column and a clock column so a spreadsheet can sort them; Ya/Tidak, lists joined with a comma, money a bare number typed as a number so a sum is a sum. CSV has no second sheet, so it is sheet one alone | `lib/reporting/report-export.ts` builds the three sheets (`buildReportExport`, `contactGroupHeaders`, `contactCells`, `attachmentLines`, `collectAttachmentPaths`, `EXPORT_LINK_SECONDS`) and is pure and unit-tested in `report-export.test.ts`; `lib/reporting/report-export-queries.ts` (`loadReportExport`) reads through the person's own session, filtered by `company_id` and chunked at 500 ids, reusing `summarizeFollowUpsByReport`; `app/workspace/reports/export/route.ts` keeps the access check, the legacy from/to fallback and the file naming, signs photos and audio through `signPhotoUrls` / `signAudioUrls` with their new `expiresIn`, and writes sheets Laporan, Kontak and — only when a note exists — Catatan pendukung, each with a frozen header row |
