/**
 * The sentence an admin types to empty the tenant's missions. Lives outside
 * the server-action module because that module may only export functions.
 */
export const CLEAR_ALL_PHRASE = "HAPUS SEMUA MISSION"
