# Shared Packages

Shared packages belong here only when both applications have a stable need.

Planned packages:

- `ui` — presentation primitives and design tokens.
- `auth` — provider-neutral auth helpers and Entra claim parsing.
- `api-contracts` — versioned LeadEngine API schemas.

Do not put application-specific database queries or business rules here.
